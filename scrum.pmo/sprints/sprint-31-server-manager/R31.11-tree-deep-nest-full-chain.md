# R31.11 — traceability TREE must deep-nest the full chain (Tron 2026-07-26)

**Tron:** two screenshots — "first is how it IS, second how it SHOULD BE."

## THE GAP (this is the real "not complete in the tree")
- **CURRENT (S31, tree-CURRENT-no-deep-nest-IMG_4701.png):** expanding a Task shows UC (e.g. `serverManager.ownerGuard`) + its Requirement, but the UC node **STOPS at the UC** — it does NOT expand into the nested Class → Method → Impl → Test chain in the TREE. The full chain appears ONLY in the bottom DRAWER (which resolves correctly — Class ServerManagerGuard → Method assertOwner → Impl → Test, so R31.10 resolution IS working).
- **TARGET (S30, tree-TARGET-S30-deep-nest-IMG_4702.png):** expanding Task 30.1's UC `traceTree.currentSprintEagerLazy` deep-nests the WHOLE chain as expandable TREE nodes, each with its type-icon + child-count badge: UC(puzzle) → **Class RbTraceTree**(cube, badge 19) → **Method renderCurrentSprintEagerLazy**(terminal) → **Impl** (`</>`) → **Test**(flask, green✓).

**So: S30's tree deep-nests UC→Class→Method→Impl→Test as expandable nodes; S31's tree does NOT (stops at UC).** The chain DATA is complete (drawer proves it) — the gap is the TREE RENDERING/nesting not walking the full chain into expandable child nodes for S31.

## NOT a data-mint gap (chains are complete + resolve correctly)
The drawer shows the correct full chain for S31 (assertOwner, not a sibling — R31.10 works). So this is NOT "mint missing units." It's: **why does the tree deep-nest the chain for S30 UCs but not S31 UCs?** = a rendering/tree-walk difference. Architect diagnoses.

## ROUTE (scenario-first, feature-bug: architect diagnose → expert fix → tester gate → Tron)
- **architect:** DIAGNOSE (measure, cite file:line) — compare how the tree expands a S30 UC (deep-nests Class→Method→Impl→Test) vs a S31 UC (stops at UC). What makes S30's chain nodes render as expandable tree children while S31's don't? Candidates: (a) the fetchAndRenderChildren / chain-walk that nests UC→Class→Method→Impl→Test relies on a structure/forward-ref S30's units have and S31's lack; (b) a queryMode/chainMethod nesting path (post-R31.10) that renders the Method but not the deeper Class/Method/Impl/Test as nested nodes; (c) child-count-badge/eager-lazy expansion not populating for S31 chain nodes. Prove the root.
- **req:** capture R31.11 AC — 'the traceability tree deep-nests the FULL chain (UC→Class→Method→Impl→Test) as expandable nodes with type-icons + badges for EVERY sprint incl S31, matching S30' (pin to the S30 target screenshot + the S31 observePosition/ownerGuard cases @390).
- **expert:** fix per architect design (shared rb-trace-tree — fixes ALL sprints by construction, DRY). **tester:** gate — expand a S31 Task's UC → the full Class→Method→Impl→Test nests like S30, @390. **Tron:** device re-verify (S31 tree looks like the S30 target).

Ref: tree-CURRENT-no-deep-nest-IMG_4701.png (S31 stops at UC) + tree-TARGET-S30-deep-nest-IMG_4702.png (S30 full deep-nest).

---

## ARCHITECT DIAGNOSIS + FIX (robbin-architect 2026-07-26) — ROOT PROVEN, confirms tester lead 841c90813
**MEASURED on disk (cite):**
- S31 UC `serverManager.ownerGuard` (40802701) model keys = `['class','method',...]` → **`class`(singular)=ior:instance:1d6933c7 (ServerManagerGuard) POPULATED; `classes`(plural)=ABSENT**; `method`=8bb1842f.
- S30 UC `traceTree.currentSprintEagerLazy` (e22113cd) → **has BOTH `class`(singular) AND `classes`(plural array)** populated.
- `src/ts/shared/chain-model.ts:18` — `UseCase: { scenarioFwd:['classes'], traceFwd:['class'], clientFwd:['classes'] }` → **the forward key is INCONSISTENT: scenario/client read PLURAL `classes`, trace reads SINGULAR `class`.**
- The /app tree (IMG_4701 breadcrumb "Browse·App") fetches `/api/trace/children` in **scenario mode** (rb-trace-tree default, no `?mode=trace`) → `forwardKeysForMode(UseCase,'scenario')`=`['classes']` (server.ts:1531) → S31 UC has no `classes` → **childRefs=[] → UC hasChildren:false → tree STOPS at UC.** /trace mode uses `['class']` → S31 would nest. The DRAWER shows the full chain because R31.10 resolves via `UC.method` (server.ts:1644), a SEPARATE path from the tree's forward-walk.

**ROOT (confirmed = tester lead, pinpointed):** key-name mismatch between MINT and READ. S31 UCs store the class link in the CANONICAL SINGULAR `class` (architect SKILL: "UC.class singular = the implementing class"; R31.10 already made singular `method` authoritative). The scenario/client forward-walk reads only PLURAL `classes`. S30 UCs happen to carry BOTH keys (redundant) → nest in every mode → masked the bug. NOT a data-mint gap (the `class` link exists + resolves).

### FIX (shared, all-sprints DRY, correct-by-construction) — hand expert
| # | File:line | Current (BUG) | Fix |
|---|-----------|---------------|-----|
| 1 (ROOT) | `src/ts/shared/chain-model.ts:18` | `UseCase: { scenarioFwd:['classes'], traceFwd:['class'], …, clientFwd:['classes'] }` | reconcile all modes to read singular `class` (canonical) + plural `classes` (legacy fallback): `scenarioFwd:['class','classes'], traceFwd:['class','classes'], clientFwd:['class','classes']`. The generic resolver already accepts BOTH a string value (server.ts:1605-1608) and an array (1600-1604), so singular `class` resolves with no other change. |
| 2 (de-dup) | `src/ts/server/server.ts:1610` | `childRefs = childRefs.filter(ref => ref !== uuid)` | `childRefs = [...new Set(childRefs)].filter(ref => ref !== uuid)` — S30 UCs carry BOTH `class`+`classes`→ same Class ref twice → would render the Class node TWICE without de-dup. |
| 3 (parent symmetry) | `src/ts/server/server.ts:1660` FWD_SCAN | `UseCase: ['classes']` | `UseCase: ['class','classes']` — reverse parent/ancestor resolution (revealNode deep-link walk) must also find a S31 Class's parent-UC via singular `class`. |

**By construction:** chain-model.ts is the SINGLE forward-key source consumed by the server child-walk (server.ts:1531) for EVERY sprint + the client (clientFwd) → one config fix deep-nests UC→Class→Method→Impl→Test for S31 and all future singular-`class` UCs, matching S30, with zero unit re-mint. Mirrors R31.10 (singular canonical field authoritative in all modes).
- **Scope/gate:** shared chain-model + generic /api/trace/children resolver → regression-check S30 (still nests, NO double Class from de-dup) + S31 (now nests) + /trace mode both. Server change = real restart. **GATE (tester @390):** expand a S31 Task's UC (serverManager.ownerGuard, and observePosition) → full Class→Method→Impl→Test nests with type-icons+badges like S30; S30 unchanged (no dup Class); then Tron device.
- **Chain:** fixes the tree render walk; rides existing units (no new UC/Class/Method). If req mints an R31.11 Test, I mint/repoint the Impl marker on the chain-model resolver.

## ARCHITECT BACKSTOP — v0.7.137 / 39af74f13 (robbin-architect 2026-07-26): **PASS (gate-verified via public /api/trace, not by-construction)**
Real restart remoteShells:0.2 (Ctrl-C + npm start per PO, NOT [r] — [r] does client-rebuild only, server change needs process restart). INV-V4 honest signal: uptime **1218→13** (reset), served==committed==**0.7.137**.
- **STATIC (3 rows exact):** chain-model.ts:18 UseCase scenarioFwd/traceFwd/clientFwd all `['class','classes']`; server.ts:1610 `[...new Set(childRefs)]` de-dup; server.ts:1661 FWD_SCAN UseCase `['class','classes']`.
- **LIVE deep-nest (public /api/trace/children — CLI-verifiable, NOT 403-limited):**
  - S31 UC `serverManager.ownerGuard` → **Class ServerManagerGuard** (childCount 3) in BOTH scenario + trace (was `[]` in scenario → tree stopped; now nests). Full walk: UC→Class→**Method assertOwner**→**Impl assertOwner impl**→**Test** (R31.2 ×2) — end-to-end like the S30 target.
  - S31 UC `drawer.observePosition` → Class RbDetailDrawer (childCount 42), both modes.
  - S30 UC `currentSprintEagerLazy` → Class RbTraceTree (childCount 19), **de-dup=ok (single Class, NOT doubled)** despite carrying both `class`+`classes` keys → de-dup proven.
- **Regression:** sacred gate `/server-manager`+`/feature-manager` → 403, `/trace` → 200 unchanged. R31.10 marker e86f0736 (server.ts:1644) = marker-only, no behavior change.
- **Remaining = VISUAL only:** tester @390 renders the nested nodes (icons+badges) on-screen + Tron device (S31 tree looks like S30 target). The DATA/walk is gate-verified PASS via API here. R31.11 chain-walk root CLOSED.

## R31.11 RESIDUAL — 2nd site: per-child hasChildren STAMP (tester gate RED b8217faec, root-confirmed; architect 2026-07-26)
**I OWN THE MISS:** my v0.7.137 backstop verified the child-FETCH resolver (/children/<UC> nests) but NOT the per-child childCount STAMP that the tree renders when expanding a PARENT (/children/<Task31.2> stamps the UC child). The tester's VISUAL @390 (expand Task → UC chevron) caught the real user flow my API-level backstop didn't. LESSON: backstop the USER's render path (parent→child hasChildren stamp), not only the direct-fetch resolution.
**ROOT (measured, = tester b8217faec):** server.ts:1648 `forwardArrays = ['tasks','useCases','classes','methods','implementations','tests','children'].map(k=>childModel[k]).filter(Array.isArray)` → line 1649 `childCount = Σ arr.length`; line 1655 `hasChildren: childCount>0`. Hard-coded PLURAL `classes` only + `.filter(Array.isArray)` DROPS strings → a S31 UC (singular `class` string, no array keys) → forwardArrays=[] → childCount=0 → hasChildren=false → **NO chevron → non-expandable leaf** (IMG_4701 persists). Separate hard-coded path; my chain-model.ts:18 fix reached the resolver (1600-1608) not this stamp.

### FIX (mirror the resolver + chain-model, DRY 2nd site) — hand expert. ★ must DE-DUP (else S30 badge regresses 1→2)
| File:line | Current (BUG) | Fix |
|-----------|---------------|-----|
| `src/ts/server/server.ts:1648-1649` | `const forwardArrays = ['tasks',...,'classes',...].map(k=>childModel[k]).filter(Array.isArray); const childCount = forwardArrays.reduce((s,a)=>s+a.length,0)` | count via the SHARED `forwardKeysForMode(ct, queryMode)` (type-specific, same source as the walk + my :18 fix), collecting UNIQUE refs (string→1, array→each) into a Set: `const cset = new Set<string>(); for (const k of forwardKeysForMode(ct, queryMode as 'scenario'\|'trace')) { const v = childModel[k]; const add=(r:any)=>{const c=String((r&&typeof r==='object')?((r as any).ior\|\|(r as any).uuid\|\|''):r).replace('ior:instance:',''); if(/^[0-9a-f]{8}-/.test(c)) cset.add(c);}; if(Array.isArray(v)) v.forEach(add); else if(typeof v==='string') add(v); } const childCount = cset.size;` |

- **★ DE-DUP IS REQUIRED (correctness, I measured):** the naive tester/PO suggestion `string(1)+array(len)` would REGRESS S30 — a S30 UC carries BOTH `class`+`classes` (same Class) → `forwardKeysForMode(UseCase)=['class','classes']` → 1+1 = **2** (currently correctly **1**, since old code counted only the `classes` array). Collecting UNIQUE refs (Set) → S30=1, S31=1 → hasChildren=true for both, badge accurate. Mirrors the walk's de-dup (server.ts:1610).
- `forwardKeysForMode` already imported (server.ts:22); `ct`=child type, `queryMode` in scope (used :1531). Type-specific = counts only the child's real forward keys (better than the old type-agnostic union).
- **Scope/gate:** SERVER change → real restart (I drive Ctrl-C+npm start). GATE: tester r3111 (b8217faec) GREEN @390 — expand a S31 Task → UC shows chevron + deep-nests Class→Method→Impl→Test; **S30 UC badge stays 1 (NOT 2)** — de-dup regression check; both modes. I backstop at the RENDER/stamp level this time (fetch /children/<Task> → assert UC child hasChildren=true + childCount=1), not just /children/<UC>.
