# R31.11 — traceability TREE must deep-nest the full chain (Tron 2026-07-26)

**Tron:** two screenshots — "first is how it IS, second how it SHOULD BE."

## THE GAP (this is the real "not complete in the tree")
- **CURRENT (S31, tree-CURRENT-no-deep-nest-IMG_4701.png):** expanding a Task shows UC (e.g. `serverManager.ownerGuard`) + its Requirement, but the UC node **STOPS at the UC** — it does NOT expand into the nested Class → Method → Impl → Test chain in the TREE. The full chain appears ONLY in the bottom DRAWER (which resolves correctly — Class ServerManagerGuard → Method assertOwner → Impl → Test, so R31.10 resolution IS working).
- **TARGET (S30, tree-TARGET-S30-deep-nest-IMG_4702.png):** expanding Task 30.1's UC `traceTree.currentSprintEagerLazy` deep-nests the WHOLE chain as expandable TREE nodes, each with its type-icon + child-count badge: UC(puzzle) → **Class RbTraceTree**(cube, badge 19) → **Method renderCurrentSprintEagerLazy**(terminal) → **Impl** (`</>`) → **Test**(flask, green✓).

**So: S30's tree deep-nests UC→Class→Method→Impl→Test as expandable nodes; S31's tree does NOT (stops at UC).** The chain DATA is complete (drawer proves it) — the gap is the TREE RENDERING/nesting not walking the full chain into expandable child nodes for S31.

## NOT a data-mint gap (chains are complete + resolve correctly)
The drawer shows the correct full chain for S31 (assertOwner, not a sibling — R31.10 works). So this is NOT "mint missing units." It's: **why does the tree deep-nest the chain for S30 UCs but not S31 UCs?** = a rendering/tree-walk difference. Architect diagnoses.

## ROUTE (scenario-first, feature-bug: architect diagnose → expert fix → tester gate → Tron)
- **architect:** DIAGNOSE (measure, cite file:line) — compare how the tree expands a S30 UC (deep-nests Class→Method→Impl→Test) vs a S31 UC (stops at UC). What makes S30's chain nodes render as expandable tree children while S31's don't? Candidates: (a) the fetchAndRenderChildren / chain-walk that nests UC→Class→Method→Impl→Test relies on a structure/forward-ref S30's units have and S31's lack; (b) a queryMode/chainMethod nesting path (post-R31.10) that renders the Method but not the deeper Class/Method/Impl/Test as nested nodes; (c) child-count-badge/eager-lazy expansion not populating for S31 chain nodes. Prove the root.
- **req:** capture R31.11 AC — 'the traceability tree deep-nests the FULL chain (UC→Class→Method→Impl→Test) as expandable nodes with type-icons + badges for EVERY sprint incl S31, matching S30' (pin to the S30 target screenshot + the S31 observePosition/ownerGuard cases @390).
- **expert:** fix per architect design (shared rb-trace-tree — fixes ALL sprints by construction, DRY). **tester:** gate — expand a S31 Task's UC → the full Class→Method→Impl→Test nests like S30, @390. **Tron:** device re-verify (S31 tree looks like the S30 target).

Ref: tree-CURRENT-no-deep-nest-IMG_4701.png (S31 stops at UC) + tree-TARGET-S30-deep-nest-IMG_4702.png (S30 full deep-nest).
