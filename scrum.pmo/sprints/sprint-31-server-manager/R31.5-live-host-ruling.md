# R31.5 composed-layout live host — TRON RULING (2026-07-23)

**Context (architect measured):** R31.5 all 7 pieces BUILT + chain-complete-to-Test + GREEN DET-3x @390 (21f981311), but only in the TEST DOM. The composed layout is NOT mounted live — /edit still renders pre-R31.5 panels (rb-file-tree/rb-code-editor/rb-preview :127-144); <rb-woda> is live nowhere. Umbrella can't reach DONE until composed layout renders on a live page for Tron device-visual.

## TRON RULING: SWAP /edit to the R31.5 layout
Replace /edit's old panels with the R31.5 editor strip **[Local] | [Result] | [Repo]** (rb-editor-layout). The foundation becomes THE editor.

## ★★★ HARD GUARDRAIL (PO): PRESERVE THE DIFF/MERGE FUNCTION — positioning != function
/edit hosts the 3-way diff/merge editor (R30 arc: spline connector ribbons, 3-pane alignment, accept/reject, deletion regions, native folding — all Tron-device-validated). The swap changes LAYOUT/PRESENTATION ONLY. It MUST NOT regress ANY diff/merge behavior:
- 3-way merge (Local/Result/Repo) still works: accept/reject hunks, apply-all-nonconflicting, conflict resolution.
- Spline connector ribbons (R30.34) still render + map across panes.
- 3-pane row alignment (R30.30) 0px, native code folding (R30.53), deletion regions — all intact.
- Deep-links (/edit/<path>?repo=&left=&right=&3way=1) still open the exact diff.
The R31.5 strip [L]|[C]|[R] IS the generalization of the 3-pane diff — so this should be a presentation-refactor that HOSTS the existing diff/merge, not a rewrite. If any diff/merge function can't be preserved in the new layout, STOP and flag me — do not ship a regression.

## ROUTE (scenario-first)
architect: design the /edit live-wiring (swap panels → rb-editor-layout hosting the EXISTING diff/merge component, positioning!=function, no diff/merge rewrite) + a rollback-safe path (feature-guard if needed) → expert wires → tester @390: composed layout renders LIVE + FULL diff/merge regression suite (3-way/ribbons/alignment/folding/deep-link) GREEN → Tron device visual (composed /edit + diff/merge intact) → planner umbrella QA-Review → DONE.
Gate = BOTH the composed layout renders @390 AND the diff/merge regression is clean. Real restart if server-side; verify-by-pid.

## ★ TRON RULING #2 (2026-07-23) — portrait merge = ALWAYS 3 COLUMNS
Tron confirmed the architect's reading (design 60ef48f4e): in portrait, the 3-way MERGE stays a co-visible ALWAYS-3-COLUMN cluster (panes narrow, scroll/zoom) — per his own R30.34. NOT one-pane-snap. So:
- rb-diff-editor is hosted UNCHANGED via rb-editor-layout (zero diff/merge rewrite = zero regression).
- The merge is EXEMPT from R31.5 portrait scroll-snap (stays always-3-col).
- R31.5 snap applies ONLY to non-merge chrome (tree <-> editor).
- Ribbons (R30.34) + 0px alignment (R30.30) coordinate space intact; no re-parenting of individual panes.
- rollback-GUARD: ?layout=r31.5 flag, old panels default until diff/merge regression GREEN + Tron OK; one-line revert.
CONFIRMED SHIPPABLE (positioning!=function by construction). Route: req ACs -> expert wires (mount rb-diff-editor behind guard, NO diff/merge edits) -> tester @390 composed + FULL diff/merge regression -> Tron device -> umbrella DONE.
