# R31.9 round-2 — full-width intermediate tier + tree-scroll-past-drawer regression (Tron device 2026-07-24)

**Tron:** *"this is a major achievement. only small issues left. 2nd screenshot is a weird state — that's where the requirement 'full width should have applied'. achievement: drawerbar resizing works in that state. new issue: the background tree cannot be scrolled up far enough to see the last tree item above the drawer. same applies in next smaller state and is a TRUE REGRESSION as this was possible before. I tested it also in the Server Manager — consistent same issues and an additional, but that says you followed DRY well. 4th screenshot, you see the window autosizes somehow small with the shell inside — here again 'full width' shall apply until it switches to compartment. screenshot 5 that works well."*

## ✅ ACHIEVEMENTS (Tron-confirmed)
- "MAJOR achievement" — Sprint 31 works end-to-end on device.
- Drawer-bar **resizing works** in the intermediate state (the R31.9 continuous-resize fix holds).
- **DRY confirmed** — the same issues appear identically on /trace AND Server Manager = the shared detail-container component (Tron: "that says you followed DRY well"). Fix once → fixes both.
- Desktop compartment (screenshot 5) "works well".

## ⚠ ISSUE 1 — FULL-WIDTH intermediate tier missing (drawer autosizes narrow instead of full-width)
There should be a middle responsive tier between mobile-bottom-drawer and desktop-compartment where the drawer is **FULL VIEWPORT WIDTH**. Currently in that intermediate range the drawer is **narrower / centered / autosizes small** (screenshot 2 = /trace drawer not full-width; screenshot 4 = SM terminal "autosizes somehow small with the shell inside"). **Tron: "'full width' shall apply until it switches to compartment."**
- Responsive tiers should be: **narrow(mobile) = bottom drawer** → **intermediate = FULL-WIDTH bottom drawer (spans 100% viewport width)** → **desktop(≥~1025px) = side Details compartment (tree|details)** (screenshot 5, works).
- So: from mobile up to the compartment breakpoint, the drawer width = 100vw (full-width), NOT a narrow/centered/content-sized box. Applies to both /trace detail drawer AND the SM/FM terminal+detail drawer (DRY, same component).

## ⚠ ISSUE 2 — TREE-SCROLL-PAST-DRAWER regression (TRUE REGRESSION — worked before)
The background tree/content **cannot scroll up far enough to reveal the last item(s) hidden behind the bottom drawer**. The drawer overlays the bottom of the scroll area and there is no bottom scroll-space, so the last tree item is permanently occluded. **Tron: this was POSSIBLE BEFORE = a true regression.** Applies to /trace AND Server Manager (DRY = consistent).
- FIX: the scrollable background (tree/content) needs **bottom padding / scroll-space equal to the drawer's occupied height** (peek + expanded/dragged height) so EVERY item can be scrolled to appear ABOVE the drawer. Likely R31.9 changed the container layout (`.trace-page` flex row/`:has()` branches) and dropped the bottom scroll-inset the old fixed-drawer had.

## ROUTE (scenario-first, CMM3/4 — req formalizes → planner tasks → architect diagnoses/designs → expert builds → tester gates → Tron)
- **req:** formalize the requirement + testable ACs (add to R31.9 or mint next): (a) AC-fullwidth-intermediate-tier (drawer = 100vw from mobile up to the compartment breakpoint, both /trace + SM/FM); (b) AC-scroll-past-drawer (background scrolls so the last item clears the drawer at every drawer height; REGRESSION — cite it worked pre-R31.9). Each pinned @390 + intermediate-width + desktop.
- **planner:** task it, board.
- **architect:** diagnose the scroll regression (what R31.9 dropped the bottom scroll-inset) + design the 3-tier full-width responsive + the scroll-space fix (positioning!=function, ONE instance, reuse R31.9 observePosition/data-position).
- **expert:** build. **tester:** gate @390 + intermediate + desktop + scroll-to-last-item + full-width-until-compartment. **Tron:** device re-verify.

Ref: screenshots in this dir (trace-drawer-not-fullwidth, SM-terminal-autosized-narrow, desktop-compartment-works).
