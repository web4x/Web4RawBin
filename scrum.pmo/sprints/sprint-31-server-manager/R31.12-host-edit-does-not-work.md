# R31.12 — host can OPEN room settings but CHANGING does not work (Tron device, 2026-07-27, v0.7.144)

**Tron device (verbatim):** *"ok opening the room setting works again. changing it does not work."*

## Status
- ✅ **OPENING works** — the title-tap fix (role=button + un-gate + WebKit-fire) is CONFIRMED live on Tron's real device. That half of T31.12 is device-validated (closes the tap-fires-on-WebKit AC).
- ⚠ **CHANGING/saving does NOT work** — Tron is the HOST/owner (Marcel); he opens settings but cannot change/save room config. NEW symptom (was unreachable before because opening didn't work → possibly PRE-EXISTING host-edit bug now exposed, OR a regression from the read-only fix).

## Two hypotheses (architect to MEASURE which — decides the fix)
1. **isHost resolves FALSE for the owner on device** → the modal opens READ-ONLY for Tron even though he IS the owner. Root = `isHost = (roomOwnerToken === client.playerToken)` (RoomView:74) mismatching on his device/session (ownerToken≠playerToken — the architect flagged this exact risk in the title diagnosis). If so: the OWNER is being wrongly treated as non-host → read-only → "can't change." FIX = correct the isHost/owner resolution.
2. **isHost=TRUE but the SAVE path is broken** → (a) the Save button has the SAME iOS-WebKit fragility class as the old title h2 (not a real button / doesn't fire on tap), or (b) the save-handler doesn't wire, or (c) UPDATE_ROOM_CONFIG doesn't apply server-side. FIX = make Save WebKit-reliable / fix the save wiring.

## Clarifying observation needed from Tron (PO asking)
As the host opening settings: are the fields **greyed-out/read-only (can't type)** → hypothesis 1 (isHost false); OR **you can type but Save does nothing** → hypothesis 2 (save-broken)?

## Route (feature bug: architect → expert → tester; device-QA)
- architect (0.3): MEASURE which hypothesis (isHost resolution for the owner on device + Save-button WebKit-fire + UPDATE_ROOM_CONFIG apply). Root-cause + fix spec + size.
- expert: fix. tester: gate REAL @390 (host edit+save actually persists) + engine-independent. Tron re-confirm.
- T31.12 stays In-Progress (opening validated, changing open).
