# R31.8c FeatureManager detail — Tron device feedback ROUND 2 (2026-07-23)

**Tron:** *"minor changes are done on the Feature Manager. the real profile. good. in the item view description i want the full uuid not a truncated phone number. on selecting a user and granting or revoking access the tree view must update automatically… latest on using the refresh button! the tree behaviour of eager lazy loading should apply. if the features are collapsed they still shall already show the correct amount of children… not 0. also the revoke button should be directly under the drawer bar and then not just name and truncated phone number but the full existing profileviewer for that profile. reread the requirements i said. they included it already on the first time."*

## ★★★ HONEST GATE-GAP (PO owns): two ROUND-1 ACs were captured but NOT delivered, and the gate passed anyway
From R31.8c-fm-detail-feedback.md (IMG_4616, ALREADY required round 1):
- **AC-2 (round 1): "Details = the user's FULL profile scenario (RbProfileDetail), NOT just name + opaque-id + a button."** → SHIPPED still name + truncated phone + button. NOT met.
- **AC-3 (round 1): "actions (Revoke) directly under the drawer grab-bar, content below."** → SHIPPED but Tron re-flags position. NOT met.
LESSON: the gate verified the wrong surface (functional flow, not the AC's rendered content @390). Re-gate MUST assert the actual rendered profile-viewer + drawer layout at Tron's viewport, NOT "flow works." [[gate-the-ac-surface]] [[device-qa-regression-means-missing-ac]]

## THE 4 ITEMS (scenario-first — pin AC-2/AC-3 as UNMET, add the 2 new)

1. **[RE-STATED, UNMET] Drawer detail = the FULL EXISTING PROFILE VIEWER.** Render the user's profile using the **existing profile-viewer component** (the same full RbProfileDetail / profile viewer used elsewhere — DRY, do NOT fork or hand-build a mini name+phone card). Masked/owner-gated, opaque ref preserved server-side (FIX-2). "the real profile. good" = the profile-detail data is right; now show the WHOLE existing viewer, not a stub.

2. **[RE-STATED, UNMET] Revoke button directly UNDER the drawer grab-bar**, then the full profile viewer BELOW it. Order: `[grab-bar] → [Revoke] → [full profile viewer]`.

3. **[NEW] Item-view DESCRIPTION = the FULL UUID, not the truncated phone.** In the tree, the granted-user child item's description/subtitle line must show the **full uuid**, not the masked `+49…723` phone. (Tron wants the stable identifier visible, not the masked phone.)

4. **[NEW] Tree AUTO-UPDATES on grant/revoke — at the LATEST on Refresh.** After granting or revoking a user's access, the tree view must reflect it automatically (child appears/disappears, counts update); at minimum the **Refresh** button must rebuild it correctly. Currently the tree does not update post-action.

5. **[NEW] EAGER/LAZY tree behavior must apply — collapsed features show the CORRECT child count, not 0.** Reuse the SHARED tree's eager-child-count + lazy-load behavior (the generic itemView/rb-trace-tree mechanic, solved once) so a COLLAPSED feature already shows its real granted-user count in the badge, not 0. Do NOT re-fork tree behavior — it lives in the shared component. [[generic-behavior-in-the-shared-component]]

## ROUTE (scenario-first, feature-UI: req → architect → expert → tester → Tron device)
- **req:** re-open AC-2/AC-3 as UNMET (do not re-close until built+gated at the real surface) + capture the 3 new ACs (full-uuid description, tree-auto-update-on-grant/revoke incl Refresh, eager-lazy-child-count-when-collapsed). Pin each to a testable rendered assertion @390.
- **architect:** design reuse of the EXISTING profile-viewer component in the drawer (DRY, no fork) + actions-under-bar layout + the tree using the SHARED eager/lazy child-count mechanic (not a FM-specific reimplementation) + the grant/revoke→tree-refresh wiring. Opaque FIX-2 ref + INV-F7 preserved.
- **expert:** build against the units.
- **tester:** gate @390 asserting the RENDERED surface — full profile viewer present (not a stub), Revoke under the bar, description = full uuid, collapsed badge = real count, grant/revoke reflects in tree (+Refresh). NOT "flow works."
- **Tron:** device re-verify.

Ref: R31.8c-fm-detail-IMG2.png (this round) + R31.8c-fm-detail-feedback.md (round 1, the already-required ACs).

## ★ TRON RULING (2026-07-23) — AC-item-description-full-uuid resolved
Tron chose: description = the OPAQUE userId (FIX-2 `sha256(token)[:16]`), NOT the raw token, NOT a separate profile-scenario uuid. Rationale: it's the stable, non-sensitive, already-URL-addressable identifier; preserves INV-F7 (no token exposure) by construction; safe even when FeatureManager delegates to non-owners later. Display example: `Marcel Donges / d5e3bb07a1f4c9e2`. req refines AC-item-description-full-uuid to "full OPAQUE userId ref (sha256[:16]), not masked phone, not raw token."
