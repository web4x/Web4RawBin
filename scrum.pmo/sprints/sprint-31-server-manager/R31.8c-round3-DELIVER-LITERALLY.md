# R31.8c ROUND 3 — DELIVER LITERALLY (Tron, 2026-07-23) — PO owns the misses

**Tron (frustrated, rightly):** *"thats not the users uuids...it some other uuid. you correckted the layout order but thats is NOT the users profile viewer. what is so hard to understand what i command! and still the data is scrambled phone numbers...why!!!"*

## ★★★ PO ROOT-CAUSE OF THE REPEAT MISSES: I INJECTED CAUTION TRON NEVER ASKED FOR
- I steered the uuid to `sha256(token)[:16]` via a "security" framing → Tron sees a hash = "some other uuid", NOT the user.
- We built a NEW masked mini-viewer (RbProfileView) instead of THE existing /profile viewer → "NOT the users profile viewer".
- INV-F7 masking got applied to TRON'S OWN owner-only admin view → "scrambled phone numbers".
- I deferred the /profile→shared-viewer migration as "fast-follow" → that deferral IS why the drawer shows a stub not the real viewer.
**LESSON: deliver what Tron LITERALLY commands. Do NOT inject "safe"/masked interpretations he didn't ask for. [[gate-the-ac-surface]] [[device-qa-regression-means-missing-ac]]**

## WHAT TO DELIVER (literal, no interpretation)
1. **Subtitle = the user's REAL PROFILE UUID** (the profile scenario unit's own uuid), NOT sha256(token), NOT the raw auth token, NOT a hash. The real identifier of that user.
2. **Drawer detail = THE EXACT SAME /profile VIEWER** — the full real profile viewer (Name, uuid, Secret Code area, Devices list with details, Bug Reports list — everything /profile shows). NOT a name+identifiers+bugcount card.
3. **REAL DATA, unscrambled** — real phone/identifiers/devices. Drop the owner-side masking; this is Tron's own owner-gated (root-of-trust) admin console viewing real users.
4. **DO THE /profile → shared-viewer MIGRATION NOW (not fast-follow).** ONE real profile viewer component used by BOTH /profile AND the FM drawer. This is the PROOF it's the real viewer: /profile must look identical rendered through it. This was the deferral that caused the stub.

## SECURITY BOUNDARY (keep the REAL protection, drop the scope-creep)
- KEEP: non-owner → 403 on all Feature/server-manager endpoints (the ACTUAL P0 fix — public non-owner token leak). UNTOUCHED.
- DROP: owner-side data masking on the owner-gated FM admin view (Tron is root-of-trust; he sees real data — that masking was scope-creep we added, not a Tron requirement).
- FLAG (architect, future not now): IF FeatureManager is ever delegated to NON-owner admins, revisit whether a delegate should see a user's raw token/secret. Today Tron is sole owner → real full data. Do NOT block round-3 on this.

## ALSO: AC4 collapsed-badge=0 RED (tester 44e972e07, real bug) — 1-line expert fix rb-trace-tree.ts:186 add root.childCount as serverChildCount (badge-half twin of the :186 expander bug). Proceeds regardless.

## ROUTE (scenario-first, feature-UI): req refine ACs (real-profile-uuid subtitle / drawer==/profile-viewer / real-data-no-mask / migrate-/profile-now) → architect redesign (ONE shared real full-profile viewer, /profile is caller, FM drawer is caller, real data, non-owner-403 kept) → expert build → tester gate @390 AGAINST THE /profile REFERENCE (drawer renders the same full viewer + real data + real profile uuid; non-owner still 403) → Tron device.

## ★★★ TRON RULING (2026-07-23) — identity-consolidation, NOT a display guess
The subtitle-uuid repeat-miss ROOT = an unconsolidated-identity gap (architect 3fedd1337): Tron's temp profile (41ad88c4, uuid==token) and rich profile (Marcel Donges 37fcb752) are the SAME person but UNLINKED in data (diff phones, no redirectTo/consolidatedFrom) — no code can bridge them (needs Tron's knowledge).
TRON CHOSE: **CONSOLIDATE → show rich uuid.** One-time link temp(41ad88c4)→rich(37fcb752) via the EXISTING consolidation/device-link mechanism (redirectTo→primary), so Tron appears as ONE identity and the subtitle shows the real rich uuid 37fcb752. Tron AUTHORIZED this data mutation on his own profiles by the ruling.
DELIVER: (a) architect specify the EXACT one-time consolidation action (existing mechanism, redirectTo temp→rich; NOT a manual JSON hack — correct-by-construction). (b) expert: ship the resolver profileUuidOf = follow redirectTo→primary→Profile-unit.uuid (yields rich uuid IFF consolidated, honest token-uuid else) + EXECUTE the one-time consolidation of Tron's temp→rich. (c) req: pin resolver AC + record the consolidation as the resolution. (d) tester: gate subtitle == 37fcb752 (rich uuid) after consolidation @390 + non-owner 403 intact.
SCOPE: Tron chose the ONE-TIME fix (option i), NOT build-consolidation-as-a-feature (option iii) — do the consolidation + resolver now; broader auto-consolidation feature is NOT this round (don't over-build).
