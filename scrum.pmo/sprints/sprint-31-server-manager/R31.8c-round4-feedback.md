# R31.8c ROUND 4 — Tron device walk-through (2026-07-23) — DILIGENT scenario-first, FULL team

**Tron:** *"i doubt that it IS the profile viewer... looks like a fork of the profileviewer. i pressed revoke...i got a green confirmation that appeared and disappeared. but the tree did not update and remove the user from the feature children. clicking refresh is a pure no-op. why is there 'no graph' on top of the tree. design the planning, req and architecture diligent scenario first with the team!!!!"* + *"THATS the profile viewer [/profile screenshot]... and it might be in the drawer"* + *"i never want the user to need to do refresh... its always reactive live update.... refresh is only a last resort."*

## ★★★ PO OWNS THE GATE-GAP (again): tester gated GREEN but the REAL INTERACTIVE OWNER FLOW is broken
Round-3 was gated @390 on /profile-render + a seeded/structural view — NOT Tron's actual interactive owner session. So it missed: the drawer is a FORK (not identical to /profile), Revoke doesn't live-update the tree, Refresh is a no-op, a stray "no graph" label. Re-gate MUST drive the REAL owner-interactive flow. [[gate-the-ac-surface]] [[tron-on-390px-mobile-gate-there]]

## REFERENCE (Tron-confirmed): the REAL profile viewer = the /profile page
/profile (screenshot R31.8c-round4-profile-reference / uploads IMG /profile Marcel Donges v0.7.125): avatar + name + **Token** + **Secret Code** + **Devices(N) with FULL details (Mac, IP, resolution, Nx connected, Last timestamp)** + Feature access. THE drawer must render THIS, identically.

## THE 4 ISSUES (each pinned to a REAL interactive @390 assertion)
1. **DRAWER IS A FORK — must be the IDENTICAL /profile viewer.** Current FM drawer (screenshot R31.8c-round4-drawer-fork): "Profile UUID / Token / Identifiers / Secret Code / Feature grants" + NO Devices list = a DIFFERENT layout/fields from /profile. The "same rb-profile-view" claim is FALSE at the render surface (different component OR different data-shape OR conditional render). FIX: the drawer renders the EXACT /profile viewer — same fields, same Devices(N) list, same layout. Gate: drawer screenshot === /profile screenshot (same sections incl Devices), not just "a component named rb-profile-view mounted".
2. **REVOKE must REACTIVELY LIVE-UPDATE the tree.** Pressing Revoke flashes a green confirm but the user STAYS in the feature's children. FIX: revoke → the child is removed from the tree IMMEDIATELY (live), badge count decrements — WITHOUT a refresh.
3. **★★★ REACTIVE PRINCIPLE (Tron, standing):** the tree is ALWAYS reactive live-update — grant/revoke (and any change) reflect immediately. **The user must NEVER need to press Refresh. Refresh = last-resort fallback ONLY.** Design the tree against a reactive/live data source, not a fetch-once static render.
4. **REFRESH is a pure no-op** — even as the last-resort fallback it must actually re-fetch+re-render. But primary = reactive (issue 3).
5. **"no graph" stray label** at the top of the tree — remove it (leftover empty-state/debug label rendering incorrectly).

## ★ LIKELY ONE ROOT (architect to confirm): the FM tree is NOT backed by a reactive/live data source
"no graph" + revoke-no-live-update + refresh-no-op strongly suggest the FM itemView/rb-trace-tree is rendered from a one-shot fetch with no reactive graph/store behind it → nothing updates live, refresh isn't wired, and the empty-graph placeholder ("no graph") leaks through. Diagnose whether issues 2/3/4/5 share this single root and fix it once (reactive data source), reusing the SHARED itemView/tree mechanics — don't fork per-symptom. [[generic-behavior-in-the-shared-component]]

## ROUTE — DILIGENT scenario-first, FULL TEAM (Tron: "planning, req and architecture")
- **planner (0.6):** plan the round-4 task(s) + sprint view — scaffold the work, keep the board honest (round-3 chain-closed but device-QA re-opened these ACs).
- **req (0.4):** capture-gate each of the 5 as ACs, pinned to a REAL interactive @390 assertion (drawer===/profile incl Devices; revoke→child-gone-LIVE-no-refresh; reactive-always/refresh-last-resort; refresh-works-as-fallback; no stray label). RE-OPEN round-3 AC-detail-full-profile as UNMET (drawer≠/profile fork).
- **architect (0.3):** diagnose the single reactive-data-source root + design: (a) drawer renders the IDENTICAL /profile viewer (one component + one data shape — likely /profile and drawer must share BOTH view AND data feed); (b) reactive live-update tree (grant/revoke reflects immediately, refresh=fallback); (c) kill the "no graph" leak. DRY, no forks.
- **expert (0.1):** build. **tester (0.5):** gate the REAL owner-interactive flow @390 (seed an owner session + actually click Revoke + observe the tree change live + compare drawer-vs-/profile screenshots side-by-side) — NOT seeded-structural. **Tron:** device re-verify.

Refs: R31.8c-round4-profile-reference (the real viewer) + R31.8c-round4-drawer-fork (the current fork) + prior rounds.
