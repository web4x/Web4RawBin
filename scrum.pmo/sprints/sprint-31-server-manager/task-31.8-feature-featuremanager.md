<!-- GENERATED FROM SCENARIO UNITS — DO NOT HAND-EDIT -->

[Back to Planning](./planning.md)

# Task 31.8: Feature typed unit type + FeatureManager root-of-trust — product layer linking implementations to user grants

[task:uuid:71aeff3c-797a-4fcc-9abe-5fb6b5db0d31]

## Status
- [x] Planned
- [x] In Progress
  - [x] refinement
  - [ ] creating test cases
  - [~] implementing
  - [ ] testing
- [ ] QA Review
- [ ] Done

## Remaining Issues

DEVICE-QA RE-OPENED (Tron walk-through 2026-07-23, round-4, spec ed11fb8f8). Round-3 chain-closed but gated on /profile-render + seeded/structural view, NOT the real interactive owner session -> missed 5 issues. RE-OPEN: (1) drawer is a FORK, must render the IDENTICAL /profile viewer incl Devices(N) full details [re-opens AC-detail-full-profile as UNMET]; (2) Revoke must LIVE-update the tree (child removed immediately, no refresh); (3) REACTIVE PRINCIPLE: tree always live-update, user NEVER needs Refresh; (4) Refresh is a no-op (fallback must work); (5) stray no-graph label remove. Architect design DONE (f94875f6e): ROOT A = FM tree non-reactive (lazy roots + ViewBus wipe) -> INLINE children like /server-manager + shared rb-trace-tree no-graph fix (issues 2/3/4/5); ROOT B = drawer feed!=/profile -> shared profileViewData builder (issue 1). req capturing @390-real-interactive ACs; expert builds; tester gates REAL owner-interactive flow @390 (click Revoke + observe live + drawer-vs-/profile side-by-side), NOT seeded-structural. HONEST: round-3 Tests STAY, device-QA ACs RE-OPEN. status In-Progress.

## Traceability

  - up
    - [Sprint 31 Planning](./planning.md)
    - Requirement R31.8 `[requirement:uuid:a93f5fce-e55a-4d42-9d4d-976a7b892523]`
  - down
    - None (atomic task)

## Task Description

Tron model (2026-07-21, brief 0427d264f) + architect security design 83836631a (APPROVED rulings). ior:class:Feature is a first-class TYPED scenario unit = the PRODUCT layer: {name, description, icon, implementations[] (BOTH-WAY: Impl.feature back-ref, mirror-maintained), allowedUsers[] (-> profile TOKENS, BOTH-WAY with profile.features)}. Two instances: ServerManager (the R31.1-R31.4 feature) + FeatureManager (grant/revoke Features on profiles, root-of-trust). Generalizes R31.1 (profile renders granted-Feature list off m.features, server-computed) + R31.2 (access = requireFeatureAccess = isLiveSession AND user-in-Feature.allowedUsers, data-driven). SECURITY (architect-owned, finalized off 83836631a): FeatureManager grant-editing = HARDCODED owner-gate (root-of-trust, NOT data-driven = closes self-grant escalation); feature ACCESS = data-driven allowedUsers, AND-composed with isLiveSession; OWNER_TOKEN literal used ONLY for root-of-trust + bootstrap seed (==1 preserved, no literal-bypass into feature-access — owner enters via seeded membership); bootstrap idempotent-seeds OWNER_TOKEN into ServerManager+FeatureManager allowedUsers; INV-F5 empty/malformed allowedUsers FAILS CLOSED. Preserves R31.2 INV-G1/G2/G3 (grants only ADD checks). Route: req formalizes (this) -> expert implements (Feature units + Impl.feature back-refs + profile.features + requireFeatureAccess + FeatureManager grant/revoke owner-gated + bootstrap seed + m.features render) -> tester gates (both-way nav; grant/revoke flips member-200/non-member-403; profile lists granted; INV-F1..5 hold).

## Acceptance Criteria

- [x] **[AC-feature-type]**: ior:class:Feature is a first-class TYPED scenario unit (FeatureModel schema): name, description, icon, implementations[] (refs to Impl units), allowedUsers[] (refs to user profile TOKENS — profiles are Map+JSON today, server.ts:166; token is the stable identity; full profile-as-unit = later migration). The PRODUCT layer linking code to user grants.
- [x] **[AC-both-way-impl]**: INV-F-MIRROR: Feature.implementations[] <-> Impl.feature both-way — for every f in Feature.implementations, Impl(f).feature==Feature; Feature->its impls AND impl->its Feature both resolve. Mirror-maintained like Class.methods[]/Method.ownerIor; a drift-check fails loud.
- [x] **[AC-both-way-user]**: Feature.allowedUsers[] <-> profile.features[] (new UserProfile field, Feature-uuid refs) both-way — a grant writes BOTH sides atomically, a revoke removes from BOTH. Feature->its users AND user->their features both resolve.
- [x] **[AC-instances]**: Two S31 Feature instances: ServerManager (implementations=[assertOwner impl 335dbf3d, attachPane 394eac63, readSessionTree 5c1701bc, renderFeatureGrants f345b8ed, buildSeedNode 5b3d9f1a, reapOrphans 5d313828 — representative, architect-refined]; allowedUsers=[Tron 41ad88c4]) + FeatureManager (function: enable/disable Features on profiles = grant/revoke; implementations to-be-built; allowedUsers=[Tron]).
- [x] **[AC-profile-renders-features]**: REFRAMES R31.1: replace the single serverManager flag (server.ts:2625) with features:<Feature[] where token in allowedUsers> on the PROFILE ws msg (server-computed, same trust path, client can't self-grant). renderFeatureGrants iterates m.features -> one entry per granted Feature (name/icon/link) at #feature-grants. Tron -> [ServerManager, FeatureManager]; non-granted -> empty -> section ABSENT by construction (not UI-hidden).
- [x] **[AC-data-driven-access]**: REFRAMES R31.2 (architect design 83836631a): feature ACCESS = requireFeatureAccess(req, featureName) = isLiveSession(token) AND token in Feature.allowedUsers — data-driven membership, AND-composed (both fail-closed), replacing the hardcoded per-feature owner-gate. Added at the SAME server-side choke (server.ts:891 pattern), never bypassed; UI-hiding is NOT the gate. INV-F1: every feature's routes+ws -> 403 for non-members.
- [x] **[AC-featuremanager-root-of-trust]**: INV-F4 (root-of-trust): FeatureManager grant/revoke — ANY write to allowedUsers/profile.features — is gated by the HARDCODED owner-gate (ServerManagerGuard.assertOwner / the R31.2 OWNER_TOKEN literal), NOT data-driven. A data-driven-only path to grant-editing is the escalation HOLE (anyone reaching FeatureManager could self-add to any feature) and is REJECTED. Only the hardcoded owner mutates grants; a non-owner grant attempt -> 403, never mutates.
- [x] **[AC-bootstrap-seed]**: BOOTSTRAP + INV-F5: at first run an idempotent seed writes OWNER_TOKEN into ServerManager.allowedUsers AND FeatureManager.allowedUsers from the hardcoded literal (a generated seed step, R31.7-aligned) — the hardcoded owner is the bootstrap root, from there Tron grants others via FeatureManager; NO grant path exists that doesn't originate at the hardcoded owner. INV-F5: a feature with EMPTY/MALFORMED allowedUsers FAILS CLOSED (403 for all, incl owner-unless-seeded), NEVER fails-open.
- [x] **[AC-inv-g-preserved]**: R31.2 INV-G preserved, NOT weakened: INV-G1 stays ONE server-side choke (now calls requireFeatureAccess(feature), not requireOwnerHttp); INV-G2 the OWNER_TOKEN literal stays ==1 (used ONLY for the FeatureManager root-of-trust + bootstrap seed; feature access adds NO new literals, uses allowedUsers data; grep-guard still ==1); INV-F3 (=INV-G3) a feature ws upgrade checks requireFeatureAccess BEFORE handleUpgrade, non-member -> socket.destroy before open. The OWNER reaches ServerManager via seeded allowedUsers MEMBERSHIP (same data path as any grantee) — NO literal-bypass OR into feature-access.
- [x] **[AC-mint-generalization]**: The /session mint GENERALIZES: any LIVE authenticated user mints a session carrying THEIR OWN token (non-privileged binding — the session binds the caller's own identity, NOT an owner privilege). Grant/revoke stays HARDCODED-owner (root-of-trust, INV-F4); feature ACCESS = per-feature allowedUsers (requireFeatureAccess). INV-G RE-VERIFIED under the generalized mint: a non-owner WITH a live session is STILL 403 on ServerManager — gated by allowedUsers MEMBERSHIP, NOT by session-absence (the session existing does not grant access; allowedUsers does). This makes the Feature model actually WORK for non-owners (else allowedUsers is inert — no non-owner could ever hold a session to be membership-checked).
- [x] **[AC-featuremanager-view]**: Tapping the FeatureManager entry in the profile Feature-access list OPENS the FeatureManager VIEW (v0.7.107 BUG: FeatureManager RENDERS in the list but does NOT open on tap — no view exists). The view MOUNTS in the DEFAULT DRAWER (consistent with ServerManager + the R31.4 drawer direction / the DRY shared detail-flow, NOT a bespoke overlay). The view LISTS each Feature + its granted users (allowedUsers). The OWNER grants a feature to a user (by link-code / token) AND revokes — backed by the PROVEN grant/revoke API (POST /api/feature-manager, owner-gated; the slice-(b) grantFeature/revokeFeature 5e2f6781/987a31a9). OWNER-ONLY opens/uses it (owner in FeatureManager.allowedUsers, enforced by requireFeatureAccess — NOT UI-hiding). ★ DRY TREE-VIEW REFRAME (Tron, supersedes the card-list): the FeatureManager view IS a TREE on the SHARED rb-trace-tree (like ServerManager otmux tree) — Features = ITEMS (root nodes), granted-users (allowedUsers) = CHILD ITEMS under each Feature node. Item-detail (feature node / user node) opens in the shared rb-detail-drawer. NO bespoke list/cards. The card-list framing above is SUPERSEDED. ★★ MAXIMALLY-DRY (Tron): the tree renders the Feature SCENARIO UNITS (ior:class:Feature) via the EXISTING scenario-unit itemView (NOT a reshaped API list) — the Feature UNIT is the node, its allowedUsers refs are the CHILD ITEMS; GRANT = ADD an allowedUsers ref to the unit, REVOKE = REMOVE the ref, and the itemView RE-RENDERS natively from the unit. Scenario units render themselves (same mechanism /trace uses) — no bespoke render, no API-list reshape.
- [x] **[AC-grant-user-selector]**: The FeatureManager grant input is a USER SELECTOR with c2-style COMPLETION, replacing the raw 'player token to grant' field. Typing a name / email / phone / UUID completes/suggests matching USER PROFILES, each shown with identifying detail (SEE the profile, like c2 Tab-completion); selecting one -> Grant. MULTI-KEY resolve: name / email / phone / uuid -> the user token (leveraging the existing phone/email alternate-UUID + profile data). OWNER-GATED for BOTH search and grant (requireFeatureAccess / owner-gate; the search endpoint is owner-only, never leaks profiles to non-owners). Applies to the revoke side too where useful. PROFILE DETAIL REUSES THE SHARED DRAWER (DRY, no new component): selecting/expanding a matched user in the selector opens their DETAILED PROFILE in the shared rb-detail-drawer — consistent with the ServerManager / FeatureManager / trace detail-views (same shared detail-flow, not a bespoke popup). ★ TREE FRAMING (Tron): GRANT = ADD a user CHILD-ITEM under the Feature node (via the c2-completion user selector); REVOKE = REMOVE the child-item. The matched-user detailed profile opens in the shared rb-detail-drawer (tagMap profile->rb-profile-detail). Consistent with the shared item/itemView tree, no bespoke cards.
- [x] **[AC-trace-children-owner-gated]**: /api/trace/children on a Feature unit (it resolves allowedUsers via allowedUsersChildren, emitting COMPOSITE member refs that carry the user TOKEN) MUST be OWNER-GATED (INV-F): a non-owner GET -> 403, and token-bearing composite member refs are NEVER emitted to non-owners. ALSO evaluate whether the composite ref should carry the RAW TOKEN at all (token in a URL-addressable ref leaks via logs/history) — prefer a non-token composite (e.g. profile:<featureUuid>:<userUuid>) resolved server-side, per architect security design. Expert-discovered gap (v0.7.112): the native-render wired /api/trace/children to resolve Feature children WITHOUT owner-gating, leaking member tokens to non-owners — violates INV-F1 (owner-only) + masked-PII. The FUNCTIONAL tester gate MUST include this: non-owner /api/trace/children on a Feature -> 403, no token leak. ★ RESOLVED (architect 2919770c3, BOTH THIN no-new-node): (1) OWNER-GATE = thin wire of the EXISTING requireFeatureAccess (35e91ba7) into the /api/trace/children type===Feature branch (server.ts:1499) BEFORE returning children -> non-member/non-owner 403 (membership-gated Feature Manager, consistent w/ the view+GET; non-Feature units stay public). RELEASE-BLOCKER, gate FIRST. (2) DROP-RAW-TOKEN = impl-edit on allowedUsersChildren 72c660f9: composite ref = profile:<featureUuid>:<userId> (STABLE NON-TOKEN id), server resolves userId->token for display+revoke; the raw AUTH TOKEN NEVER rides a URL-addressable ref (defense-in-depth). Both impl-edits, expert wires. Functional gate asserts: non-owner /api/trace/children on a Feature -> 403 + no token in any emitted ref.
- [x] **[AC-featuremanager-lists-all-members]**: On expand/load of a Feature node in the FeatureManager tree, ALL of that Feature enabled users render as itemView CHILD nodes — a Feature with N allowedUsers shows EXACTLY N child user-nodes (COMPLETENESS: no subset, no cap/pagination-drop, no manual-refresh required). Resolved by allowedUsersChildren (72c660f9/Impl ad622052) which maps over the FULL Feature.allowedUsers (measured: no slice/cap in the built impl — unlike searchUsers which is capped-10, because MEMBERS are the authoritative set and must be complete), rendered NATIVELY via the existing itemView (CHAIN_TYPE_CONFIG Feature.scenarioFwd:[allowedUsers]). Owner-gated (AC-trace-children-owner-gated). Tester functional gate asserts N-in -> N-children on load at Tron viewport.
- [x] **[AC-children-itemview-user-scenario]**: The granted-user CHILD nodes under a Feature are itemviews BOUND TO the user SCENARIO unit (the person profile), NOT standalone opaque-uuid rows (device IMG_4616: children were generic uuid rows, subtitle just repeated the name). The itemview resolves + navigates to the REAL user. ★ SECURITY (architect-owned, preserves FIX-2): the URL-addressable ref STAYS OPAQUE (<featureUuid>:<sha256(token)[:16]>, no raw token) — the itemview resolves the user SERVER-SIDE (owner-gated) for display/nav; do NOT re-expose the token to satisfy itemview-to-user-scenario. Display resolves the user; the ref stays hashed.
- [ ] **[AC-detail-full-profile]** (REOPENED-UNMET): ⚠ RE-OPENED UNMET (round-4 device-QA, Tron 2026-07-23): round-3 shipped a FORK, not the identical /profile viewer. The FM drawer render MUST === the /profile render — SAME sections INCLUDING the Devices(N) list with FULL details (Mac/deviceId, IP, resolution, Nx-connected, Last-timestamp), same fields (avatar / name / Token / Secret Code / Devices(N) / Feature-access), same layout. The round-3 drawer showed 'Profile UUID / Token / Identifiers / Secret Code / Feature grants' + NO Devices list = a DIFFERENT layout/fields (different component OR data-shape OR conditional render) — the 'same rb-profile-view' claim was FALSE at the render surface. @390 GATE (REAL owner-interactive, NOT seeded-structural): side-by-side SCREENSHOT match drawer-vs-/profile — same sections incl the Devices(N) list — not merely 'a component named rb-profile-view mounted'. Ref: R31.8c-round4-drawer-fork vs R31.8c-round4-profile-reference. ★ ROOT (confirmed, architect f94875f6e + PO): this is NOT a component fork — the COMPONENT is shared; the DATA FEED differs (grantedUserProfile returns a SUMMARY / no-devices shape vs /profile's FULL / devices shape). FIX = extract ONE shared profileViewData(full, incl a devices builder) used by BOTH grantedUserProfile AND /profile → drawer === /profile BY CONSTRUCTION (same component + same data shape). INV-F7 kept (owner real data; non-owner → 403 is the security boundary).
- [ ] **[AC-drawer-actions-layout]** (REOPENED-UNMET): [RE-OPENED UNMET 2026-07-23 — Tron re-flags the layout] Order in the drawer body: [grab-bar (shared RbDetailDrawer chrome, untouched)] -> [Revoke button directly UNDER the grab-bar] -> [the FULL profile viewer BELOW it]. RENDERED @390 ASSERTION: in DOM order the Revoke control sits immediately under the grab-bar and the full profile viewer renders beneath it. Gate at the RENDERED surface @390, NOT 'flow works'. Do NOT re-close until built AND rendered-gated.
- [x] **[AC-inv-f7-server-side-resolve]** (WITHDRAWN): WITHDRAWN (round-3, 0cd05131a): INV-F7 owner-side MASKING (sha256 opaque userId, masked phone, opaque avatar route, no-secret/devices on the owner FM path) was OUR scope-creep, NOT a Tron requirement — Tron is sole owner /root-of-trust viewing REAL data on his own owner-gated console. The REAL protection is UNCHANGED and KEPT: non-owner → 403 on all Feature / /api/feature-manager / granted-user / Feature-branch-of-/api/trace/children endpoints (see AC-trace-children-owner-gated — the actual P0 fix). Keeping the raw token out of URL-addressable refs persists as a consequence of using the real profile uuid (item-1), not as re-added masking. FUTURE-ONLY flag: if FeatureManager is ever delegated to NON-owner admins, revisit raw token/secret/PII exposure — does NOT block round-3.
- [x] **[AC-item-description-full-uuid]**: The granted-user child item's SUBTITLE (.oi-desc) == FeatureManager.profileUuidOf(token) — the consolidation-following resolver output (see AC-token-profileuuid-resolver). ✓ RESOLVED-BY-RESOLVER (PO 2026-07-23): the resolver honestly returns the owner's PRIMARY uuid; premise was inverted — the architect MEASURED the data (c99c49598) and Tron's premise is BACKWARDS — 41ad88c4 is ALREADY Tron's PRIMARY consolidated identity (tombstone chain 37fcb752 → 8f74dfba → 41ad88c4), and 37fcb752 is an OLDER tombstone that redirects INTO 41ad88c4. So the earlier Tron ruling to consolidate 41ad88c4 → 37fcb752 (448f6837f) is UNSAFE: it would redirect the PRIMARY to a tombstone → assertOwner fails → LOCKS TRON OUT. That consolidate action is HELD, NOT executed. The pinned resolver ALREADY returns 41ad88c4 for the owner CORRECTLY (that IS his primary uuid) = NO data mutation needed. @390 ASSERTION (today): subtitle == profileUuidOf(token) OUTPUT (== 41ad88c4 for the owner, his real primary), asserted vs the RESOLVER RESULT — never a hardcoded value, NOT sha256/opaque, NOT masked-phone. PO is taking the corrected picture to Tron for a re-decision on what the owner's subtitle should show; until then the honest resolver output (41ad88c4) stands. Resolver UNCHANGED (2b4d7151b, correct either way). ✓ RESOLVED-BY-RESOLVER: subtitle == profileUuidOf output = 41ad88c4 (owner's real primary), GREEN DET-3x @390 screenshot-verified (Test d1c63226→a1c4e5f0). Tron's cosmetic accept/re-home of the displayed uuid is a display-preference with NO code impact; the gate asserts subtitle==resolver-output (not a literal). No consolidation mutation.
- [x] **[AC-tree-auto-update-on-action]**: After granting or revoking a user's access, the tree view reflects it AUTOMATICALLY (the child appears / disappears, the parent feature's child-count updates); at the LATEST, pressing Refresh rebuilds it correctly. RENDERED @390 ASSERTION: grant -> the new child row is present + count incremented (auto, or after Refresh at worst); revoke -> the child row is gone + count decremented. Currently the tree does NOT update post-action = the defect.
- [x] **[AC-collapsed-child-count-eager]**: A COLLAPSED feature node already shows its REAL granted-user child count in the badge (not 0), via the SHARED tree's eager-child-count + lazy-load mechanic (the generic itemView / rb-trace-tree behavior, solved once) — NOT a FeatureManager-specific reimplementation (do NOT re-fork tree behavior; it lives in the shared component). RENDERED @390 ASSERTION: with the feature collapsed, its count badge == the real number of allowedUsers (>0 when members exist), not 0. ROUND-3 impl path (unchanged intent): rb-trace-tree.ts:186 (PATH-B) passes root.childCount as serverChildCount to buildSeedNode (twin of the :186 expander fix); featureRoots already emits childCount. Expert 1-line, survives round-3 as-is. Tester bug ref 44e972e07.
- [x] **[AC-profile-single-viewer-fastfollow]**: ROUND-3 NOW (no longer a deferred fast-follow — the deferral caused the round-2 stub): /profile (server.ts:2254) MIGRATES to render via <rb-profile-view> fed the PROFILE ws msg m.profile (the viewer's own real profile incl token) → renders IDENTICAL to today's inline HTML = the PROOF <rb-profile-view> IS the real full viewer. The inline HTML layout MOVES into RbProfileView (data-driven, renders the fields present); feature-grants stays appended. RbProfileView is THE ONE full-profile viewer; /profile AND the FM drawer are its two callers. @390 GATE: the FM drawer renders the SAME viewer with real data; /profile still looks IDENTICAL (the reference). Architect round-3 design 0cd05131a.
- [x] **[AC-token-profileuuid-resolver]**: AUTHORITATIVE resolver (architect 3fedd1337, AUTHORITATIVE-profileUuidOf.md — PIN verbatim, do NOT re-derive). FeatureManager.profileUuidOf(token): (1) primary = userProfiles.get(token)?.redirectTo || token  // follow the EXISTING consolidation to the primary token (the mechanism rooms/IDENTIFY already use); (2) return ScenarioIndex.get(primary)?.model.uuid ?? primary  // the primary Profile unit's OWN uuid (== primary token by construction, since ensureProfileUnit mints model.uuid==token). This yields the RICH profile uuid IFF the token is CONSOLIDATED into it (redirectTo); absent consolidation it HONESTLY returns the token-keyed Profile-unit uuid (== the token). FeatureManager.tokenOfProfileUuid(uuid) (reverse, for revoke): a Profile unit is token-keyed with model.uuid==token, so uuid IS a token → verify ScenarioIndex.get(uuid)?.ior==='ior:class:Profile'; the grant key in Feature.allowedUsers[] is that token. ★ HARD RULE (the repeat-miss guard): NEVER a hash (no sha256), NEVER a hardcoded/guessed 37fcb752, NEVER an invented new-field uuid — the resolver ONLY follows existing consolidation. REPLACES + RETIRES userIdOf(sha256[:16]) / resolveGrantedToken(sha256→token) / grantedAvatarUrl(opaque). Used by allowedUsersChildren (subtitle + ref) / grantedUserProfile (id row) / revoke (uuid→token).
- [ ] **[AC-revoke-live-tree-update]** (UNMET): Pressing Revoke → the granted-user child is removed from the tree IMMEDIATELY (live) AND the feature badge count decrements — WITHOUT any Refresh. @390 GATE (real interactive): seed an OWNER session, click Revoke, observe the child DISAPPEAR from the feature's children live + badge N→N-1, with NO refresh press. Round-4 bug: revoke flashed a green confirm that appeared+disappeared but the child STAYED in the tree. ★ ROOT A1 (confirmed): /api/feature-manager returns the allowedUsers INLINE (like /server-manager does) → the SHARED renderItems reconciles grant/revoke LIVE (reactive; no fetch-once static render). Same shared itemView/tree mechanics, no per-symptom fork.
- [ ] **[AC-reactive-always-no-refresh]** (UNMET): ★ TRON STANDING PRINCIPLE: the FM tree is ALWAYS reactive live-update — grant/revoke and ANY change reflect IMMEDIATELY. The user must NEVER need to press Refresh; Refresh = last-resort fallback ONLY. The tree is backed by a REACTIVE/LIVE data source, NOT a fetch-once static render. @390 GATE (real interactive): any grant/revoke reflects in the tree with ZERO refresh. Tron: 'i never want the user to need to do refresh... its always reactive live update.... refresh is only a last resort.' ★ ROOT A1 (confirmed): /api/feature-manager returns the allowedUsers INLINE (like /server-manager does) → the SHARED renderItems reconciles grant/revoke LIVE (reactive; no fetch-once static render). Same shared itemView/tree mechanics, no per-symptom fork.
- [ ] **[AC-refresh-fallback-works]** (UNMET): Refresh, as the LAST-RESORT fallback (primary = reactive, AC-reactive-always-no-refresh), must ACTUALLY re-fetch + re-render the FM tree — currently it is a PURE NO-OP. @390 GATE: press Refresh → the tree re-fetches + re-renders (fallback works). This is the fallback only; the primary path is reactive.
- [ ] **[AC-no-stray-graph-label]** (UNMET): No stray 'no graph' / empty-state / debug label renders at the TOP of the FM tree (currently a leftover empty-graph placeholder leaks through — a symptom of the tree lacking a reactive graph/store behind it). @390 GATE: the FM tree top shows NO 'no graph' label. ★ ROOT A2 (confirmed): the items-tree must SKIP the graph ViewBus subscription (the FM tree is items-driven, not graph-driven) AND clear the stale 'no graph' empty-state placeholder — a SHARED rb-trace-tree fix (the same reactive-source root as A1). Do NOT special-case per symptom.

## Subtasks

None (atomic task).
