# T31.4 drawer-UX GATE-SPEC (tester probe-done → fresh-session builds full DET-3x gate FAST from this)

**Task:** gate T31.4 drawer-UX @390 on served **v0.7.129** (client-only, no restart; activate via hard-reload/SW update-banner). **SELF-VERIFY served .version==0.7.129 FIRST** (curl /api/config). Build-item 1 of 2 for the Tron device batch.

## Chain (req 2026-07-23) — marker on GREEN
UC paneTerminal.teardown `44c98b1b` → Class RbDetailDrawer `d86af73d` → Method tearDownTransientDetail `9c6391fc` → **Impl `cb153623`** (rb-detail-drawer.ts:264). On GREEN → place `[test:uuid:]` marker → req mints INV-T1 Test onto cb153623 → two-key. AC-maximize folded into same T31.4.

## Harness (reuse r31r4 owner-seed + sm_session cookie — PROVEN)
- seedOwner: localStorage `rawbin-player-id=the owner literal` + `rawbin-device-{privateKey,publicKey,signature}='e2e-bypass'`, name 'Owner'.
- /app (waitUntil networkidle, sleep 2500 → live ws IDENTIFY) → `POST /api/server-manager/session` with x-player-token=OWNER (mint sm_session httpOnly cookie) → THEN goto /server-manager (cookie auto-sent = real owner data). ⚠ owner-seed = PO-sanctioned coordinated blip (evicts Tron ~1×; he re-auths). @390 iPhone-12.

## Probe findings (measured 2026-07-23, v0.7.129)
- /server-manager renders **11 otmux sessions**; **panes are LAZY** → expand session→window→pane (click `.oi-expand` down the chain) before a `otmuxpane:%NN` node exists.
- Click a pane (`rb-object-item[ref="otmuxpane:%NN"]`) → **spawns 2 GROUPED `sm_` tmux sessions** (`sm_pNN_<id>` ×2, grouped — killing one kills both).
- Drawer = `#sm-drawer` (rb-detail-drawer) with `rb-terminal-detail`; header buttons = **Maximize (`.drawer-max`, ⛶)** + **Close**. baseline sm_=0.

## (a) INV-T1 AUTO-CLOSE — the core (was 14 leaked; target 0)
**OBSERVE-ONLY. NEVER pattern-kill sm_** (classifier-blocked + would kill Tron's/other users' terminals on the shared host). Count via `tmux ls | grep '^sm_' | cut -d: -f1` (execSync in the node gate — runs on WODA.prod host). Track MY sessions by NAME-DIFF vs the pre-open baseline; assert MY new sessions are GONE after each teardown; if a RED leaks, clean up ONLY my EXACT tracked names (`tmux kill-session -t <exact>`), never a pattern.
Gate EACH teardown path (open pane terminal → my sm_ appears → trigger → my sm_ gone):
1. **Close button** (drawer header) — primary.
2. **deselect** (click another/empty node → selection clears) — call site :98.
3. **minimize** (grab-bar `.drawer-handle` drag/click to peek) — call site :406.
4. **ESC** — call site :251. ★★ **ESC IS CAPTURED BY XTERM** (goes into the focused terminal, NOT drawer-close). To gate ESC you MUST blur the terminal / focus the drawer first (e.g. click the drawer header, or `document.activeElement.blur()`), else it's a FALSE leak (probe artifact confirmed). Prefer Close/deselect/minimize as the primary GREEN; ESC only with focus-management.
DET-3x each.

## (b) AC-MAXIMIZE
Click `.drawer-max` (⛶) → drawer fills full viewport (`inset:0` / `100dvh`). Assert geometry drawer rect ≈ {0,0,vw,vh} in BOTH **portrait (390×844)** AND **landscape** contexts + terminal re-fits (xterm cols/rows grow vs pre-maximize). Default-off (general feature).

## (c) REGRESSION (unregressed)
/trace + /scenario drawer: scroll / grab-bar / expand / minimize / detail-open all work (fix is terminal-SPECIFIC by tag; maximize general-but-default-off). Assert rb-detail-drawer opens a detail + grab-bar toggles on /trace, /scenario renders.

## (d) non-owner → 403 + INV-F7 unregressed (spot-check)
GET /api/feature-manager (no cookie/token) → 403; no raw token/secret leak.

## Engine / scope
Real WebKit for codicon/terminal-render VISUAL → **Tron device** (webkit unlaunchable here — needs sudo install-deps). The sm_ LIFECYCLE (tmux count), maximize GEOMETRY (DOM rect), 403, regression = **chromium-gateable** (engine-independent). DET-3x. On GREEN → screenshot to Tron + marker cb153623 → req.
