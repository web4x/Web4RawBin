# R31.9 — detail container = ONE CSS-responsive instance (drawer↔compartment), no JS instance-switch (Tron)

**Tron (2026-07-24, desktop):** *"this looks very good. here the issues: making the window just a little wider makes the drawer JUMP because of media breakpoint and the sizing with the mouse on the drawbar becomes broken… the behaviour still shifts. this is an indicator that it's NOT the same Instance of the drawer as before anymore = a big DRY violation. double check that, prove me wrong, and just let CSS apply not javascript instance switches or something more stupid. sizing it to a desktop-size (screenshot 3) should have the responsive compartments from Task 31.5 where the details container is NO LONGER a drawer but a COMPARTMENT similar in sizing to screenshot 4 (WODA) but NOT from the CI."*

## ✅ DEVICE-CONFIRMED WINS (Tron: "this looks very good")
- **Server Manager otmux tree** (T31.3): renders session→window→pane, child-count badges, select-pane opens terminal. ✓
- **Terminal (T31.4)**: LIVE interactive terminal on a remote pane — Tron watched a real Python 3.14.6 build running through it (opy1@WODA.prod + v60211.1blu.de panes). Maximize + Close present. ✓ (core works)
- **Feature Manager (T31.8)** + profile: not flagged = looks good. ✓

## ⚠ THE ISSUE (architectural DRY — the one refinement before DONE)
Widening the window slightly makes the **drawer JUMP at the media breakpoint**, and the **drag-bar mouse-resize breaks / behaviour shifts** across it. Tron's diagnosis (likely correct): the detail container is being **SWAPPED to a different instance** (JS instance-switch / component swap at the breakpoint) instead of being ONE component that responds via CSS. That discontinuity (jump + broken resize) is the tell.

## THE REQUIREMENT (Tron)
1. **DIAGNOSE FIRST — prove it (architect):** is the detail container the SAME instance across the breakpoint, or is JS swapping instances/components (rb-detail-drawer ↔ rb-compartment or a re-mount)? Measure — prove Tron right or wrong. The jump + resize-break = evidence to confirm/refute.
2. **ONE instance, CSS-responsive — NOT JS instance-switch:** the detail container is a SINGLE component whose mobile-vs-desktop presentation is driven by **CSS media queries** (positioning != function, the R31.5 principle), so there is NO jump and the resize behaviour is continuous across the breakpoint. No re-mount, no component swap, no state loss.
3. **Desktop = COMPARTMENT, not drawer (screenshot 3):** at desktop width the details container is NO LONGER a bottom-drawer — it becomes a **Details COMPARTMENT** in the R31.5 composed responsive-compartment layout (What / Overview / Details / Actions, like WODA screenshot 4). Mobile = bottom drawer; desktop = side/composed compartment; **same instance**, CSS transition.
4. **Sizing like WODA (screenshot 4), CI = RawBin:** the desktop compartment sizing/proportions mirror the WODA IDE layout (screenshot 4) but keep RawBin's visual identity (NOT WODA's CI).

## WHY IT MATTERS (DRY)
This unifies rb-detail-drawer (T31.4 mobile drawer) and rb-compartment (R31.5 desktop compartment) into ONE responsive detail-container — the exact "generic behavior in the shared component / positioning != function / don't fork" principle. A JS instance-switch is the fork Tron is calling out. Fix = one component, CSS-responsive, drawer-on-mobile / compartment-on-desktop.

## ROUTE (scenario-first, feature-architecture: architect → expert → tester → Tron device)
- **architect:** (a) DIAGNOSE — measure whether the breakpoint swaps instances (rb-detail-drawer vs rb-compartment / re-mount) = confirm/refute Tron's DRY-violation claim; (b) DESIGN — ONE detail-container component, CSS-media-query responsive (mobile drawer ↔ desktop Details-compartment in the R31.5 layout), continuous drag-resize across the breakpoint, no jump/re-mount, WODA-like desktop sizing + RawBin CI. Reuse R31.5 rb-compartment; positioning!=function.
- **req:** capture R31.9 ACs, each @390 AND @desktop-width rendered assertion: no-jump-across-breakpoint, continuous-resize, same-instance (no re-mount), desktop=Details-compartment (WODA-sizing/RawBin-CI).
- **expert:** build (collapse any JS instance-switch to CSS; one instance).
- **tester:** gate at BOTH mobile @390 AND desktop width + AT the breakpoint (drag the window across it: no jump, resize stays continuous, same DOM instance).
- **Tron:** device re-verify (resize across breakpoint + desktop compartment).

Ref: screenshots in this dir (server-manager tree+terminal @various widths + WODA IDE layout).
