# R31.12 — TWO in-room REGRESSIONS (Tron 2026-07-27)

**Tron:** *"we have two regressions in-room. the chat is not working anymore and the room settings don't open anymore on the room title."*

## THE 2 REGRESSIONS (worked before, broken now)
1. **In-room CHAT not working** — the chat is broken in-room (was working). Likely tied to the shared `rb-detail-drawer` refactor: R30.20 wired in-room chat to the drawer (`chatPanel`, `closeOrReturn` — X→chat), and R31.4 (drawer-UX/auto-close/maximize) + R31.9 (detail-container ONE CSS-responsive instance, data-position, observePosition) + R31.11 (chain-model) all touched the drawer/detail path. A drawer change likely broke the chat panel mount/render or the ws.
2. **Room settings don't open on the room TITLE** — tapping the room title used to open room settings; now it doesn't. Likely a click/tap handler on the room title regressed (drawer/detail-container refactor changed the select/open flow, or a handler removed/rewired).

## PRIME SUSPECT (regression source — architect confirms by bisect/measure)
The recent R31.4/R31.9 drawer + detail-container refactor (rb-detail-drawer position:static/fixed, data-position, closeOrReturn, renderDetailForRef, the R31.9 observePosition/CSS-responsive changes) — the in-room flow (RoomView drawer=chat, room-title→settings) shares that component. Something in the drawer/detail refactor regressed the in-room chat mount + the room-title-open-settings handler. **NOT R31.11 chain-model (that's trace-tree only).** Architect measures WHICH commit + what.

## ROUTE (scenario-first, feature-regression: architect diagnose → expert fix → tester catch → Tron)
- **architect:** DIAGNOSE (measure, cite file:line + the regressing commit) — (a) in-room chat: what broke the chat panel in RoomView (drawer chatPanel mount? ws? closeOrReturn/setMode?); (b) room-title→settings: what removed/rewired the room-title click handler that opened settings. Bisect against the pre-R31.4 in-room behavior. Prove both roots.
- **req:** capture R31.12 = 2 regression ACs (verbatim): (1) in-room chat works (send/receive), (2) tapping the room title opens room settings — each a REGRESSION (cite pre-R31 working). Pin @390 in-room.
- **expert:** fix per architect design (preserve the R31.4/R31.9 drawer wins — the in-room flow is a SHARED behavior on the same drawer, positioning!=function). **tester:** gate @390 IN-ROOM: chat send/receive + room-title→settings-opens; + regression-check the R31.4/R31.9 drawer wins still hold (no re-break). **Tron:** device re-verify in-room.

★ These are REGRESSIONS = the drawer/detail gate never covered the IN-ROOM flow (chat + room-title-settings). Re-gate MUST cover in-room, not just /trace + Server Manager. [[device-qa-regression-means-missing-ac]]
