# T37.21 — TRON EXTENDED SCOPE (2026-09-01)

**Task:** `1bf4acc5-4c9b-41a2-9284-b30d323cfbdf` — *Room Members/Files become real Folder scenario-units with sunburst detail (rides R40.16, no dup)*

**TRON: "that and only that is the task."** · **Deadline: delivered in full + driven to QA REVIEW by TOMORROW MORNING for his approval.**

Evidence (his own screenshots, in `t3721-tron-evidence/`) — **look at them; if an image does not match its description below, say so immediately, the mapping is mine and may be wrong.**

---

## The five parts

### 1 — Room Members/Files ARE folders (even with no disk folder)
*shot1-app-files-is-a-folder.png · `/app`, Heartspaces room.*
Tron: **"Files (5) IS A FOLDER even if it's not on disk."**
Room `Members (5)` / `Files (5)` pseudo-collections become **real Folder scenario-units**. Virtual (no physical dir) but genuine units, not pseudo-nodes. Rides R40.16, **no duplicate folder model**.

### 2 — "Add folder" creates a PHYSICAL folder on disk + live MVC + WS fan-out
*shot2-model-add-folder-physical.png · `/model`, a real on-disk folder (`server`).*
Tron: the **`Add folder`** action must
- **create a physical folder ON DISK**, and
- **live-MVC update the tree view with the new folder — no reload**, and
- **a SECOND BROWSER on a similar view updates immediately over WS.**

Three assertions, all required. A refresh-to-see-it is a FAIL. A single-browser-only update is a FAIL.

### 3 — Kill the redundant Scenario/Edit links in the detail
Tron: *"we already have std action bar buttons for scenario and edit so we see redundant links in the details."*
The detail body repeats Scenario/Edit which the **action bar** already provides → remove the duplication. (Do not remove the action-bar buttons — those are R34.7/R33.6.5 and Tron-verified as of v0.8.153.)

### 4 — The SUNBURST diagram is MISSING from the detail
Tron: *"...but not the tasks required sun burst diagram."*
The task's required **sunburst** is not rendered. Add it to the detail view.

### 5 — Virtual `puml` collection must also show the REAL physical folder tree
*shot3-puml-virtual-vs-physical.png · `/model`, `puml` (61).*
Tron: *"we see the puml files under puml but not in the physical folders as they are. puml is a virtual collection with no physical folder — below I want the folder paths where the real puml files are, as a tree of physical folders as scenario-first units."*
So: keep the virtual collection, and **beneath it surface the actual on-disk folder paths** where those `.puml` files live, as a **tree of physical folder scenario-units**. (Note in shot3: several puml names appear duplicated — `class-diagram.puml` ×4, `use-cases.puml` ×2 — likely because they live in *different physical folders*. That is exactly the information the physical tree must reveal.)

### QA target
*shot4-task-3721-to-qa.png* — drive the task through its checklist to **QA Review**: refinement → creating test cases → implementing → testing → **QA Review**. Tron approves from there. **Nobody flips Done — Done is Tron's act.**

---

## Standing constraints (unchanged)
- **Scenario-first (#126)**: req mints requirements **before** the expert builds. Planner extends the task file.
- **Push freeze ACTIVE** — commit locally, deploy via the served tree, nobody pushes.
- **No security work** without Tron's own explicit GO.
- **A build ships the client immediately** — build + commit + restart as ONE atomic move; bump the **SOURCE** config unit; boot-check.
- Client-facing → verify at **390 real-WebKit, screenshot evidence**, never DOM counts.
- No prod identity/room/device creation, ever.

---

# ⚠ EVIDENCE CORRECTION (PO, 2026-09-01)

**My screenshot files were WRONG and are deleted.** I copied an older upload batch; Tron's four screenshots from this session were never written to disk. **req, tester and planner each read the images independently and all three caught it** — that is the correct behaviour and it is why the error cost minutes instead of a day.

**THE SPEC TEXT ABOVE IS AUTHORITATIVE.** Tron's verbatim words per part are the requirement source. Proceed from them.

## What Tron's four screenshots actually showed (PO-transcribed from his message — not a file, treat as PO-sourced, re-derive against the live app)

**#1 — `prod.wo-da.de:4444/app`, Heartspaces room, v0.8.153.** Header ♥ Heartspaces, members chips (SystemTester, Amos Donges, Marcel Donges, Marcel Donges (you)), "Drop content here / Files become room scenario units". Below, a tree card: **♥ Heartspaces (2)** containing **Members (5)** and **Files (5)**, both with **folder icons** and count badges. Bottom drawer: action bar **[◆ Scenario] [✎ Edit]**, then **Files (5)** listing `LinkedIn Banner.png`, `BABËL - A Music Organization for the World`, `Ed Sheeran - I See Fire (Music Video).mp3`, and two **WebItem** rows (`app#file.show?uuid=…`). ⇒ **Part 1**: Files/Members ARE folders.

**#2 — `prod.wo-da.de:4444/model`.** Left: M2 · UML Profile (31), M1 · Projects (3), RawBin (4) → ts (3) → public (86), shared (1), ts (3) → scenario (39), **server (23) SELECTED** → real files (DeploymentModel.ts, FeatureManager.ts, federation-transfer.ts, FileApi.ts, FolderService.ts, generate-project.ts, migrate-cli.ts, Migration.ts, OtmuxBridge.ts, ProfileView.ts, proxy-fetch.ts, PtyBridge.ts, repo-registry.ts, revoked-tokens.ts, Room.ts…). Right drawer action bar: **[◆ Scenario] [✎ Edit] [📁 Add folder] [⤓ Import PUML]**; body: `COLLECTION / server / 5e4695a9-2f5e-4712-a734-55e33f5815be`, **redundant** `📄 Scenario ✎ Edit` links, then `no children`. ⇒ **Part 2** target is that **Add folder** button; **Part 3** = the redundant body links.

**#3 — `prod.wo-da.de:4444/model`, `puml (61)` expanded.** Lists `avatar-crop-lifecycle.puml` **twice**, `avatar-lifecycle.puml`, `avatar-workflow.puml`, `bug1-chain-excludes-self.puml`, **`class-diagram.puml` ×4**, `cr1-unify-chain-label.puml`, `migration-workflow.puml`, **`object-verb-usecases.puml` ×2**, **`pwa-update-workflow.puml` ×2**… Same right drawer (`COLLECTION / server`). ⇒ **Part 5**: repeated names ⇒ the files live in **different physical folders**, and that physical tree is what must be surfaced.

**#4 — `prod.wo-da.de:4444/model`, Task 37.21 detail.** Action bar **[◆ Scenario] [✎ Edit] [📄 Open Task file]**. Body: `Task 37.21: Room Members/Files become real Folder scenario-units with sunburst detail (rides R40.16, no dup)`, uuid `1bf4acc5-…`, Parent `Profile: Marcel Donges`, **Status Planned**, checklist Planned → In Progress (refinement / creating test cases / implementing / testing) → QA Review → Done. Forward Links: `no links`. Traceability Chain: UseCase `roomCollection.asRealFolderUnit` → Class `FolderService` (`src/ts/server/FolderService.ts`) → Method `FolderService.mintRealUnit` → Implementation. **No sunburst rendered.** ⇒ **Part 4** + the QA-Review target.

## Also observed by req + tester, NOT in Tron's five parts
An older room shot showed a detail reading **"File unit not found"** (a broken/dangling File-unit detail). **Real, recorded, NOT worked** — it is outside "that and only that". Capture it as a deferred defect only.
