# R40.4-phase-2 — S37-FIRST migration log (the reference case)

**By:** robbin-req 2026-08-17, PO-authorized. This is the S37-FIRST phase of the R40.4-phase-2 sprint/task-name DRY migration (UC `sprintName.migrateToAttribute c9f394b1`, req R40.4 `9a8cbffe`). It is the **reference case** the migration tooling must replicate across the remaining ~19 sprints.

## What changed (BEFORE → AFTER)
- **Unit:** Sprint 37 = `scenario/index/b/8/6/b/5/b86b53cc-13cb-409a-81d6-2025b5f2979e.scenario.json`
- **BEFORE:** `model.name = "Sprint 37 — Consistency by Construction"` + `model.number = 37` → the sprint number stored in **two** places → the generator composed `sprintPrefix(37) + " Requirements — " + name` = **"Sprint 37 Requirements — Sprint 37 — Consistency by Construction"** (the double-number H1 lie Tron screenshotted).
- **AFTER:** `model.name = "Consistency by Construction"` (bare title) + `model.number = 37` **UNCHANGED** (the single source).

## Conditions met (PO-required)
1. **Prefix-only strip** — only `"Sprint 37 — "` removed; `model.number` untouched.
2. **Content-conservation** — the bare title is an **exact substring** of the original (no theme text lost). This replaces INV-T byte-diff==0, which cannot apply to a deliberate change.
3. **Reversible** — see revert recipe below.
4. **Fix-unit-then-regen** — the unit was edited, then `generate-sprint-md.ts <S37-uuid>` regenerated the view; the generated MD was **never hand-edited**.
5. **H1 verified** — regen produced `# Sprint 37 Requirements — Consistency by Construction` = the number **exactly once**, no doubling. `check:sprint-md` S37 = **byte-match** (was DRIFT).
6. **Reported** to PO + architect.

## ★ REVERT RECIPE (reversibility decoupled from any chat message)
The S37 regen (`requirements.md +9`) rode into architect commit **be3b66894** via a narrow shared-index pickup (COUPLING risk: reverting be3b66894 also reverts the S37 regen). To revert THIS migration independently:
1. In `b86b53cc-...scenario.json`, restore `model.name` to `"Sprint 37 — Consistency by Construction"`.
2. Re-run `/opt/node22/bin/node --import tsx scripts/generate-sprint-md.ts b86b53cc-13cb-409a-81d6-2025b5f2979e`.
3. That reproduces the pre-migration board (with the double-number H1). No other unit changed.

## For the tooling (remaining ~19 sprints)
Same shape per sprint: number from the `sprint-<n>` DIRECTORY (authoritative), strip the leading `"Sprint <n>[ —:-]"` from `model.name`, dry-run + before/after + AMBIGUOUS-list (unresolvable numbers REPORTED, never invented), assert content-conservation (bare title ⊂ original), then regen. Also the TASK family (`taskDisplayName`, delete `task.sprintName`, set `task.taskIndex`). Phased, self-draining `check:sprint-md` report-only→auto-strict-at-0 gate tracks the remaining count.
