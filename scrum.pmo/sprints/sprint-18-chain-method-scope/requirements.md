[Back to Planning](./planning.md)

# Sprint 18 — Requirements

**Source:** Tron directive 2026-06-05. Verbatim preserved in [compound-requirement-source.md](./compound-requirement-source.md).
**Decomposed by:** robbin-req (Rules 9-11 applied: deduplication check, verb×noun cross-product, compound-source-as-input).
**Process:** Scenario.json-first (dogfood S17). These requirements become scenario units BEFORE task MDs are generated.

---

## Verb × Noun Cross-Product (Rule 10 gate)

Tron's directive contains 4 verbs and 4 nouns:

| | Class.methods display | Chain narrowing | Sprint authoring | Role protocol |
|---|---|---|---|---|
| **show / keep** | R18.1a (scenario: ALL) | — | — | — |
| **narrow / filter** | R18.1b (trace: ONE) | R18.2 | — | — |
| **author / generate** | — | — | R18.3 | — |
| **co-specify / codify** | — | — | — | R18.4 |

4 atomic requirements identified. Deduplication check: no existing requirement covers "scenario browser shows all methods while traceability browser narrows to one." This is genuinely new.

---

## Requirements

- [ ] **R18.1: Scenario browser shows ALL methods of a Class; traceability browser shows ONLY the chain-relevant method.**
  [requirement:uuid:18a1b2c3-d4e5-4f60-8a71-000000018001]
  > TRON: "classes have All methods in the traceability chain. this is very good for the overall scenario browser! but on the traceability browser it needs to be only exactly the one method, that fulfills the current requirement down to the test that tests it."

  Two views of the same Class, two different method sets:
  - **/scenario browser** (`/scenario?ior=<class-uuid>`): expands to show ALL methods of the class. This is the complete object model view. KEEP as-is — Tron says "this is very good."
  - **/trace browser** (`/trace`): when the chain req→task→UC→Class is followed, the Class node expands to show ONLY the one Method that is in the current requirement's chain (the Method whose Implementation satisfies THIS requirement's Test). Other methods of the same class are NOT shown in the trace tree.

  **Acceptance criteria:**
  - [ ] In /scenario, expanding a Class node shows ALL its methods (unchanged)
  - [ ] In /trace, expanding a Class node shows ONLY methods that are in the chain from the current requirement root to a Test
  - [ ] A Class with 5 methods but only 1 in the current chain shows 1 method in /trace, 5 in /scenario
  - [ ] If a Class has methods in multiple requirement chains, /trace shows only the methods relevant to the chain being browsed

- [ ] **R18.2: Traceability chain through a Class narrows to the single requirement-fulfilling Method, its Implementation, and the Test that tests it.**
  [requirement:uuid:18b2c3d4-e5f6-4a71-9b82-000000018002]
  > TRON: "on the traceability browser it needs to be only exactly the one method, that fulfills the current requirement down to the test that tests it"

  The narrowing is not just at the Class→Method level — it continues through the full tail: Class → THE one Method → THE Implementation → THE Test(s). The chain does not fan out at any level below Class. The path from a requirement root through the trace tree is a single thread (with only the final impl→test being 1:N per R-E cardinality).

  **Implementation hint:** When `rb-trace-tree` expands a Class node in `/trace` context, it must intersect `class.methods[]` with the set of methods reachable from the current requirement root. Only methods that appear in BOTH the class's full method list AND the requirement's forward chain are shown.

  **Acceptance criteria:**
  - [ ] Expanding a Class in /trace shows only methods that trace (via impl→test) back to the requirement root of the current chain
  - [ ] The narrowed chain is: req → task → UC → Class → 1 Method → 1 Impl → N Tests
  - [ ] No method from a different requirement's chain appears under this Class in /trace

- [ ] **R18.3: Sprint 18 is authored as scenario.json units first; planning.md and task-*.md are generated from those units.**
  [requirement:uuid:18c3d4e5-f6a7-4b82-9c93-000000018003]
  > TRON: "start to plan a new sprint as scenario.json first and then generate the sprint and task mds from it"

  This sprint dogfoods the S17 scenario-unit system (R17.7-R17.10):
  1. Sprint unit: `<uuid>.scenario.json` with `ior: "ior:class:Sprint"`, model containing sprint name/goal/tasks[]
  2. Task units: `<uuid>.scenario.json` with `ior: "ior:class:Task"`, model containing task name/status/ACs
  3. Requirement units: `<uuid>.scenario.json` with `ior: "ior:class:Requirement"`, model containing atomic text + tasks[]
  4. `planning.md` generated by `ViewTemplateRegistry.renderMd(sprintUnit)` — NOT hand-written
  5. `task-*.md` generated by `ViewTemplateRegistry.renderMd(taskUnit)` — NOT hand-written

  **Acceptance criteria:**
  - [ ] Sprint 18 scenario.json units exist in `scenario/index/` BEFORE any .md files
  - [ ] `planning.md` is generated output, not hand-authored
  - [ ] `task-*.md` files are generated output, not hand-authored
  - [ ] Modifying a scenario.json unit and re-running generation updates the .md file
  - [ ] Generated .md matches the format/quality of hand-written sprint planning files

- [ ] **R18.4: The three refinement roles (req-eng, architect, planner) co-specify their protocols as SKILL.md files.**
  [requirement:uuid:18d4e5f6-a7b8-4c93-9da4-000000018004]
  > TRON: "let them try to co-specify this as Skills for their roles"

  The precedence analysis (refinement-precedence-analysis.md) and each role's decomposition/chain/scheduling protocol becomes a durable SKILL.md file:
  - `robbin-req/SKILL.md`: compound→atomic decomposition, deduplication (Rule 9), cross-product gate (Rule 10), compound-source-as-input (Rule 11)
  - `robbin-architect/SKILL.md`: chain-vs-dependency separation (Rules 1-5), UC/Class/Method creation precedence, PUML-first design
  - `robbin-planner/SKILL.md`: task creation only after decomposition complete (Rules 6-8), label reservation, closure freeze

  **Acceptance criteria:**
  - [ ] Each of the 3 roles has a SKILL.md in `.claude/agents/<role>/` or `session/agents/<role>/`
  - [ ] Each SKILL.md codifies the role's rules from refinement-precedence-analysis.md
  - [ ] SKILL.md survives agent reboot (read on boot, not just in context)
  - [ ] The three SKILL.md files are consistent (no contradictory rules)

---

## Decomposition Completeness Confirmation

**Verbs in Tron text:** "have" (methods), "needs to be only exactly the one" (narrow), "start to plan" (author), "co-specify" (codify)
**Nouns:** scenario browser, traceability browser, method/class, sprint/task as scenario.json, role skills
**Cross-product exhausted:** 4 cells filled, 12 cells empty (verb doesn't apply to noun). No missing atoms.
**Deduplication:** No existing requirement covers "scenario shows all, trace shows one." Checked R-U1/R-V1/R-Y1 (lazy-load depth, different topic), R-E (chain order, different topic).

**Decomposition COMPLETE.** Planner may create tasks.

---

## Forward Traceability

| Req | UUID (short) | Task | Description |
|-----|-------------|------|-------------|
| R18.1 | `18a1b2c3` | TBD | Two-view method display (scenario=all, trace=one) |
| R18.2 | `18b2c3d4` | TBD | Chain-through-Class narrowing to single method thread |
| R18.3 | `18c3d4e5` | TBD | Scenario.json-first sprint authoring |
| R18.4 | `18d4e5f6` | TBD | Role SKILL.md co-specification |
