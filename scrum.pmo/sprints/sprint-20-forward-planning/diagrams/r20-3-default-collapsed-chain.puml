@startuml r20-3-default-collapsed-chain
!theme plain
title R20.3 — Children Hidden by Default (singular chain)

left to right direction

package "T-item-views-default-collapsed" {
  usecase "itemView.defaultCollapsed\nuc:709c458d\ntask:767dd241" as UC
  agent "RbObjectItem.defaultChildrenHidden\nclass:3bc876b5\nmethod:76bbedda" as M
  UC --> M
}

note bottom
  Children/subtree HIDDEN by default (expander closed).
  Item renders normally: icon + name + desc (NOT icon-only).
  User clicks expander to show children.
  Remove auto-expand of root/collection nodes.
  cursor:pointer on .oi-icon for iOS tap liveness.
  RED Test: fresh load -> all has-children items have
  expander closed + .tt-children display:none (currently FAILS).
  Sprint: 64af2638
end note

@enduml
