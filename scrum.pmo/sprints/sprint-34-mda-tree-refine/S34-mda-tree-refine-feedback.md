
## R-A A2 FORK RULING (robbin-architect 2026-08-03, expert query) — OPTION (A), resolve-at-detail
Locking the R-A A2 approach: **OPTION (A)** — the tree node-ref STAYS `dir:`/`file:` (rb-trace-tree + `mofChildren` expansion BYTE-UNCHANGED = zero tree/expansion regression, matches my "tree render UNCHANGED"). The "real unit" requirement is satisfied at the **RESOLVER layer**: when a `dir:`/`file:` node's DETAIL is opened, `/api/ior` (+ `/scenario`) maps the ref → a real MODEL_STORE `ior:class:Folder`/`ior:class:File` unit, uuid = `keyToUuid(<rel-path>)` (R32.2 law), minted IDEMPOTENTLY & LAZILY on first detail (deterministic → re-resolve re-binds, no dup), MODEL_STORE-only (prod scenario/index UNTOUCHED). That unit backs the detail view + the R-A A1 «Scenario»/«Edit» default (Scenario→`/scenario?ior=<uuid>`).
- **REJECT option (B)** (node-ref → `ior:instance:<uuid>` + re-route mofChildren via stored srcRel): more invasive (touches expansion routing + the A1 refUuid path) AND it CHANGES the tree refs = violates "tree render UNCHANGED". No added benefit — the real-unit need is met at the resolver without touching the tree.
- **INV-A2:** tree + mofChildren expansion byte-identical (INV-A2-1, zero regression); resolver mints Folder/File idempotently by deterministic uuid (A2-2, no dup); MODEL_STORE-only, prod untouched (A2-3). Reuse `keyToUuid` + `createFileUnit` shape + the existing `/api/ior`↔MODEL_STORE routing (isModelUnit). NO fork of the tree.
- **Chain note:** req must IMPL-MINT the T34.2/R34.2 chain (UC + Method on a name-matching `mofChildren`/resolver decl + Impl markerPending) — currently useCases:[] (expert can't self-mint, #126). I mint/repoint if req env can't.
