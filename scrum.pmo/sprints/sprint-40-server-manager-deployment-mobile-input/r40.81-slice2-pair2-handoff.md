# R40.81 Slice-2 → PAIR-2 HANDOFF (durable, for a COLD tester) — robbin-expert 2026-09-06

The tester was rewound before PAIR-2; this file carries everything a fresh tester needs to fire PAIR-2 cold — no pane
memory required. All inputs are committed. (A pane message dies at the cut; this file does not.)

## THE ONE VALUE PAIR-2 WAS MISSING: SLICE2_COMMIT
- **`SLICE2_COMMIT = 4e3427949`** — v0.8.194, the Slice-2 repoint commit that honours `MODEL_READ_SOURCE`.
- It collapses the `isModelUnit ? MODEL_STORE : scenario/index` read-fork (+ mofChildren caller-index + trace-merge model
  reads + the `isModelUnit` discriminator) into ONE resolver `modelReadDir()` in `src/ts/server/server.ts` that reads
  `process.env.MODEL_READ_SOURCE` AT the resolution point (every call) → the flag genuinely flips the read. 0 read-fork remains.

## RUN PAIR-2 (the runner already exists + is committed)
```
SLICE2_COMMIT=4e3427949 <project-node> test/visual/r4081c-pair2-run.mjs
```
(project node e.g. `/opt/node22/bin/node` or a vscode-server node; the runner boots the SAME build twice.)
- Runner: `test/visual/r4081c-pair2-run.mjs` — boots SLICE2_COMMIT TWICE, same seeded MODEL_STORE + same scenario/index,
  flipping ONLY `MODEL_READ_SOURCE`: **PRE=`model-store`** vs **POST=`scenario-index`**. Same code, zero drift.
- Canonical set: `test/baseline/r40.81-behaviour-pair-canonical-213-set.txt` (the runner reads it).
- Pinned snapshot: `test/baseline/model-store-premigration-v0.8.186.tar.gz` (tar-seeded, never cp/hardlink → real inodes).
- FLAG-LIVENESS positive control is BUILT IN (sentinel `facade99-…-081` written to both scratch stores with DIFFERENT
  bytes): if the resolver returns the SAME name across the flag-flip → the flag is a NO-OP → INSTRUMENT GAP, exit-2, NEVER
  green. This is the architect's "not a no-op false-green" backstop, already in the runner.

## PASS / FAIL (pre-defined, cannot be reinterpreted after)
- **GREEN** = every unit in the 213-set ANSWERS BYTE-IDENTICAL from scenario/index (POST) as from MODEL_STORE (PRE), AND the
  sentinel name DIFFERS across the flip (flag proven live), AND mof-m1 is non-empty. ⇒ the repoint is transparent ⇒ the
  claim Slice-3 (delete MODEL_STORE) rests on.
- **RED** = any unit answers DIFFERENTLY from scenario/index than from MODEL_STORE ⇒ STOP, do NOT delete the source store;
  fail-closed. Empty mof-m1 or a no-op sentinel = INSTRUMENT GAP (exit-2), never a green.

## APPLY NUMBERS + INV-DATA (Slice-1, measured at apply-time)
- Slice-1 relocate APPLIED + durable — commit `1d7462512` ("669 relocated units, INV-DATA GREEN, path-limited").
- Apply-time re-measure (2026-09-06): MODEL_STORE real files = **703**; **702 overlap byte-identical** (the 669 relocated +
  33 original SET B); **DIVERGENT = 0** (per-uuid sha re-check, fail-closed, no winner picked); A relocate live = **1**
  (one NEW straggler appeared post-snapshot — needs req/architect RE-RECONCILE before applying, NOT a blind relocate).
- INV-DATA: model units resolve via scenario/index when flipped; MODEL_STORE PHYSICALLY INTACT (relocate-not-delete,
  Slice-3 removes it only after PAIR-2 GREEN); `/api/model/tree` at DEFAULT (model-store) = no-regression (mof-m2 31 / mof-m1 3).

## PROD STATE (do not disturb until PAIR-2 GREEN)
- Prod runs v0.8.194 at DEFAULT `MODEL_READ_SOURCE=model-store` → TRANSPARENT, nothing flips for Tron.
- The flip to `scenario-index` on live is the ROLLBACK lever — flip it live ONLY after this scratch PAIR-2 is GREEN.
- Architect verifies repoint-honours-flag before handing the sha; borderline server.ts add-view fallback read left pending an architect ruling (not a read-view PAIR-2 site).
