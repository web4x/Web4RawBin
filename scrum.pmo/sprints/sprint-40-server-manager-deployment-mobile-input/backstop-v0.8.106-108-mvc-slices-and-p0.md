# Backstop v0.8.106/107/108 + P0 process analysis (architect)

robbin-architect 2026-08-18. Backstop of the 3 landed ships against the locked criteria + the PO process question (which matters more than the hotfix). Served==committed==0.8.108.

## Backstop verdict — criteria
| Criterion | Verdict | Evidence |
|---|---|---|
| **client reads out.code (not optimism)** | ✅ PASS | `universal-actions.ts:101` branches on real `r.status` (200/403/409); `:105` even distinguishes "200 but status NOT Done" → shows "⚠ Recorded, but status is X (NOT Done)" instead of claiming success. Reads the server's actual code AND derived status. |
| **atomic = real single transaction (refused persists nothing)** | ✅ PASS (by evidence) | validate-refuse → **409 BEFORE any persist** (make-current catch classifies "only a Planned/In-Progress" → 409; client 409 branch). approve routes `approveByOwner → apply(Done) → deriveStatusEnum` (sole writer) → emit. ⚠ the deepest transaction-tear (a throw MID-write leaving a partial unit) is what the action-smoke (below) must exercise — not statically provable. |
| **ONE bus, 2nd file retired, Profile/Room/badge live** | ✅ PASS-with-note | `src/public/ts/ViewBus.ts` now `import { ViewBus } from './trace/ViewBus.js'; export { ViewBus }` and `viewBus` = a **thin adapter** (`publish→notify`, `subscribe→subscribe` keyed `classType:uuid`) over the ONE trace instance — **no own subs Map → the 2nd SOURCE is retired** (one instance, one emit); ProfileSheet/RoomBrowser/rb-member-badge/ProfileEditor now ride it. NOTE: the FILE remains as a compat shim (not deleted). If PO wants the file gone too, the 3 consumers import `trace/ViewBus` directly — a cleanup, NOT a correctness gap. |
| **owner resolves from protected-identity set on BOTH surfaces** | ◐ PASS-with-caveat | approve AC-1 = `approver.id = profileUuidOf(ownerToken)` (non-credential). make-current uses `${actor}`; confirm `actor` resolves from the SAME protected-identity/owner set as approve (the hotfix only fixed the log identifier — verify the AUTH resolution is the one set on both). Flag for the action-smoke. |
| **acting-tab emits CONFIRMED unit-changed, no special path** | ✅ PASS (construction) | v0.8.107 (92356ef9e) = TAB-A re-render onto the ONE bus. Runtime-pixel of the surgical re-render remains the honest gap (was never executed — see below; the server crashed on approve before any propagation could run). |

## ★ PROCESS ANSWER (the priority)
### (a) Did one of the 4 slices introduce the crash? NO — it was pre-existing (R40.10) and EXPOSED, plus copied.
`ownerTok8` in the approve `addLog` traces to **`ca4582ae7` R40.10 (v0.8.73)** — the ORIGINAL approve/decline feature. It was a LATENT scope bug: the approve handler's `addLog` referenced `ownerTok8`, which is only a PARAMETER name of `declineToChangeRequest` — undeclared in the approve scope. The 4 MVC slices did **not** introduce it; they **EXPOSED** it — the action-bar + live-approve work finally made an owner-SUCCESS approve reachable, so Tron's tap on 37.26 was the FIRST time the `2xx → res.end → addLog` branch ever ran (existence≠connection, L5: the buggy branch existed since v0.8.73 but was never CONNECTED to a real owner-success execution). The **make-current** crash IS recent — T37.26 (v0.8.105) copied the same buggy `addLog` pattern.

### (b) What else on that path is unexercised? The entire owner-SUCCESS branch.
An undeclared identifier surviving to prod PROVES the `2xx → res.end → addLog` branch never executed once in any test. Every prior verification — **mine included** — was **reject-direction** (non-owner → 403, which returns BEFORE the addLog) or **construction-only** (per-line vs served). The **accept-direction** (owner approves → 200 → propagate) was deferred to "Tron's 2-tab tap." That deferral is exactly how a server-crashing bug survived ~10 live-MVC iterations: the server DIED on approve before any propagation could run, so the MVC behavior we kept iterating on **never actually executed**. Unexercised: owner-success approve, owner-success make-current, and the live propagation they trigger.

### (c) What gate would have caught it — an ACTION smoke, not a boot smoke.
Boot-check passes because the server BOOTS; the crash is on the ACTION path. Fix-forward gate = a real **owner-SUCCESS action smoke** (R40.31-ISOLATED scratch — never prod, never a real Done, disposable worktree index): exercise approve + make-current end-to-end AS OWNER and assert **200 on owner-success · 409 on validate-refuse · 403 on non-owner · ZERO 5xx** (any 5xx / dropped connection / process-exit = RED). **stub-must-fail:** re-introduce a post-response throw or an undeclared identifier → the smoke catches the 5xx. This is the accept-direction coverage that reject-direction + construction backstops **structurally cannot** provide. The hotfix's `if (!res.headersSent)` guard closes the crash CLASS; the action-smoke proves the branch RUNS.

## ★ L12 (recorded) — boot-check ≠ action-check; exercise the owner-SUCCESS branch headlessly
A reject-direction (403) or construction (per-line) pass proves NOTHING about the branch that only runs when the action SUCCEEDS. An undeclared identifier / post-response throw fires only on that exact branch. Never defer the accept-direction to a human tap — make it a headless isolated action-smoke (owner-success → 200 + zero 5xx). This was MY recurring gap: I repeatedly verified reject-direction + construction and wrote "accept = Tron's tap" — and that is how the P0 reached prod. Own it; the fix is the standing action-smoke gate.

## Recommendation
Ships are structurally sound on the criteria; the ONE thing between here and Tron's tap is the **action-smoke actually running green** (owner-success approve/make-current, zero 5xx). PO said no Tron tap until a real end-to-end run proves the path — I concur, and the action-smoke IS that run. It also closes my owner-both-surfaces caveat (it exercises make-current's owner resolution). Capture-only for R40.44/45/46 per Tron's schedule; this backstop + the action-smoke gate are the fix-forward.
