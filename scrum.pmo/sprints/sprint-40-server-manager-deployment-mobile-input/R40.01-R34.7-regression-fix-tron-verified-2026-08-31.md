# R40.01 / R34.7 INV-E1 action-bar regression — FIX Tron-verified, PREVENTION GATE PENDING

*Recorded by robbin-planner 2026-08-31 (PO-directed). A Tron-verified fix that is not written down decays into folklore. This record is deliberately NARROW and TRUE, not wide and flattering.*

## 1. Provenance — VERBATIM (not paraphrased, not inflated)
- **approvedBy: Tron.** His exact words this session: **"delivery fixed it."**
- Said **directly this session (2026-08-31)** after he **re-checked his own device at 390px**. This is a confirmation of the FIX on his screen — NOT "Tron approved the requirement." He was looking at the Server-Manager otmux-tree terminal drawer at 390 and saw the action bar restored.

## 2. Scope — TIGHTLY what he actually confirmed
- **Confirmed:** the ACTION BAR IS BACK on the **server-manager terminal drawer** @390.
- **This resolves, ON THAT SURFACE:**
  - **R34.7 `ad8c6d3e`** INV-E1 (AC-universal-all-mounts) — the bar renders on /server-manager by construction — the regression.
  - **R33.6.5 `3c6eee8d`** AC-actionbar-fixed-in-drawer — the fixed region below the grab-bar / above content — the violation.
- **Fix:** v0.8.153, commit `198951f16`; **root cause** commit `e55e5de7e` (architect-named); expert fixed **BY CONSTRUCTION** in the shared drawer. PO verified 4-way: **served == committed == source-unit == sw == 0.8.153.**
- **★ NOT confirmed — do NOT ripple into a broader Done:** Tron did NOT confirm the WHOLE of R40.1 `caab6d86` or the WHOLE of R34.7 `ad8c6d3e` as satisfied. This is a narrow regression-fix confirmation on one surface. The covering tasks (R34.7→`3bec71f4`, R33.6.5→`eb8cf1e7`) and R40.1 stay as they are — **NOT flipped to Done by this record.**

## 3. The open half — RECORDED in the same breath
- **The by-construction PREVENTION gate is NOT yet proven.** The tester is still building the harness and **must show it RED against the pre-fix bundle** before it means anything — a gate that has never failed certifies nothing.
- **Status = FIX Tron-verified / PREVENTION GATE PENDING.** Not simply "fixed." A board reader next month must NOT conclude the regression is structurally impossible: we have not yet demonstrated the test can fail.

## Chain that got here (credit, no padding)
req named R34.7 INV-E1 as the violated container · planner independently verified on disk and corrected its own earlier R35.1/R40.5-as-container to align (one voice, not two) · architect named the exact commit `e55e5de7e` + mechanism · expert fixed by-construction in the shared drawer and self-reported a botched first restart its own boot-check caught · tester read Tron's image itself and refused to owner-auth prod to reproduce. Nobody guessed, nobody padded.
