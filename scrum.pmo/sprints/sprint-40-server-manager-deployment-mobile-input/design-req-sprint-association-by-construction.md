# Sprint-association enforced at req-mint — by-construction (architect, 2026-08-29)

PO-routed via planner: 24 requirements are wired to NO sprint (no `sprintName` + no resolvable parent-Sprint) → invisible to every sprint view = unplannable/untrackable BY CONSTRUCTION. PO ruling: do NOT backfill-attach (that hides the defect till it recurs); treat the 24 as the SYMPTOM. Question to me: is sprint-association enforced at req-mint, and SHOULD a sprint-less req be structurally impossible?

## Measured (the by-construction gap is real)
- **No shared req-mint helper enforces a sprint parent.** `ior:class:Requirement` units are minted in ≥3 code paths (skills.ts, templates.ts, server.ts) + ad-hoc scripts; grep finds no `requireSprint` / mint-path sprint check. The mint path PERMITS a sprint-less req.
- **No audit surfaces sprint-less reqs** (trace-audit.ts has no such check). So orphans are neither prevented at mint NOR surfaced after — silent by construction. The 24 are the predictable product.
- This is a **truth-decay / born-invisible** instance (family R37.25): a req reads authoritative but is untrackable-by-construction — cure = derive/reject at authoring + surface-don't-silence. Same shape as mintOrReuseClass (R27.2) and ensureViewUnit-refuses (R40.66).

## ★ COUNT CORRECTED + SPLIT (planner worklist 23224191e, scrum.pmo/reports/orphan-sprintless-reqs-2026-08-29.md) — the audit target is ALL 62, not the 24 uncovered subset
- **13 REAL legacy reqs** (have altId, S10-S18 era: R10.4/R12.1/R16.5-9/R17.48/R18.33-35 etc.) → triage to TRUE sprint (a decision, per req/planner).
- **★ DISPOSITION FLIPPED — 0 JUNK (planner structural re-classify 8e0f9f156, PO-ordered 3 signals):** running the three mechanical signals (NOT the prefix shape) resolved the "49 junk" to **0 junk · 14 LOAD-BEARING (inbound refs from tasks/UCs/Impls/Tests) · 35 real-content · all 49 REAL.** DISPOSITION: **NOTHING retired/quarantined — all 62 TRIAGE to sprints** (the 14 load-bearing FIRST, they anchor live chains). The non-v4 uuid is a **DATA-QUALITY flaw on a REAL req** (28/49 are non-v4) → **fix the uuid via the iorInstance funnel** (re-mint a real uuidv4 + rewrite inbound refs), NEVER delete the req.
- **★★ THE 3-SIGNAL CRITERION CAUGHT ITS OWN DESIGNERS — and that is the WIN:** BOTH the planner's first pass AND my "verified by inspection" pattern-matched the uuid PREFIX shape (`d4e5f6a7-*`) = the text-not-structure family, on our own junk-classifier. The structural criterion (PO condition 2, which I folded in) REVERSED a 49-unit near-irreversible misclassification BEFORE any action, and quarantine-not-delete (condition 1) meant nothing was lost while we corrected. hand-fabricated-uuid ≠ deletable: a non-v4 id on a referenced unit is load-bearing. I OWN my shape-match error (same frame as the planner's, PO-caught).
- (superseded) the three-signal quarantine mechanics below stand as the CRITERION (they produced the 0-junk result); only the DISPOSITION changed (0 quarantine, all triage). Classify by STRUCTURE, not prefix:
  - **★ SELF-CORRECTION (owned):** the first-pass classification (planner's + my "verified by inspection") keyed on the uuid PREFIX family (`d4e5f6a7-*` / `18*`) — that is the TEXT-NOT-STRUCTURE family (matching a prefix string, not structure) applied to our own junk-classifier. Prefix-matching is exactly what we must not do.
  - **THREE MECHANICAL SIGNALS, ALL required to quarantine** (reproducible/auditable, not "by inspection" — 49 irreversible-ish actions demand it): **(a) uuidv4-INVALID structurally** (version nibble ≠ 4 in group-3 OR variant nibble ∉ {8,9,a,b} in group-4 — a validity check, NOT a prefix family), **(b) ZERO inbound references** (no Task/UC/Class/Method/Impl/Test/Sprint points AT it), **(c) no real content** (empty shell — no meaningful model fields). All three ⇒ quarantine-with-evidence. **ANY inbound reference ⇒ it is NOT junk regardless of its id — it is load-bearing (quarantining it breaks live chains) → route to triage instead.**
  - **QUARANTINE = mark + evidence + recoverable** (record the three signals per unit); the classifier could be wrong (it already was once, by prefix-shape), and a marked unit stays honest and re-checkable; a deleted one is a guess we can never re-verify. [[preserve-dirty-unit-before-restoring-unexplained]].
- **★ SECOND mint-path gap revealed (companion finding):** the 49 fabricated uuids prove a mint path that accepted HAND-AUTHORED (non-uuidv4) identifiers — a DISTINCT by-construction defect, sibling to requireSprint AT THE SAME CHOKE-POINT. So the mint choke-point (below) must ALSO **validate uuidv4** (reject a fabricated/hand-authored id, fail-loud). This is [[never-fake-suffix-uuid]] (learning #17) enforced structurally. The r4011 owed `iorInstance()` funnel is the shared cure locus: ONE mint choke-point validating BOTH sprint-association AND uuidv4-ness (+ the r4011 no-raw-construction rule) — three by-construction gaps consolidating at one place.
- **Positive-control ANCHOR (verify at detector-run):** R37.13 `5576307b` (sprintName=Sprint37, planner-provided) — the detector must NOT flag it (proves a later 0 = real-absence, not blind-query). I confirm both-ways when the detector is built.

## RULING: YES — make a sprint-less req structurally impossible (S37's own thesis)
1. **Prevention (by-construction, primary):** route req-mint through ONE helper that REQUIRES a resolvable sprint — `sprintName` resolving to an existing Sprint OR a `parent` → Sprint edge. No resolvable sprint ⇒ **reject + fail-LOUD** (never mint a silent orphan). Every req-mint call-site routes through it (the choke-point pattern; a mint that bypasses it is itself a violation, caught by #3). It is NOT enough to warn — an unparented req must not come into existence.
2. **Backstop (CI, un-skippable):** trace-audit asserts **0 sprint-less reqs** (a req with neither a resolvable `sprintName` nor a `parent`→Sprint). **DELTA-gated** (the R27.2-INV2 lesson): the 24 pre-existing are debt NOT created by this guard → **WARN + SURFACE now** (list the 24 by uuid in the audit output — coverage-self-report style, never a silent count), flip to **RED after triage**.
   - **★ CONCRETE TIME-BOX (PO refinement, [[define-the-timebox-concretely]] — a delta-gate with no flip date is a permanently-yellow gate = the background-noise failure):** START = 2026-08-29. METRIC = sprint-less req count (**62 today** — 13 real + 49 fabricated-junk; 24 was only the uncovered subset). TARGET = 0 (13 triaged to true sprint + 49 retired). CHECKPOINT = the count is PUBLISHED in the audit output each run (convergence visible). DEADLINE = **2026-09-12** (14 days) to FLIP surface-only → hard-RED. ESCALATION CEILING: if count > 0 at the deadline → **escalate to PO** (a triage-rate/blocker decision), NEVER silently extend the warn phase. **RATIFIED by PO 2026-08-29.** ★ SLIP CONDITION (PO): if the deadline slips it must be an EXPLICIT escalation to PO with a NEW DATED deadline — never a silent extension, never "still surfacing". A date that moves quietly is the same as no date (the time-box's own truth-decay guard). The published-each-run count makes convergence-or-stall visible without anyone asking.
   - **★ TWO-SIDED detector control (PO refinement — the vacuous-pass twin):** the stub-must-fail asserts BOTH directions: (a) it **FLAGS a known sprint-LESS req** (proves it can fire = Arm-B positive control), AND (b) it does **NOT flag a known SPRINTED req** (proves it isn't flagging everything = the vacuous-pass guard). One without the other is the false-confidence shape — a detector that flags everything "finds" all 24 and means nothing.
3. **The 24 = SYMPTOM, surfaced NOT backfilled (PO ruling honored):** each triaged INDIVIDUALLY by req/planner — a real req → attach to its TRUE sprint (a decision, not a default); a dead/superseded req → retire. **Never bulk auto-attach** (that manufactures a false "planned" and hides the recurrence). The audit's surfaced list IS the triage worklist. I'll take the 24 uuids and verify the detector BOTH-WAYS before trusting the list: (a) it flags each of the 24 as genuinely sprint-less, AND (b) it does NOT flag a known-SPRINTED req (else a detector that flags everything "finds" all 24 and means nothing — the vacuous-pass twin).

## Boundary (freeze intact)
This is a DISTINCT by-construction guard (req-mint sprint-association), related to the truth-decay family by cure-shape but NOT one of the frozen 6 per-class gates — the R37.25 per-class set stays frozen. Cross-ref the family; whether this becomes a family member or a standalone S37 guard is req/planner+PO's call. I ruled the mechanism, not the taxonomy placement.

## Handoff
req: mint the requirement (req-mint rejects sprint-less; audit 0-sprint-less delta-gated) + the triage decisions for the 24. planner: stand up the guard task + the 24-triage. expert: the mint-path choke-point + the trace-audit check (self-biting). I re-inspect (mint rejects a sprint-less req; audit lists the 24 then REDs post-triage; detector positive-control). Not urgent vs recorder/fact-2; a real by-construction improvement.
