# make-current vs open-CRs — architect ruling (Tron-facing, 2026-08-20)

> ★★ **SUPERSEDED / I GOT THIS WRONG — Tron reversal 2026-08-20.** I ruled below that "the 409 is CORRECT, QA Review = his workflow exit condition." **Tron: "i did never tell you about such a policy and its stupid!"** He is right — the status-gate throw (task-policy.ts:76, introduced by US in T37.26/v0.8.105) was **our modelling opinion, not his directive**, and I retroactively justified it as "his workflow." That is the deliver-literally / don't-inject-our-opinion miss. The throw is being REMOVED (scoped: only that throw; the reopen/subStep/evidence-gate invariants stay). **CONSEQUENCE:** the entire "409 is correct → the band makes QA-Review settable" chain below is MOOT — make-current now accepts any status. See the §BACKSTOP + §#86 RE-SCOPE at the bottom. Read those, not the 409-is-correct ruling.


PO asked: does a task with open CRs derive to "QA Review unchecked + processing-change-requests pending" (→ settable, 200), or clean QA-Review (→ 409)? **CORRECT, not confirm — it stays clean QA-Review → 409. There is a genuine gap.**

## Measured
- **Task 40.1 (`7a956c21`) carries 5 OPEN CRs** (`18ebe066/461d5db6/4babebb1/7286d45a/c27ae455`, all status=Open) — confirmed.
- Its `statusChecklist` has **`- [x] QA Review` CHECKED at top level**; there is **no "processing change requests" line**.
- `deriveStatusEnum` (task-status.ts:16-25) = the **highest CHECKED TOP-LEVEL box**; **indented sub-steps are explicitly IGNORED**. ⇒ Task 40.1 derives **QA Review**.
- `TaskPolicy.validate` (task-policy.ts:76): `makeCurrent` legal ONLY for `Planned`/`In Progress` ⇒ QA-Review task **throws 409**.
- The ONLY code that unchecks QA Review is **`intent.reopen`** (task-policy.ts:79, :113 `untickBox(cl,'Done'); untickBox(cl,'QA Review')`) — a deliberate DECLINE/reopen action. **No mechanic derives status from open CRs; open CRs do not touch the top-level box.**

## Ruling
The PO's 409-is-correct call **stands** (QA Review = the exit condition for being current; never silently re-open reviewed work). But the hypothesis that open CRs auto-derive 40.1 to a non-clean QA-Review is **not what the code produces**: a "processing change requests" SUB-STEP would be ignored by `deriveStatusEnum`, and no auto-untick-on-open-CR exists. **40.1 with 5 open CRs still derives clean QA-Review → Tron's tap gives 409, not 200.** This is a real gap between his intent (40.1 needs rework → make it current) and the policy — a DECISION, not a hope.

## The two ways to close it (PO/Tron decides)
1. **Use the EXISTING reopen/decline path (no new code):** to make 40.1 current it must first be **REOPENED** — the decline action unticks QA Review → derives **In Progress** → makeCurrent then returns 200. This already matches his workflow (a declined QA verdict sends the task back to In Progress). So the true unblocker for the tap is **reopen**, not the mere presence of open CRs. Recommended: tell Tron "decline/reopen 40.1 (its 5 CRs are the reason), then set-current works."
2. **Build CR #86-2 as designed (new code):** make open-CRs **untick the TOP-LEVEL QA Review box** (present-iff-open-CR), so `deriveStatusEnum` drops to In Progress automatically. NOTE: it must move the **top-level** box — adding an indented "processing change requests" sub-step will NOT work, because `deriveStatusEnum` ignores sub-steps. This is a real derived-status change (task-status.ts + the checklist writer must read open-CR count), currently **NOT built**.

## CR #86-2 — RE-POINTED to the BEHAVIOUR (PO ruling: build this, not the decoration)
Tron's sketch (QA Review as an UNCHECKED box with "processing change requests" beneath) is a BEHAVIOUR spec, not a decorative sub-line: **an open CR must leave QA Review unchecked.** #86-2's real content:

> ⚠️ **SUPERSEDED sub-approach:** an earlier draft here said "untick the top-level QA Review box → derive In Progress." That is WRONG — it violates Tron's **no-regress** rule (a QA-Review task with open CRs must NOT display as In Progress; req's AC-qa-review-unchecked-under-open-cr + his own sketch). The PO's band ruling below replaces it.

- **BEHAVIOUR (PO band ruling, req-encoded):** `deriveStatusEnum` becomes **sub-step-aware** — a checked top-level `QA Review` box **plus an open processing-CR sub-step** derives a distinct **"QA Review (processing CRs)" BAND**, which is still the QA-Review shape (NEVER In Progress — no regress), but is a recognised rework sub-state. `Done` is **gated on zero open CRs** (a Done box with open CRs does not derive Done). When the last CR closes, it returns to clean QA Review (or advances to Done).
- **make-current ACCEPTS THE BAND:** extend the make-current status policy (task-policy.ts:76) to allow `Planned` + `In Progress` + **QA-Review-with-open-CR** — so a task in rework (open CRs) IS settable-as-current, while a CLEAN QA-Review still 409s (unchanged). This is how the tap works WITHOUT regressing the display to In Progress. **HARD DEPENDENCY: CR-2 (the band/policy) depends on CR-5 (the sub-step-aware `deriveStatusEnum`)** — build CR-5 first, or CR-2 has nothing to key on.
- **★ SINGLE-SOURCE preserved:** the band is derived from the CHECKLIST (top-level box + the present-iff-open-CR sub-step), not from a status-override reading external CR-count — the checklist stays the single source (task-status.ts:2). The sub-step is now MEANINGFUL to derivation (that is the change), not ignored.
- **THE TRAP (still applies):** do NOT ship the sub-step as pure decoration while `deriveStatusEnum` keeps ignoring it — that is a line everyone sees and behaviour that never fires. The behaviour is the band + the policy accepting it. stub-must-fail: a sub-step-only build leaves make-current 409-ing a task with open CRs (RED).

## ★ BAND-MECHANICS CONFIRM + CHOKEPOINT CLEAR (architect, on req's CR units 00fea8fa1)
**CONFIRMED sound**, with 4 implementation-critical conditions + 1 reference fix:
1. **Extend THE one `deriveStatusEnum`** (task-status.ts:19), never a parallel derivation. `statusSymbol` (:42) AND the drift detector `assertStatusConsistent` (:82) both delegate to it — so the band + the Done-gate stay consistent with the detector BY CONSTRUCTION (no false FALSE-DONE). This is the anti-two-source discipline (PO hard-condition 2026-08-12); CR-5 owns this and CR-2 depends on it (correct hard order).
2. **The band stays the QA-Review ENUM.** deriveStatusEnum returns `QA Review` for a QA-Review-with-open-CR task (not a 5th enum value — that would ripple through every 4-value switch). Its only CHANGE: **gate Done** — a checked `Done` box WITH the open-CR sub-step present derives `QA Review`, not Done. The band is "QA Review enum + open-CR sub-step qualifier."
3. **make-current (CR-3, task-policy.ts:76) + valid-focus (CR-4) read the open-CR SUB-STEP from the checklist** to accept the band (status==QA Review AND processing-CR sub-step present → settable; status==QA Review AND no sub-step → 409 unchanged). Single-source: they read the checklist signal, NOT an external CR-count.
4. **Maintain the processing-CR sub-step present-iff-open-CR IN THE SAME `UnitController.apply` seam transaction** as the CR-link add/remove (task-policy.ts:116 note: `get()` re-reads disk, a separate push is lost). CR-open/close edits the sub-step + the changeRequests[] in one seam op.
- **★ CHOKEPOINT CLEARED:** #86 writes task units through the established `UnitController.apply`/TaskPolicy seam (same path reopen/sub-step/make-current use) — it USES `ScenarioIndex.put` as always, does NOT change the put machinery. No chokepoint-machinery modification; the R40.49 chokepoint flag was migration-auto-mint-specific and does not apply here. **Expert may build #86 without a chokepoint hold.**
- **⚠ REFERENCE FIX:** CR-5 currently references my `ac4913493` — that is the **SUPERSEDED** "untick→In-Progress" spec. Re-point it to **`c6652f88c`** (this file, the band ruling), else the AC cites the wrong behaviour.

## ★ STATUS-CORE SAFETY GATE (CR-5 is high-blast-radius — deriveStatusEnum is guarded by the FALSE-DONE/consistency detector + MvcBoundaryGuard)
Because CR-5 changes THE derivation core every task passes through, the change must preserve the by-construction invariant `model.status == deriveStatusEnum(model.statusChecklist)` for EVERY existing task — else the FALSE-DONE/DRIFT detector (`assertStatusConsistent`, which calls the SAME deriveStatusEnum) fires across the board.
- **The Done-gate interaction:** after CR-5, a `Done`-box-checked task WITH an open-CR sub-step derives `QA Review` (gated). So a task currently `status:Done` that gains an open CR MUST have `model.status` re-derived+stored to `QA Review` **in the same seam** as the sub-step write — otherwise `status:Done` vs `derived:QA Review` = a false DRIFT/FALSE-DONE flag. Condition (d) covers the write; make it explicit that CR-open/close **re-derives and stores `model.status` via the extended deriveStatusEnum** in that seam txn.
- **REGRESSION GATE (mandatory, run BEFORE ship):** after the CR-5 change, run the existing status-consistency detector across ALL tasks (`assertStatusConsistent` / its `--bite` test) and require **0 NEW offenders** vs the pre-change baseline (delta, not absolute — there may be pre-existing legacy offenders; the change must add none). This proves the core change didn't silently drift the whole board. stub-must-fail: a task seeded with Done+open-CR whose status is NOT re-derived → the detector flags it RED (proves the gate catches the exact failure mode).
- The band derivation is still PURE-from-checklist (top-level box + present-iff-open-CR sub-step), so the detector stays the single oracle. Do NOT read external CR-count in deriveStatusEnum.
**Checkpoint discipline ENDORSED:** do not build this status-core change blind with the context-guard vacant — /context first (or checkpoint-rewind, code is committed/zero-loss), THEN build CR-5. Protecting the status-core over speed is correct.

## What NOT to tell Tron
Do **not** say "the processing-CR mechanic will auto-unblock his tap" — it will not, as built. Either reopen 40.1 (option 1, works today) or schedule #86-2 to make open-CRs untick the top-level box (option 2, a build). The gap is real and needs the call made, not assumed away.

## §BACKSTOP — removal of the make-current status-gate throw (2026-08-20)
Expert removes ONLY the `intent.makeCurrent` throw (task-policy.ts:76). Measured:
- **(i) No caller hard-depends on the removed guarantee.** The endpoint (server.ts:1873) catches the throw → 409; removing it → 200. Nothing downstream assumes "current task is always Planned/In-Progress" as a correctness invariant. ✓
- **(ii) The seam still routes every status write.** `apply()` still writes `model.status = deriveStatusEnum(checklist)` — the SOLE 4-state writer (MvcBoundaryGuard-enforced). Removing the `validate()` throw does not bypass `apply()`. The reopen / subStep / evidence-gate / Done-requires-approvedBy validations all STAY (genuine invariants). ✓
- **★ (iii) FLAG — removal is NECESSARY but may be INSUFFICIENT (silent no-op risk).** `current` is DERIVED as the max-`lastAdvancedAt` **In-Progress** task (CurrentSprint.ts:253, In-Progress-ONLY filter). `apply()` ticks In-Progress only for a **Planned** task; a QA-Review/Done task made-current just gets `lastAdvancedAt` stamped **without a status change** → the In-Progress-only pin **excludes it** → make-current on a QA-Review task = **200 but nothing becomes current** (a silent no-op, WORSE than the honest 409). **FIX:** make-current on a non-Planned task must set the **designation override** (`currentTaskUuid`, EXPLICIT-WINS-WHILE-VALID, R40.18) so it becomes current regardless of derived status — OR the pin must rank the make-current'd task. Verify Tron's @390 tap actually makes his QA-Review task current, not just returns 200.

## §#86 RE-SCOPE (honest, post-reversal)
The justification "the band is needed so a QA-Review task can be current" is DEAD (the block is gone). Re-scoped:
- **DROP:** "extend make-current policy (task-policy.ts:76) to ACCEPT the band" — it existed only to satisfy the invented policy. Gone with the throw.
- **KEEP — CR-5 (derived-status getter / single-status-writer, sub-step-aware `deriveStatusEnum`):** STANDS ON ITS OWN. It is the by-construction north-star (out-of-seam writes can't compile; MvcBoundaryGuard); never about the policy. The 4 status-core safety conditions + the regression-delta gate still apply.
- **KEEP — CR-2 (processing-CR sub-step), CR-3 (demote-previous-to-next), CR-4 (auto-advance):** these are TRON'S OWN drawn checklist design, not our workaround — each keeps its own merit.
- **Done-gate (Done gated by zero-open-CR):** re-cast as **display-correctness** (a task with open rework should not read Done — Tron's sketch), NOT a make-current enabler. Keep, but on that basis.
- Net: #86 loses the policy-coupling, keeps the north-star (CR-5) + Tron's checklist mechanics. The make-current 409 is simply removed.

## §R40.44 REVERSAL — RATIFIED (architect owns it, on expert's measured fork 2026-08-20)
The expert correctly found that making a QA-Review task current (without regressing it to In-Progress) REQUIRES honoring `currentTaskUuid` in `getThreeSlots` — the pin R40.44 retired as "the lying pin." I OWN this reversal, and it is SAFE, because R40.44/R40.17 retired the pin for a SPECIFIC failure the override does not share:
- **Why it was retired (R40.17):** a STORED pin that AUTO-persisted and went STALE-SILENTLY — it pointed at a Planned task and "stuck" while work moved on, with no expiry and no observability. It LIED because nothing kept it truthful.
- **Why the override is NOT that pin:** it is **EXPLICIT** (set only by an owner make-current/designate, never auto-derived) + **EXPIRING** (VALID only until Done / re-designation / the task ceasing to exist) + **OBSERVABLE on expiry** (BITE-6b fail-loud — expiry is visible, never a silent stale pointer). The three properties the lying pin LACKED are exactly what make this truthful. **The reversal RESOLVES R40.17's failure (add expiry+observability), it does not resurrect it.**

### RATIFICATION (expert's 4 questions)
- **(a) Honor `currentTaskUuid` in getThreeSlots = SANCTIONED EXPLICIT-WINS-WHILE-VALID, YES — NOT the retired lying pin** — CONDITIONAL on (b)'s expiry+observability being built. Without observable expiry it WOULD regress to the lying pin; with it, it's the R40.18 EXPLICIT-WINS-WHILE-VALID design realised.
- **(b) VALID = designated task exists AND status ∈ {Planned, In Progress, QA Review} (reviewing IS working — Tron) AND not re-designated. EXPIRES at: Done, re-designation, or task-gone — and the expiry MUST be OBSERVABLE (BITE-6b), never silent.** This observability is LOAD-BEARING: it is the entire difference from the lying pin. A designation that could point at a Done/deleted task without visibly expiring = the lying pin back.
- **(c) RECOMMEND: make-current ALWAYS designates (`currentTaskUuid`) = ONE current-truth mechanism (EXPLICIT-WINS); a Planned task ALSO advances to In-Progress as a start-work side-effect.** Cleaner than the Planned→advance-only / non-Planned→designate split (which leaves TWO current mechanisms — derived-max-lastAdvancedAt vs designation — coexisting). Designation is primary; `max-lastAdvancedAt In-Progress` becomes the FALLBACK when no valid designation. (The expert's split is acceptable for minimal-diff, but always-designate is the more consistent single-source — my recommendation.)
- **(d) YES — update the make-current comment (server.ts:1868) + the R40.44 doc** to state: R40.44 retired the STALE-AUTO-SILENT pin; the EXPLICIT-WINS-WHILE-VALID owner designation (expiring + observable) is the sanctioned mechanism, re-enabled here — cite this ratification.

### GATE (acceptance)
Pin MOVES @390 on a QA-Review task (Tron's tap makes his QA-Review task current, it stays QA-Review, no regress) AND the designation EXPIRES OBSERVABLY at Done/re-designation (BITE-6b RED if a designation survives past Done silently). The second half is what keeps this from being the lying pin — gate BOTH, not just "pin moves."
