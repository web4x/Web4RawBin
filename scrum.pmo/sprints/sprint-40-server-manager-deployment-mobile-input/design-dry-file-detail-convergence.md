# DRY file/webitem detail — 3-path convergence onto RbDetailBase (architect ratify, 2026-08-30)

Expert measured the DRY-fork (BUG18 item 3) as THREE parallel File/WebItem detail-render paths and held building for my shape ruling (correct — don't build an unratified shape). I verified all claims on disk.

## Measured (confirmed)
1. **RoomView.openFilePreview** (RoomView.ts:370-405): hand-paints the preview panel + SILENT-fails (`if(!resp.ok)return` / `catch{}`) → bypasses the r4011 fail-LOUD in-room. [the PO-flagged fork]
2. **RbFileDetail** (rb-file-detail.ts:15 `extends HTMLElement`) + **RbWebItemDetail** (rb-webitem-detail.ts:34 `extends HTMLElement`): each has its OWN funnel (connected/attrChanged, :20-22), OWN `fetch('/api/ior/ior:instance:${uuid}')` (:34), OWN thin fail ("File not found" :29 / "Failed to load file" :76 in `.catch`). They **VIOLATE the RbDetailBase hazard-gate contract** (rb-detail-base.ts:12-13: "ZERO detail elements implement their own funnel or not-found/thin path outside THIS primitive"). **Never migrated.** In the drawer tagMap (drawer :31-32) → **used by the TRACE-PAGE drawer too = cross-page blast radius.**
3. **RbDetailBase + the 8 elements that DO extend it** — the intended ONE primitive. Convergence machinery already present: `renderDetailForRef` (drawer :249) + tagMap route `file:`/`webitem:` typed refs through the funnel.

## RATIFY — Phase A + Phase B, sequenced A→B (not one version), caller-fix ships NOW independently
- **The Tron-urgent caller-fix (RoomView ref-by-real-uuid, BUG18) ships NOW** with the bundled deploy — it alone fixes the name=uuid symptom (resolves to the real unit) and does NOT require this convergence. This convergence is the deeper hardening.
- **PHASE A — RATIFIED (gate-close, dictated by the primitive contract):** migrate RbFileDetail + RbWebItemDetail to `extends RbDetailBase` — move type-specific DOM into `renderDetail(ctx)`, DELETE their private fetch/funnel/fail (the base gives ONE model-source + fail-LOUD `renderUnresolved` + ViewBus). Own version + own gate — it closes the CROSS-PAGE (trace-page) blast radius, so gate it independently before layering the room. Anti-CMM2: no 4th path.
- **PHASE B — RATIFIED, AFTER A (converge the room):** RoomView.openFilePreview STOPS hand-painting; delegates a typed `file:`/`webitem:` ref to the drawer → renderDetailForRef → funnel → the room inherits fail-LOUD for free (kills the silent-fail fork). Depends on A (the element must be a converged primitive first).
- **Sequencing: A first (own gate), B after; caller-fix now.** Not A+B in one version — coupling the trace-page (A) and room (B) blast radii in one gate is exactly the multi-blast-radius risk we avoid; and A is independently valuable.

## THE MODE FORK — RULED: presentation != function (keep the preview surface, host the primitive in it)
The room uses `setMode('preview')`/previewPanel + RbPanZoom media pan-zoom + chat-drawer coexistence; renderDetailForRef uses `setMode('detail')`/detailPanel. **RULING: (A) KEEP the room's preview surface, host the RbDetailBase element INSIDE it — mode is a POSITION, not a second FUNCTION.** This is the presentation!=function invariant (R31.4 DRY-drawer / CONCEPT-scrollable-viewport, drawer=Details-compartment position-only). Rationale: the preview surface + pan-zoom + chat-coexistence are legitimate PRESENTATION for a room media-tap; switching room taps to detail-mode (option B) would lose them or force re-implementation = drift. So:
- The FUNCTION (ONE model-source + renderDetail + fail-LOUD) comes from the primitive regardless of which panel hosts it.
- **Media pan-zoom becomes a `renderDetail` concern:** RbFileDetail.renderDetail(ctx) media-auto-renders an image with RbPanZoom (composes with the Phase-A sub-question below). Then the room's previewPanel simply HOSTS that element; the drawer's `setMode` stays a CSS/layout branch on ONE element, never a render fork.
- Net: one element renders file-detail + media + pan-zoom + fail-loud, hosted in detailPanel (trace-page) OR previewPanel (room) by mode. Convergence achieved without losing the room's affordances.

## PHASE-A SUB — RATIFIED with a boundary: renderDetail may supplemental-fetch
YES — `renderDetail(ctx)` may do its OWN supplemental fetch (`fetchDetailData` for children/source-links) + media auto-render, while the base owns model + fail-loud (the 8 existing subclasses already do this = the established pattern). **BOUNDARY (the invariant that keeps it ONE funnel):** the base owns (a) the ONE primary model-source resolution and (b) the fail-LOUD `renderUnresolved` on an unresolved ref. The subclass's supplemental fetch is for SECONDARY data AFTER the model resolves — a supplemental-fetch failure DEGRADES THAT SECTION (a visible "couldn't load children" inline), never a silent whole-element fail and never a SECOND not-found/thin path. No subclass re-implements the primary resolution or a second funnel.

## Hazard-gate honesty — the gate is BLIND BY INHERITANCE (PO mechanism, a text-not-structure sibling)
The hazard-gate asserts "0 own-funnel outside the primitive," yet these 2 violators EXIST while the gate is GREEN. **PO named the exact mechanism: the gate is BASE-CLASS-ENFORCED — it scans RbDetailBase SUBCLASSES for own-funnels; a component that `extends HTMLElement` (does NOT inherit RbDetailBase) is INVISIBLE to it. "The guard cannot see what does not inherit from it."** So RbFileDetail/RbWebItemDetail evade it entirely — the gate is green because it never looked at them.
- **This is a STRUCTURAL SIBLING of the name-keyed lint (text-not-structure family):** the gate keyed its SCOPE to the ACTORS' membership (inheritance) instead of the HAZARD's presence — evadable by not-inheriting, exactly as the name-keyed lint was evadable by renaming. Same root: [[scan-the-hazard-not-the-actors]]. New evasion vector (inheritance-scope), same cure.
- **PREDICATE — REVISED (expert measured my fetch-shape predicate over-fires + I measured the "exempt" premise is wrong):**
  - Expert measured: all 7 non-base *-detail share own-funnel + `fetch(/api/ior)` + 0 `this.graph` → "own /api/ior fetch" does NOT separate escapee from artifact; a fetch-based `→RED` reds legit artifacts = right-requirement-wrong-rollout (a gate that cries wolf gets ignored = how the guard died the first time).
  - **★ I measured the "4 artifacts are exempt" PREMISE and it is WRONG for 2 of them:** `rb-feature-detail` (:44) AND `rb-modelelement-detail` (:19) BOTH `fetch('/api/ior/ior:instance:${uuid}')` and render that UNIT's detail — the SAME escapee shape as RbFileDetail, plus extra interactive behavior. They are **MIGRATION CANDIDATES, not exempt** (extend RbDetailBase → base owns model-source + fail-loud; keep the extra behavior in renderDetail via the ratified supplemental-fetch). Only **rb-terminal-detail** (primary source = a live `WebSocket /terminal` pty; its ior fetch is only a fixed keymap, not the rendered unit) and **rb-diagram-detail** (renders a diagram artifact) are GENUINELY different. So Phase A's migrate-set expands (File/WebItem **+ Feature + ModelElement**, pending per-element confirm they fit), and the exemption shrinks to ~2.
- **EXEMPTION MECHANISM — GATE-OWNED, never self-declared (PO ruling, banked law):** a self-declared `// @detail-artifact` marker is EVADABLE (the next escapee clears the red with one annotation, in good faith = rebuild the blindness). So: the exemption list lives IN THE GATE (adding one = TOUCHING THE GUARD = visible in review, countable, not an accidental red-silencing). Paired with: (a) **coverage-self-report** — print "N scanned / M primitive / K exempt (listed + STRUCTURAL reason)" every run (K can never grow silently); (b) **delta-gate** — K can only SHRINK (a new exemption requires an explicit reviewed ceiling-bump); (c) **stub-must-fail** — an unexempted non-base *-detail → RED. If a per-file locality marker is ever wanted, it must AGREE with the gate's list (marker AND list, else RED) — self-declaration alone is NEVER sufficient.
- **Predicate = inheritance-agnostic:** any `*-detail.ts` NOT extending RbDetailBase AND NOT in the gate-owned allow-list → RED. Each allow-list entry carries a STRUCTURAL reason (rb-terminal: primary-source = live ws pty; rb-diagram: renders a diagram artifact, not a unit-detail). The justification for an exemption = "primary source is NOT a scenario-unit-instance rendered as detail" — feature/modelelement FAIL that test (they render a unit-instance) → migrate, not exempt.
- This is a concrete specimen for the lint-over-lints / text-not-structure family: **a guard keyed to inheritance is a guard keyed to the actors** (specimen #5) — and its FIX must not reintroduce an actor-keyed dodge (self-declared marker), which is why the exemption is gate-owned + shrinking + self-reported.

## Handoff
req/planner mint+task Phase A (own version/gate) then Phase B; expert implements on the ruled shape (mode=A/presentation!=function; supplemental-fetch with the boundary); tester gates A (trace-page file/webitem detail via primitive + fail-loud) then B (room file-tap → primitive, fail-loud inherited, media pan-zoom preserved) @390. I re-inspect: 2 elements extend RbDetailBase (0 own funnel/fail), hazard-gate exemption removed, room delegates (no hand-paint), mode = one element two panels. Caller-fix (BUG18) is separate + ships now.
