# R40.81 migration — architect↔tester reconciliation of the overlap set (2026-09-06)

Two-measurements-conflict on the one operation that can lose Tron's data (PO). RECONCILED — the 82-unit discrepancy is **symlinks**, and both measurements now agree on the real collapse set = **33**.

## What each counted (the 82 = symlinks)
- **Architect (first pass):** total 784 = relocate 669 + "overlap 115". That 784 counted `.scenario.json` files **including 82 symlinks** in MODEL_STORE; the "115 overlap" = 33 real-in-both **+ 82 symlink-shadows** (a symlink trivially byte-equals its target, which inflated the overlap and hid that only 33 are real unit collapses).
- **Tester (gate 8d6b3dcb2):** 702 **real** unit files (symlinks excluded) = 669 data-only distinct + **33 real-in-both**.
- **Reconciled:** 784 − 82 symlinks = 702 real; "115 overlap" − 82 symlink-shadows = 33 real overlap. **669 agreed all along.** Architect re-measured EXCLUDING symlinks → reproduced the tester's **33 exactly**.

## Correct definition for the migration
**REAL unit files only; symlinks EXCLUDED.** A symlink is a link, not a duplicate unit to collapse — it is repointed, never a which-copy-wins decision. The tester's definition is the correct one. (Symlink counts: the store carries ~82 in MODEL_STORE; those relocate/repoint mechanically, no collapse.)

## The AGREED collapse set — 33, enumerated by uuid (not a count)
All 33 are `ior:class:ModelElement` — the **deterministic-uuid MOF/MDA meta-model elements** (R32.1 mda-identity / R32.2 ts-to-model), e.g. #1 "Class", #5 "UmlAttribute", #17 "ts-attribute-code", #33 "Measurable". They are REAL model units (not throwaway fixtures); they are byte-identical across both stores because the MDA generator emits them deterministically (fixed uuid + content) into each.

`a1d2e3f4-0000-4a1b-8c2d-000000000001` … `a1d2e3f4-0000-4a1b-8c2d-000000000033` (sequential, 33 units).

**Per-uuid byte-identity: 33/33 IDENTICAL, 0 DIVERGENT** (architect re-measure + tester gate 8d6b3dcb2 agree).

## Backstop conditions on the apply (enforced, PO)
1. **Per-uuid, not aggregate:** all 33 proven byte-identical individually (33/33 named above), not a rolled-up `divergent=0`.
2. **FAIL-CLOSED on ANY divergence:** a divergent pair STOPS the migration — never silently resolved by picking a winner. `Unit.resolve` may define the rule, but a divergence halts and escalates.
3. **relocate-verify-THEN-remove:** Slice-1 copies the 669 + collapses the 33 to one copy in `scenario/index`; MODEL_STORE stays **physically intact**. Nothing is removed until Slice-3, after readers repoint AND every unit is proven present + byte-identical in the one store.
4. **Snapshot survives** until the tester's behaviour-pair (the 212-ref read-path baseline) is GREEN post-migration — reversibility floor held through the whole arc (`test/baseline/model-store-premigration-v0.8.186.tar.gz`, 784 files == live).
5. **Post-apply INV-DATA re-verify** (architect): all 784 uuids resolve in `scenario/index` + the 669 byte-identical to origin + MODEL_STORE untouched, BEFORE Slice-2.

## Verdict
Numbers agree (669 relocate + 33 collapse, symlinks repoint). Collapse set is 33/33 byte-identical → collapse is loss-free. Expert cleared to apply the RELOCATE + collapse under the 5 conditions; fail-closed on any per-uuid divergence.

## ★ UUID-FOR-UUID MATCH CONFIRMED (2026-09-06) — ONE agreed in-scope set
Diffed my real-in-both set vs the tester's SET B (tester canonical: scrum.pmo/R40.81-reconciliation.md, commit 6b80a0c8f). **EXACT MATCH: 33/33, symmetric-difference = 0** (in-mine-not-tester = NONE; in-tester-not-mine = NONE). Tester's A/B/C/D is the canonical breakdown: A=669 relocate (data-real/scenario-absent) · B=33 collapse (data-real+scenario-real, 0 divergent) · C=82 symlink-skip (= the 82-gap) · D=0. Zero-data-loss = 0 divergent on B (33/33 byte-identical, both measures) + D=0. **Set B is the ONLY at-risk set; fail-closed on any B divergence.** Canonical doc = the tester's (top-level, enumerated); this file cross-refs it. Expert cleared to apply under the 5 conditions above.
