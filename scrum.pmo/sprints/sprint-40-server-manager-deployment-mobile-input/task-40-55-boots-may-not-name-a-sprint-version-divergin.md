<!-- GENERATED FROM SCENARIO UNITS — DO NOT HAND-EDIT -->

[Back to Planning](./planning.md)

# Task 40.55: Boots may not name a sprint/version divergin

[task:uuid:71334df3-c10d-4219-83f2-49460411ac4b]

## Status
- [x] Planned
- [x] In Progress
  - [x] refinement
  - [x] creating test cases
  - [x] implementing
  - [x] testing
- [ ] QA Review
  - [ ] processing change requests
- [ ] Done

## Task Description

Deliver + verify requirement R40.55 (Boots may not name a sprint/version divergin). Retroactive task unit minted scenario-first from the existing requirement (#126 gap: the req existed, its Task unit was never minted). Covers R40.55; status derived from reality, staged by planner.

## Acceptance Criteria

- [ ] **(layer1/by-construction)** For every boot.md that NAMES a sprint/version in an ACTIVE-STATE position, boot.named(sprint,version) == gitHEAD.derived(sprint,version), else RED. gitHEAD.derived = package.json version + the CurrentSprint pin's sprint (single-sourced, the SAME source the app reads). An equality invariant like served==committed.
- [ ] **(layer1/single-source-principle)** The currency guard discovers its set by the derived-GLOB PRINCIPLE with a divergence check (the discovered set is computed, never a hand-list; a boot present on disk but unevaluated => RED) — the SAME principle as R40.54 AcGuard and R37.12 live-MVC. HONEST STATE (verified on disk check-boot-currency.ts:18-20 + grep: no shared symbol exists): there is NO literal shared `structuralDiscover` symbol today; R40.54, R37.12 and this lint each implement the principle LOCALLY = 3 copies. The AC requires the PRINCIPLE + divergence (met); the single-SYMBOL extraction that would collapse the 3 copies is recorded as explicit DEBT (see extractionDebt) — NOT papered over, because a satisfied AC claiming 'shared' over 3 copies would be a false-satisfaction breaking our own meta-guard R40.54.
- [ ] **(layer1/enumerate-not-universal)** Discovery is BY GLOB of every session/agents/*/boot.md (INCLUDING other teams: oosh*, @host variants), NEVER a hand-list. Divergence check: a boot file present on disk but NOT in the lint's evaluated set => RED, so a NEW or RENAMED agent boot cannot hide by being unlisted. (This already earned its keep: the architect's glob caught 3 stale oosh* boots that a 7-name robbin list MISSED.)
- [ ] **(layer1/fail-closed-symmetric)** SYMMETRIC fail-closed, BOTH sides of the comparison. (a) COMPARED VALUE: a boot naming a version/sprint the parser CANNOT resolve to a comparable value => RED, never skipped-as-pass (an ambiguous boot is STALE until proven current). (b) TRUTH-SOURCE: if the guard's OWN source is unreadable/absent — package.json version OR the CurrentSprint pin cannot be read — the guard goes RED/error, NEVER pass-green. A guard that silently passes when it cannot read HEAD is exactly the silent-failure class this requirement kills (fail-LOUD, not fail-safe). Same fail-closed posture as the R3 uuid resolver, applied to the compared value AND the comparator's source. (Architect re-inspection hardening 2026-08-20: original AC covered only the boot-parse side.)
- [ ] **(layer1/enumerated-exemption)** A version inside an explicitly-marked lessons heading (e.g. '## Hard-won patterns (v0.6.0 ...)') is PROVENANCE, not active-state, and is EXEMPT — but ONLY under a marked lessons section, NEVER in a ## Current / ## Goal / anchor position. The exemption is POSITIONAL and ENUMERATED (a named lessons-heading scope), NOT a blanket 'ignore all versions' and NOT a self-declaring escape hatch (PO ruled positional (b) over content-sniffing (a)). A version OUTSIDE a lessons heading correctly BITES — that is a true positive, not something this exemption is meant to catch.
- [ ] **(layer1/self-failability)** The currency lint ships with a RED-proving fixture: a seeded boot naming a deliberately-old version MUST make the lint RED. If the stub PASSES, the lint is not wired and CI blocks. Isolated: fixture in scratch, no prod mutation, cleanup-on-failure (R40.31). This is the R40.54 own-failability requirement applied to this lint itself.
- [ ] **(layer2/cure-target-scoped-owned)** The REAL target (deeper assertion) is that a boot carries NO active-state: timeless rules + identity + an ANCHOR POINTER to context.md (R113 shape). ★ WHAT IS STATE — the FACT-vs-FINDER test (architect D3): a line asserting a ROTTING FACT ABOUT THE WORLD => STRIP; a HOW-TO-FIND pointer/rule/identity => KEEP. Enumerated subtle classes the sprint/version check misses: transient-git-ref (stash@{0}), infra-path-or-hash (NODE18 vscode-server bin hash, port), pane->role roster — WITH a divergence/completeness check (a fact-shaped line in no enumerated class => triage-flag, not silent-pass; enumerate-not-universal). ★ WHOSE STATE COUNTS — honest scope (PO false-UNSATISFACTION ruling; same OWNED/EXCLUDED/UNCLASSIFIED shape as named-exclusion ed9eadecb), 3 clauses: (a) COVERAGE UNIVERSAL — the guard DISCOVERS + evaluates EVERY boot fleet-wide; (b) SATISFACTION OWNED — this AC's satisfaction is judged ONLY over boots WE own (robbin-*); a robbin boot carrying active-state => UNMET; (c) EXCLUSION EXPLICIT + TIME-BOXED — other-team boots (oosh-trio, another PO's files we may not edit) are NAMED-EXCLUDED + WARN-LOUD (never silently dropped) + NOT counted against our satisfaction, and the exclusion CLOSES on an oosh-PO-coordination clock (contextAnchorCurrencyDebt-style), NEVER a permanent silent carve-out; and an exclusion whose named boot no longer exists (a DEAD exemption) => RED, since a stale exemption is itself the rot it was meant to bound (aligns 1:1 with exclusion mechanism ed9eadecb: EXCLUDED carries reason + TIME-BOX + must resolve). This prevents FALSE-UNSATISFACTION (an AC permanently unmet through no fault of ours = the mirror of the false-satisfaction this family kills), while NOT entrenching state-bearing robbin boots as acceptable. ★ 3-BUCKET SEMANTICS (architect, matches shipped guard + design 6e626ed98 — post-satisfaction wording alignment to the VERIFIED behaviour, not a requirement change): OWNED = timeless, RED IFF state-bearing. EXCLUDED = named-or-predicate not-ours, WARN-loud, NEVER RED. UNCLASSIFIED = held to the OWNED standard (RED IFF state-bearing; a TIMELESS unclassified boot PASSES) + surfaced-for-triage. It is NOT unclassified=RED-by-default. ★ PO LEGITIMACY TEST (why this post-satisfaction reword is legitimate, NOT satisfy-first-then-reword-to-fit): (a) it aligns the AC DESCRIPTION to the VERIFIED shipped behaviour; (b) the BAR WAS NOT LOWERED — auto-RED-on-unclassified was NEVER the intent; (c) THE TEST: was the original wording the INTENDED BAR we then softened, or a WRONG DESCRIPTION of an intent that never changed? => the LATTER — the architect corrected its OWN design doc (6e626ed98, verified: 'RED iff state-bearing, not auto-RED') for the SAME error AFTER the PO ruled the implementation right + prose wrong; the shipped 3-bucket behaviour is what was built, verified + satisfied; evidence UNCHANGED. A description correction, not a bar relaxation.
- [ ] **(layer2/delta-vs-absolute)** The ABSOLUTE no-active-state assertion ships WARN now, and flips to RED per-file as each agent's cure-migration lands (the R40.54 delta-vs-absolute lesson: do NOT RED-gate absolute-conformance before the migration that achieves it, or it false-fails the very files being cured). Per-boot conformance flag: state-bearing vs timeless+pointer; the currency check applies to state-bearing; a timeless+pointer boot passes by construction (nothing to rot). Publish the count of not-yet-cured boots, never falsely-drained.
- [ ] **(layer2/lane-boundary)** Guard COVERAGE is fleet-wide: the discovered+evaluated set INCLUDES oosh* and other-team boots (a missing other-team boot => coverage RED). But REMEDIATION is NOT unilateral: rewriting another team's boot files is coordinated with THEIR PO. The guard may FLAG an other-team stale/state-bearing boot in its report, but an edit to an oosh-owned file requires oosh-PO coordination — coverage universal, remediation coordinated.

## Subtasks
