# Cross-view role AGREEMENT — architect design (2026-08-24, Tron rejected R40.56 at his surface)

Tron's screenshot (record aa3fa299): the pin reads "📌 Current — Task 40.1" while THAT task's drawer still offers "📌 Set as Current." Two views on ONE screen disagree about a derived role. Design-only, req mints. Independent of the tester's 3-cause discrimination — valuable whichever cause.

## ★ THE REFRAMING (PO, load-bearing): single-source ≠ correct-source — it means EVERY CONSUMER AGREES
R40.56 deleted the second *derivation* and proved the RESOLVER returns the designation. **True and insufficient.** A consumer can read the correct resolver and STILL render a contradiction if its own COPY of the result is stale. Our gate asserted the SOURCE; Tron's eye asserted the AGREEMENT — and the eye was the stronger test. New gate CLASS: **consumer-vs-consumer on the rendered artifact**, not consumer-vs-model. [[assert-the-rendered-artifact-not-a-proxy]]

## MEASURED root (first-hand) — two COPIES at different freshness
- **pin/tree** `rb-trace-tree.ts:107` subscribes `ViewBus.subscribe(viewBusKey({type:'CurrentSprint', uuid:singleton}), …)` → on a live make-current it RE-DERIVES. **Fresh.**
- **action-bar** `rb-detail-drawer.ts:498` reads `rModel.pinRole` — a field BAKED into the served task payload at fetch time (`attachTaskPinRole`, server.ts:2901, on the /api/ior GET). The drawer does NOT subscribe to the currentsprint broadcast, so a pin-move never refreshes its `pinRole`. **Stale.**
- ⇒ make-current on Task 40.1: tree badge updates live → "current"; the drawer's already-rendered `pinRole` still says "other" → still offers Set-as-Current. R40.56's source-fix can't help: both compute from `slots.current`, but the drawer computed ITS copy at a stale moment. **The disagreement is a lifecycle gap, not a derivation gap.**

## RULING — the by-construction fix: NO SECOND COPY + derive-at-render from ONE live event
Answering the PO's three options:
- **(iii) YES — primary: do NOT ship `pinRole` in the payload at all.** The disagreement is POSSIBLE only because the role is a separate copy with its own lifecycle. A derived ROLE need not be copied — it is a pure function of `(thisTaskUuid, currentSlotUuid)`, and the client already has both live. Remove `pinRole` from the served task model (retire `attachTaskPinRole` as a payload field). No copy ⇒ nothing to go stale ⇒ the class is eliminated, not mitigated.
- **(ii) YES — the mechanism: same broadcast, one event, atomic re-derive.** The drawer subscribes to the SAME `viewBusKey({type:'CurrentSprint', uuid:singleton})` the tree uses (ONE builder both sides — the R37.12 Q3 lesson; a raw-uuid subscribe is exactly why the tree once went stale). On that broadcast AND on render, the drawer derives `taskRole` by comparing its shown task's uuid to the CURRENT current-slot uuid, read from the ONE live current source both views share. Pin-move → one event → BOTH re-derive → **agreement is atomic by construction**: "derived-role and pin-slot change atomically for all consumers" IS the real invariant.
- **(i) fresh-wins on the baked `pinRole` — REJECTED as the wrong shape here.** Fresh-wins (R40-root-#2, status) is the mitigation for data you MUST copy (status CONTENT). A derived role need not be copied at all; keeping the copy + racing to refresh it re-opens the gap on any missed subscription (exactly today's bug). Eliminate the copy (iii), don't manage its freshness.

**Net shape:** the current-slot uuid is the ONE live client-side value (fed by the currentsprint broadcast, read via one accessor); every role-consumer — pin badge, tree highlight, action-bar verbs, scoreboard — derives its role from THAT at render time. No server-baked role field. Single live source + derive-at-render = every consumer agrees because they all read the same live value on the same event.

## ★ MISSING AC — CROSS-VIEW AGREEMENT (the gate Tron's eye is)
**On one rendered screen @390, for the SAME task, the pin/tree role and the action bar's offered verbs MUST agree: `current ⇒ Set-as-Current ABSENT; not-current ⇒ PRESENT`. Asserted by SCREENSHOT, the views checked against EACH OTHER — NOT each against the model.**
- **Why consumer-vs-consumer:** the prior gates (single definition-site, resolver-correct) assert derivation is single-sourced. They CANNOT catch a stale-copy disagreement — both consumers "read the right resolver." Only comparing the RENDERED views to each other catches what Tron saw. This is the distinct gate class the session was missing.
- **★ THE STATE UNDER TEST IS PART OF THE ASSERTION (PO, load-bearing — write into the AC, do NOT let it be simplified to initial-load):** the DYNAMIC-post-broadcast state is not a test-setup detail, it is part of WHAT is asserted. A cross-view agreement check run on INITIAL LOAD would PASS and miss the bug entirely — the assertion is correct but the STATE it is asserted in is the wrong one. This is a NEW kind of vacuous-pass (same family as gating a base-only enrichment sample, or a gate that reds for an incidental reason): correct assertion, wrong state. So the AC MUST specify the state: render → make-current on a DIFFERENT task via the LIVE path (broadcast, NO drawer re-fetch) → THEN screenshot; agreement must hold ACROSS the live event. An initial-fetch-only variant is a vacuous simplification and is itself a gate defect. The stub-must-fail must exercise THIS state (a disagreement injected post-broadcast), not initial load.
- **stub-must-fail:** inject a disagreement — a screen where the tree shows `current` but the drawer offers Set-as-Current (i.e. re-bake a stale `pinRole`, or suppress the drawer's broadcast subscription) → the gate MUST RED. A gate that can't be made to fail by injecting a disagreement is not asserting agreement. (This IS the R40.56-defect specimen at the RENDER layer — the strongest baseline.)
- **enumerate-not-universal + fail-closed:** enumerate the role-consumers on a screen (pin badge · tree highlight · action-bar verbs · scoreboard). A NEW role-consumer must be added to the agreement set or the gate flags it (a consumer outside the cross-check can silently disagree — the divergence check). If any enumerated consumer's role can't be read for the comparison, RED — never skip-as-pass.

## Handoff / chain
req mints the Requirement + ACs (cross-view-agreement carries its screenshot gateRef + the dynamic-post-broadcast stub-must-fail). Ties to R40.56 (same current-role subsystem): R40.56 = single derivation-site (source); THIS = single live value + derive-at-render (agreement). Both needed; agreement is the stronger, Tron-facing one. expert builds post-GO (budget/build rules apply); tester gates @390 cross-view screenshot AFTER a live make-current; I backstop the stub reds on an injected disagreement + that no server-baked role field survives (grep: 0 `pinRole` in served payload).

## ★★ RECONCILED ROOT — MEASURED, uuid-form hypothesis KILLED (2026-08-24, architect ran the resolver)
PO/expert hypothesis = uuid FORM mismatch (@host / prefix). **I ran the actual comparison (probe replicating currentTaskUuidFromSlots against the live index) — the hypothesis is KILLED; the real root is different and decisive:**
```
DESIGNATION   = "7a956c21-5f37-4062-b921-9bdd5a461546"   (bare)
iorUuid       = "7a956c21-5f37-4062-b921-9bdd5a461546"   (bare, len 36)
currentUuid   = ""                                        (EMPTY, len 0)   ← the bug
slots.current = { taskUuid: "7a956c21…", taskName:…, reqUuid:… }
RAW===        = false  =>  pinRole OTHER
```
Both ids are BARE full uuids — NO form mismatch. `currentTaskUuidFromSlots` returns **`''`** because it reads `slots.current.uuid`, but `ThreeSlots.current` is a `TaskSlot = { taskUuid, taskName, reqUuid }` (CurrentSprint.ts:331 `toSlot`) — the field is **`taskUuid`, not `uuid`**. `.uuid` is `undefined` → `String(undefined||'')=''` → the `currentUuid &&` guard short-circuits → **every task 'other', 2/2, both sprints.** Exactly the tester's data (slots.current==self TRUE — it IS the designated task; pinRole OTHER — the reader took the wrong field).

**Why TypeScript didn't catch it:** server.ts:1402 casts the result `as { current?: { uuid?: string } }` — a FALSE structural cast that told the compiler `.uuid` exists. The real `ThreeSlots` type would have ERRORED on `.uuid`. **An unsafe `as` cast defeated the type checker** = the actual hazard.

## ★ THE PO's PRINCIPLE HOLDS — only the MECHANISM was mis-identified
PO ruled "retire beats repair; one definition of a shared truth" (for uuid normalization). That principle is CORRECT and TRANSFERS — the shared truth with two definitions here is the **SLOT SHAPE**: `getThreeSlots` defines it (`{taskUuid}`); the consumer re-declared it via a false cast (`{uuid}`). The `as` cast IS the second definition. So the cure is the same shape as the PO's: **consume the ONE real exported type, retire the ad-hoc re-declaration (the cast), don't patch the field name blind.**

## RECONCILED ONE-SPEC — three layered defects, ONE build, ONE deploy
- **D1 — THE LIVE BUG (measured): false-cast slot-shape read.** FIX: DELETE the `as { current?: { uuid?: string } }` cast in `currentTaskUuidFromSlots`; consume the real `ThreeSlots` type so TS enforces the shape; read `slots.current.taskUuid`. GATE (gate-first on the live specimen): a designated task's served `pinRole` must be `current` — TODAY it is `other`/`''` (RED baseline on the live tree, captured before the fix). Guard (hazard, by-construction): **0 ad-hoc `as {…}` structural casts on a `slotsFrom`/`getThreeSlots` result** — a resolver result must be consumed at its exported type, never re-shaped by a cast. Stub-must-fail: seed a false-shape cast → RED. (Same one-number single-source shape as the other guards.)
- **D2 — uuid-form normalization asymmetry (LATENT prevention, NOT the live bug).** The expert's @host / 2894-retains-@host vs 272-strips-@host gap is a REAL latent bug (a federated `uuid@originHost` id would fail equality) but MEASURED NOT the current failure (all ids bare). Adopt the PO's cure as PREVENTION: ONE canonical `bareUuid(s)` (strip `ior:(instance|class|file):` prefix AND `@host`), every producer calls it (2894, 272, others), comparison via one identity helper, raw `===` between two producers' uuids forbidden; hazard guard = 0 ad-hoc prefix-replace / `@`-split outside `bareUuid`. RULED SECONDARY — do NOT credit it as the live-screen fix; it prevents the class, D1 fixes the screen.
- **D3 — lifecycle staleness (R40.57), independent.** Masked TODAY (pinRole always 'other' via D1); becomes the visible bug once D1 lands (pinRole correct at fetch, then goes stale on pin-move). R40.57 retire-copy + subscribe + derive-at-render STANDS. Its derive-at-render comparison must use `bareUuid` (D2) so the client doesn't re-introduce a form gap. GATE = cross-view-agreement @390 dynamic-post-broadcast (above).

**Order within the one build/deploy:** D1 (real type + `.taskUuid`) → pinRole correct at fetch → D3 (retire baked copy + subscribe + derive-at-render) → correct across pin-move → D2 (canonical `bareUuid`) → identity class prevented. All three gate-first where a live specimen exists (D1: today's 'other'; D3: cross-view stub post-broadcast). ONE deploy — no intermediate ship.

## ★ D1 GUARD — GENERALIZED (PO, 2026-08-24): the hazard is a cast that DISABLES the detector
The deepest finding: an unsafe `as`-cast is not just a second definition of a shared truth — it also DISABLES THE ONE TOOL THAT WOULD HAVE CAUGHT IT. The real `ThreeSlots` type would have ERRORED on `.uuid` at compile time; the cast told the type checker to be quiet. That is worse than a missing guard — it is a guard we actively defeated, which is why this shipped through the whole gate corpus. So the D1 guard generalizes beyond `slotsFrom`/`getThreeSlots`: **0 `as {…}` structural casts that RE-DECLARE the shape of a value returned by any of OUR OWN exported, typed functions.** Consuming such a result at anything other than its exported type is the hazard (scan it, count zero = single-source proven in one number). Narrow allowance only for genuinely-untyped boundaries (JSON.parse, external I/O), never for a value our own typed code already returns. Stub-must-fail: seed an `as`-re-decl of a typed return → RED.

## ★ D1 GUARD SCOPE — RULING (A) SHAPE-ONLY (2026-08-24, expert stopped-and-reported broad-red)
Expert's guard red 17 on the tree; only 2 are the specified hazard. RULE **(A): flag `) as {…}` — an inline OBJECT-SHAPE re-declaration applied to a call — NOT `) as any` (widening).** Reasoning: the R40.56 mechanism is RE-DECLARING a shape you then read typed fields off (`.uuid` on a `{current:{uuid}}` relabel of a `{taskUuid}` return). **FORM is the discriminator, unevadably:** a shape re-declaration is an inline `{…}` literal; DOM narrowing (`getElementById(...) as HTMLInputElement`) casts a typed return to a SUBTYPE via a NAMED element type — legitimate and a different operation. So (A) excludes the 14 DOM/loose casts BY FORM, not by an allowlist → no hand-list to rot. Reds exactly the 2 genuine re-declarations: server.ts:1402 (the bug) + :1472 (`get(...) as {model?…}`).
- **REJECT (B) positive-callee-list** — a hand-list of our-typed fns rots (scan-the-ACTORS; the exact narrowing we just removed).
- **REJECT (C) broaden the untyped-boundary allowlist** — a DOM/loose allowlist also rots (new loose type / new DOM API = false pos), and it keeps `as any` in scope dragging in the 14. (A) needs no allowlist at all.
- **FIX now regardless of the guard:** 1402 → consume real `ThreeSlots`, read `.taskUuid`; 1472 → consume `get`'s real return type; 2737 → remove the needless `slotsFrom(...) as any`. After the fixes the tree has zero of both forms.
- ★ **NAMED RESIDUAL (bound made visible, not silent — enumerate-not-universal):** (A) does NOT catch `slotsFrom(...) as any` + `.uuid` (widening-then-wrong-field) nor `as <NamedWrongType>` (both defeat TS the same way — L-S40i's "any / cast" family). These need a TYPE-AWARE lint (TS compiler API: cast target incompatible with the callee's declared return) — the only fully-general form, heavier. Recorded as a follow-on debt; until then (A) covers the dominant+realistic surface unevadably + the `as any`-on-our-fn instances are removed by hand now (2737). The guard LOGS that it bounds coverage to shape-re-declaration (silent truncation reads as full coverage — it isn't).

## ★★ D1 GUARD SCOPE — RULING SUPERSEDED: (D) STRUCTURAL-DISCOVER, not (A) (2026-08-24, PO caught A's hole; architect adopts)
I ruled (A) shape-only and NAMED an `as any`-on-our-fn residual (2737) to be hand-fixed. **PO caught it and is right: (A) has a HOLE — `slotsFrom(...) as any` then `.uuid` silences the field-check IDENTICALLY (the same bug in a different costume), and (A) would NOT flag 2737. A guard that needs a human to remember a known variant is not a guard = the actor-blocklist failure, the same shape as the scalar-return evasion I caught earlier — now in MY ruling. I own it.** Hand-fixing 2737 + naming a residual is mitigation, not by-construction.
**ADOPT (D):** STRUCTURALLY DISCOVER our own exported typed functions (the SAME `structuralDiscover` mechanism I designed for boot-essence / the R40.55 extraction debt — this is another consumer of the one discover), then flag **ANY cast — inline-shape re-declaration OR `as any` widening OR `as <NamedWrongType>` — applied to a CALL of one of them.** THE PROPERTY (cannot be sanded down): *you may not defeat the type checker on a value returned by one of our own typed functions — by re-declaring its shape OR widening it to any.* Mechanism, not syntax, is the hazard.
Resolves every case the expert listed BY CONSTRUCTION, no allowlist: 1402 slotsFrom-as-shape FLAGGED · 2737 slotsFrom-as-any FLAGGED (the case A missed) · 1472 ScenarioIndex.get-as-shape FLAGGED · 14 DOM getElementById/createElement/querySelector NOT (not ours — external, excluded structurally, no DOM allowlist) · unit.model as-any NOT (property access, not a call of our typed fn) · federation loose-model NOT (same). D also subsumes the (A) named-residual (`as any` / named-wrong-type) → no separate type-aware-lint follow-on needed for the CALL surface.
**Two implementation specifics I pressure-tested (guard-designer value-add):** (1) discovery MUST include exported CLASS METHODS with typed returns, not just free functions — the 1472 case is `ScenarioIndex.get(...)`, a method; (2) match call-sites against the discovered NAME+MODULE set (not name alone) so a same-name EXTERNAL fn can't false-positive; a call to an imported-from-our-module fn resolves cleanly. Scope stays CALLS-of-our-typed-fns (property-access widening like `unit.model as any` is a broader, separate smell — deliberately out, else it catches loose-model everywhere).
FIX unchanged: 1402 → real ThreeSlots + `.taskUuid`; 1472 → consume get's real type; 2737 → remove the `as any` (now ALSO guard-caught, not just hand-fixed). stub-must-fail: seed BOTH an as-shape AND an as-any on a discovered our-typed-fn call → both RED.

## ★ D3 WIRE — RULING (2): drawer current-slot SOURCE (2026-08-24, architect, measured)
**SOURCE = recompute-on-read (resolveSprintPin+slotsFrom), NEVER the stored `model.slots`.** ★ HAZARD I measured: the CS singleton's STORED `model.slots.current.taskUuid` = `4bc1b3d5` (Task C2) but the LIVE designation is `7a956c21` — the stored `model.slots` is a STALE persisted snapshot (R40.17 said none should persist; its presence is a latent inconsistency — see note). Reading `/api/ior/<CS>.model.slots` would be a THIRD stale copy. The tree's PROVEN-LIVE current comes from **`/api/trace/children/<CS>?mode=trace`** which RECOMPUTES via `slotsFrom` (server.ts:2737) → `.children` = the live slots. The drawer's current-slot MUST come from that recompute-on-read path, not the stored snapshot.
**NO SECOND COPY (consistent with the D3 retire-the-copy principle — two independent client fetches = the fresh-wins-per-consumer shape I REJECTED as option (i)):**
- PREFERRED: introduce ONE shared client-side current-slot value (a small module) fed ONCE per `viewBusKey({type:'CurrentSprint',uuid:singleton})` broadcast from the recompute source; BOTH tree and drawer READ it. One live value, no per-consumer copy.
- ACCEPTABLE FALLBACK (expert feasibility, if the shared module is too large for this one-deploy): drawer fetches the SAME recompute endpoint + subscribes the SAME viewBusKey (line 107) + compares via `bareUuid` (D2). CAVEAT (must be recorded): this is fresh-wins-per-consumer — a residual transient-divergence window remains during concurrent re-fetch; it is acceptable ONLY because the cross-view acceptance is checked POST-broadcast-SETTLE, and the shared-store becomes a tracked follow-on. Expert picks by feasibility; my preference is the shared value.
**Either way:** (a) identify the current slot EXPLICITLY by its role/position in the recompute output — do NOT guess position; (b) compare `bareUuid(thisTaskUuid) === bareUuid(currentSlotTaskUuid)` (D2); (c) NO as-cast on the result (guard D blocks it — consume the real type); (d) subscribe the SAME viewBusKey the tree uses (one-builder-both-sides, R37.12 Q3) so a pin-move re-derives.
**LATENT NOTE (not this deploy):** the persisted `model.slots` snapshot on the CS singleton contradicts R40.17 recompute-on-read and is stale — any consumer reading `/api/ior/<CS>.model.slots` gets a stale current. Flag as a separate cleanup (retire the persisted snapshot / recompute it on serve); do NOT rely on it. Recorded so it isn't silently trusted.

## ★ D3 SOURCE-OPTION = FALLBACK (drawer-only) + role:"current" CONFIRMED (2026-08-24, architect)
Expert chose FALLBACK (drawer-only, tree untouched) by feasibility — correct: extracting a shared store from the proven-live tree is un-verifiable @390 in this one-deploy = blast radius on the least-safe component. Shared-store = named follow-on (honest debt). Residual fresh-wins-per-consumer OK (acceptance post-broadcast-SETTLE).
**role:"current" on the recompute child — CONFIRMED (better than label-match):** matching the "📌 Current" DISPLAY label to derive a role = deriving from a fragile display PROXY (formatting/i18n drift) = assert-the-proxy anti-pattern. An explicit `role` field is STRUCTURAL. And it does NOT violate retire-the-copy: it is stamped on the LIVE RECOMPUTE output (slotsFrom, per-request, server.ts:2753), NOT a persisted/baked copy. TWO CONSTRAINTS:
1. `role` MUST be stamped from slotsFrom's OWN slot classification (which slot is `.current` vs `.lastCompleted` vs `.nextBacklog`) — i.e. it IS `slots.current` by construction, NOT a second derivation/comparison at 2753. The drawer reading `children.find(c=>c.role==='current')` then reads the ONE resolver's answer, not a re-derivation. (Single-source preserved: role is a label ON the resolver output, not a new source.)
2. HONEST ABSENCE: when there is no current (designation expired + no unambiguous derived current), there is NO role:"current" child → drawer `.find()` returns undefined → currentSlotUuid undefined → all tasks 'other' → Set-Current shows on all. Correct (absence over a guess, per D3/R40.57). Drawer must handle no-current-child as honest-absence, not crash.
Field is additive on /api/trace/children for CS (tree ignores it) = no blast radius. Backstop matches FALLBACK: (b) 0 pinRole in served payload; (d) cross-view CONSUMER frame @390 post-broadcast-SETTLE (pin + action-bar agree); shared-store follow-on = tracked debt, NOT a gate.

## ★★ PRE-DEPLOY BACKSTOP = CLEAR (2026-08-24, architect, verified BY MEASUREMENT not report)
D1 dd12e26fb + D3-server 181dbea40 + D3-drawer/D2 887891fe2. Verified:
- (b) 0 pinRole in served payload: `m.pinRole=` writes=0; attachTaskPinRole+currentTaskUuidFromSlots fully DELETED (1 comment ref only); drawer 0 pinRole reads. By construction — field gone, class cannot recur.
- (c) guard (D) selftest 9/9 GREEN — reds on `as {shape}` AND `as any` (the case A missed) AND `resolveSprintPin(...) as any`; allows exact-return-type/no-cast/DOM/property-access. stub-must-fail PROVEN both syntaxes.
- role:'current' stamped FROM slotsFrom's slot-classification (server.ts:2718-2731, `isCurrent = s.role==='current'`), NOT the display label, NOT stored model.slots. Constraint (1) honored.
- D2: src/ts/shared/bare-uuid.ts routed through drawer compare (rb-detail-drawer:101/:524) + CurrentSprint:272 designation. Single-source.
- ★ (a) CORE CORRECTNESS (ran the resolver): recompute role:current taskUuid = `7a956c21` = the LIVE designation (MATCH true); stale stored model.slots.current = `4bc1b3d5` is NOT the source. The drawer deriving from the recompute's role:current child gets the LIVE current → taskRole='current' for the designated task → Set-Current absent → agrees with the pin.
- @host: 0 remaining `iorUuid ===` (attachTaskPinRole was the only comparer, deleted) → the 2894 @host-retention is genuinely uncompared now; the "forbid raw=== across producers" lint = correctly a prevention FOLLOW-ON (raw=== count is already 0). honest-absence: drawer empty-current → all 'other'. ✓
**VERDICT: pre-deploy CLEAR — deploy authorized.** What remains is the TESTER's LIVE gate, by design (cannot be pre-deploy): (d) cross-view CONSUMER frame @390 POST-broadcast-SETTLE (pin badge + action-bar AGREE for the same task after a live make-current) — the acceptance, run on the deployed build + device. (a)'s RENDERED client role is part of that same live @390. FOLLOW-ONS (not blockers): shared-store (drawer-only fresh-wins residual), raw-===-across-producers lint, persisted-model.slots stale-snapshot cleanup.
