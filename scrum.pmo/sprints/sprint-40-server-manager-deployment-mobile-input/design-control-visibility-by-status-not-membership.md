# Action-bar control visibility must derive from STATUS, not eager-graph membership (ruling + fix)

robbin-architect 2026-08-18. Tester surfaced this unblocking landing-3; PO escalated (correctly rejected "product is correct, seed is wrong"). MEASURED myself before ruling (Tron-facing). **Design/ruling only.**

## Q1 — By design or a latent defect? → LATENT DEFECT
Status is a property of the TASK: `deriveStatusEnum(statusChecklist)`, computable on any task unit. But the action-bar (`rb-detail-drawer.ts:483`) reads a PRE-ATTACHED status field — `unit.status = obj?.status ?? rModel?.status` — and **absent status ⇒ approve/decline HIDE** (`:477`). That status is attached by the server-built EAGER current-sprint graph, but **`/api/ior` (server.ts:2848) does NOT attach it**: for a Task it runs `attachTaskChangeRequests` + `attachTaskPinRole` + `attachTaskMdHref` — there is NO `attachTaskStatus` — so it returns `model.status` as-stored (undefined for tasks whose status isn't persisted; measured a39efc32). ⇒ **control visibility depends on GRAPH MEMBERSHIP, not on the task's actual STATUS.** That is a bug, not a design.

## Q2 — Can Tron approve a QA-Review task NOT in the current-sprint eager set today? → NO (measured)
A QA-Review task reached OUTSIDE the eager window — a deep link, a previous-sprint task, anything after sprint rotation, or Tron opening a task by ref from /trace — resolves via `/api/ior` → no attached status → `unit.status` undefined → **Approve/Decline HIDE** even though it IS actionable and he IS the owner. Same class Tron escalated (controls must VANISH at Done — and by the same logic APPEAR when actionable), and **invisible to any gate that only exercises current-sprint tasks** (the T37.24 coverage-false-green pattern).

## Q3 — The fix: control visibility follows the task's ACTUAL status; absent-status must NOT mean 'not-actionable'
**Ruling (PO-confirmed):** control visibility MUST follow the task's actual derived status, NOT eager-graph presence. The detail pane already renders "QA Review" correctly for the same task → **the status source is REACHABLE on that page** — so this is a SINGLE-SOURCE consolidation: the action-bar must read status from the SAME source the detail pane uses (`deriveStatusEnum(statusChecklist)`), not a separate pre-attached field that some paths carry and others drop.
- **WHERE — `attachTaskStatus` at `/api/ior`** (mirroring the existing `attachTaskPinRole`/`attachTaskMdHref`/`attachTaskChangeRequests` compute-on-read enrichers): `m.status = deriveStatusEnum(String(m.statusChecklist ?? ''))` — compute-on-read, NEVER writes (INV-T). Every task then carries its ACTUAL derived status on EVERY resolution path. (Server-side chosen over client-derive for consistency with the attach* pattern + one place; both derive from the ONE source — `statusChecklist` via `deriveStatusEnum`, the status-getter north-star at the read boundary.)
- ★ **`absent-status ⇒ hide` (`:477`) is itself a defect — a SILENT failure (L15):** it makes "status missing" indistinguishable from "not actionable", so a data gap HIDES a real action. Fix by construction so **absent status can NEVER mean not-actionable**: (i) status is always derivable from `statusChecklist` (above) → never absent for a real task; (ii) a genuinely-unresolvable status (malformed unit, no `statusChecklist`) renders a LOUD unresolved state, NOT a silent hide. Actionability is a function of the DERIVED status, and "unknown" fails VISIBLE.
- **Requirement + gate (req mints):** control visibility = the task's derived status on ANY resolution path; the gate MUST exercise an **out-of-window** QA-Review task (deep-link / previous-sprint / post-rotation) showing controls — NOT only current-sprint tasks (else it repeats the T37.24 false-green). stub-must-fail: an out-of-window QA-Review task with hidden controls → RED.

## Landing-3 path (PO DECISION — recommended path only; the eager-seed workaround is REJECTED)
The fix (`attachTaskStatus`) is a **PREREQUISITE of the acceptance**, not a distraction: expert lands it NOW as fix-on-demand; the tester WAITS for it and does NOT eager-seed.
- **Why the eager-seed is REJECTED as GREEN:** seeding the task INTO the current-sprint eager window would prove only the EAGER case and MASK the out-of-window defect (the T37.24 false-green). PO enforces: **landing-3 MUST exercise a NON-EAGER QA-Review task** (deep-link / previous-sprint / opened-by-ref-from-/trace — NOT in the current pin's `Sprint.tasks[]`). Controls-appear on that non-eager task is the proof the fix works; approve → controls-vanish is the re-derive. The acceptance itself LOCKS the fix — an eager-seed cannot.
- (For reference only, NOT the accepted path: eager membership is `current-pinned Sprint.model.tasks[].includes(taskIor)` — server.ts:1904, NOT `sprintName` — which is why the tester's `sprintName` seed didn't take. Recorded so the mechanism is clear; do not use it to green landing-3.)

## Report
Both to PO: (1)+(2) = a real Tron-facing latent defect (controls by membership, not status); (3) fix = `attachTaskStatus` at `/api/ior` (derive from statusChecklist, single-source) + a requirement whose gate exercises an out-of-window QA-Review task. Recipe given to the tester so landing-3 unblocks; the defect does not get buried by the seed fix.

## ★ BADGE (v0.8.114) — the C-assertion is PIXEL-verified @390, NOT DOM presence (PO refinement, LOCKED)
The '⚠ status unresolved' badge is a VISUAL affordance FOR TRON. So its gate must be VISUAL, not structural — the same law turned on the gate: if a DOM attribute is SILENT to Tron, a gate asserting only the DOM attribute proves NOTHING about what Tron SEES. Asserting DOM presence for a visual affordance is the exact false-green class that burned us twice (the connector-ribbon DOM-count Tron rejected on sight; the 'in the DOM but clipped' drawer). [[visual-features-gate-by-pixel]] / [[visual-features-verify-by-screenshot-not-dom]].
- **When v0.8.114 ships, the C-stub EXTENDS to: the badge is VISIBLY RENDERED — screenshot + PIXEL at Tron's 390 viewport — on a genuinely-unresolvable task; NOT merely `data-status-unresolved` present in the DOM.** stub-must-fail: remove/clip the badge → C RED (a pixel that isn't there, not a DOM node that is).
- **Non-noisy (rare BACKSTOP state, never routine clutter):** status is always derivable from `statusChecklist`, so 'unresolved' must be genuinely rare — a defensive backstop for malformed/statusChecklist-less units, NEVER routine on Tron's screen. If it EVER shows in normal use, that is itself a signal worth ESCALATING (a data-integrity defect surfaced), not clutter to tolerate.
- **Handoff timing:** hand this pixel-C criterion to the tester WHEN v0.8.114 SHIPS — NOT during the landing-3 run (do not interrupt the acceptance mid-flight). Architect backstops the badge myself = pixel-visible @390, still reads `unit.status` single-source.
