# R32.9 — MDA feature MISSING from FeatureManager (Tron device-QA, 2026-07-30)

**Tron:** "the feature manager does not show the new feature."

## Root (measured, PO)
`src/ts/server/FeatureManager.ts:31` — `SEED_FEATURES = [ServerManager, FeatureManager]` only. The MDA/Model-Driven Code Quality feature was built (R32.1-8: model views/logic all gated) but **never registered as a `ior:class:Feature` unit + seeded into the FeatureManager**, so it doesn't list + isn't launchable by the owner. NOT stale-served — a missing registration.

## The miss (own it)
Sprint 32 gated the MODEL surface (generation/tree/diagram/edges/puml/resync) but NO AC covered the FEATUREMANAGER surface (the user-facing entry). "Functionally complete" was premature — the feature is unreachable. [[gate-the-ac-surface]] + [[device-qa-regression-means-missing-ac]]: gate where the AC specifies (the user launcher), not where the code sits.

## Fix (scenario-first, feature-completion -> architect->expert->tester)
- req: AC = "Model-Driven Code Quality is a registered ior:class:Feature + APPEARS + is LAUNCHABLE in the FeatureManager @390, owner/member-gated (R31.8 data-driven gate like ServerManager)."
- architect: design - create the MDA Feature unit, seed/register it (SEED_FEATURES or the feature-list mechanism), wire its launch -> the model view (drop->tree/diagram/edges); membership-gated per R31.8; NO new fork.
- expert: implement the registration + launch wiring.
- tester: gate the FEATUREMANAGER SURFACE - feature appears in the list + launches -> model view, @390 Tron viewport (the surface the AC specifies, not the model API).
- Then Tron @390 visual = open FeatureManager -> see + launch MDA -> drop->see the render.
