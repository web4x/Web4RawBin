# R32.10 — /model page: NO drawer opens on select + model is a DEMO not RawBin (Tron device-QA, 2026-07-30)

Tron (IMG_4715, feature LIVE 📐): "how do i reach the circle class and drop it into the drawer diagram... get its methods... where are the classes/functions/interfaces of RAWBIN? there is not even a drawer opening currently."

## Root A — NO DRAWER ON /model (measured, PO)
serverModelPage (server.ts:~1000) HTML mounts ONLY `<rb-trace-tree id=model-tree>` — NO `<rb-detail-drawer>` element. The drawer opens via document 'selection-changed' → onSelectionChanged (rb-detail-drawer.ts:63/82), but it's NEVER MOUNTED on /model (R32.9 shell added the tree, forgot the drawer). So select-a-node → nothing opens → diagram + node-detail + method-signature ALL unreachable. /trace mounts the drawer; /model must mirror it.
FIX: add `<rb-detail-drawer>` to the /model page + ensure model.js registers the detail elements (incl rb-diagram-detail for `diagram:` refs). Then: select class → drawer node-detail (+ its diagram/methods); select member(method) → signature detail.
★ GATE-GAP (again): R32.9 surface-gate checked feature-APPEARS + /model-LOADS + non-member-403, but NOT select→drawer-OPENS (the INTERACTION). 2nd "gated loads not works" (R32.9 not-listed → now drawer-not-opening). Gate the INTERACTION @390, not just load.

## Root B — model is a DEMO fixture, not RawBin (measured)
/api/model/tree = Circle/Point/Shape/Id/makeId = the r32.2-sample.ts go-live demo (server.ts:1551 generates one abs file into MODEL_STORE). Tron wants RAWBIN's real classes/functions/interfaces = generate the model from RawBin's actual src (MULTI-FILE). This is the R33 multi-file model-sync (backlogged) — ELEVATE: it's the real value of "Model-Driven Code Quality" (model RawBin itself). Needs a "model this project/src" generation over many files → the tree/diagram/edges show RawBin.

## Route (scenario-first)
- R32.10 (drawer-on-/model): architect design (mount rb-detail-drawer + wire select→detail/diagram, mirror /trace; class→diagram reachable, method→signature) → req AC (gate the INTERACTION @390: select→drawer opens w/ detail; class→diagram; method→signature) → expert → tester (INTERACTION gate @390).
- R33 (RawBin real model, multi-file): elevate from backlog — Tron wants RawBin modeled, not the toy. Needs Tron's authorization (new-sprint-or-schedule). The actual usefulness.
