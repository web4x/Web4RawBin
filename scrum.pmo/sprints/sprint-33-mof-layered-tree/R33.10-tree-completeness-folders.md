# R33.10 — Model tree completeness + folder grouping (Tron device, 2026-07-31)

**Tron:** "still missing the 55 puml under puml. are there really just 25 ts files???? missing the rest… grouping them in folders makes sense"

## Three defects/asks in the Model-Driven Code Quality tree
1. **55 source .puml MISSING under puml/** — the puml/ folder does not show the 55 source .puml on device. NOTE: planner had marked item-4 folder-populate GREEN (T33.5) — so this REGRESSED or was never truly live on device. Likely tied to architect's **BUG-A: pumlChildren drops the diagrams/ ref (server.ts:1082, 1-line code fix)** which breaks the puml enumeration. This is a CODE fix — does NOT need plantuml installed (that's BUG-B, render-only). So the missing-55-puml should be fixable now via BUG-A, independent of the ops blocker.
2. **Only ~25 TS files shown — "missing the rest"** — the tree enumeration of TS files is INCOMPLETE. Real count is higher; the tree is dropping most. Enumeration/scan-scope gap (echoes the strict-AST scan-root gaps: something limits the walk). Architect to diagnose the true file count vs shown, find why the rest are dropped.
3. **Group items in FOLDERS** — the tree should organize files/classes into a FOLDER hierarchy (by directory), not a flat/partial list. Tron: "grouping them in folders makes sense."

## Route (feature: architect diagnoses/designs → expert → tester → Tron)
- **BUG-A (missing 55 puml)** = concrete 1-line fix (pumlChildren diagrams/ ref, server.ts:1082) — dispatch-able NOW, independent of plantuml/BUG-B. Verify the puml/ folder then shows all 55 source .puml on device.
- **Tree completeness (25-vs-all ts)** — architect diagnose the enumeration gap (why most TS dropped) → fix the walk to show ALL.
- **Folder grouping** — architect design the folder-hierarchy tree (group by directory) → build → gate @390.
- Refs: IMG_4802/IMG_4803 (same Model-Driven view) in this dir. Capture as R33.10 (or fold puml-fix into R33.1.1 BUG-A). Gate @390: puml/ shows 55, ts complete, folder hierarchy renders.

## ★ PLANTUML DOCKER — INSTALLED (do NOT forget again; Tron: "you forgot")
BUG-B was NOT "plantuml missing / install on host" — the render must use a **plantuml Docker**, not a host binary. Root: server.ts:2126 calls `execFileSync('plantuml','-tsvg','-pipe')` (host PATH) → ENOENT 501. No plantuml container existed on WODA.prod or WODA.test (checked running+stopped).
FIX (Tron-authorized, PO installed 2026-07-31): ran `plantuml/plantuml-server:jetty` on the PROD host (via remoteShells:0.3 / WODA.test) — **container `plantuml-server`, `--restart unless-stopped`, host port 8089 → 8080, HTTP 302 welcome = UP**. URL = `http://localhost:8089`.
EXPERT re-wire: server.ts:2126 exec→HTTP POST/GET to the plantuml-server /svg endpoint (deflate+encode the puml, or POST raw per the jetty API). Make the URL **CONFIGURABLE** (R31.7 typed-config, not hardcoded). Version-bump + atomic. Verify Compile→SVG returns a rendered SVG on device.

## ★ CORRECTION (host + crash fix, 2026-07-31)
- WRONG HOST first: I ran it on remoteShells:0.3 = WODA.test; the RawBin PROD server + architect are on **WODA.prod** → localhost:8089 was empty for the server (architect caught it). CORRECT host = **WODA.prod** (where the RawBin server runs, so localhost:8089 resolves). Stray WODA.test container removed.
- CRASH-LOOP fix: `plantuml/plantuml-server:jetty` crashed on `pthread_create EPERM` / "cannot create GC thread" = a host seccomp/nproc limit blocking JVM threads. FIX flags: `--security-opt seccomp=unconfined --ulimit nproc=8192 --ulimit nofile=8192 -e JAVA_OPTIONS="-XX:+UseSerialGC -XX:ActiveProcessorCount=1 -Xmx512m"`.
- FINAL: container `plantuml-server` on WODA.prod, `-p 8089:8080`, `--restart unless-stopped`, running exit=0, `/svg/<encoded>` = HTTP 200 image/svg+xml VERIFIED. plantumlUrl config = http://localhost:8089. Expert's /api/puml-render wire (v0.8.34) is correct+live → BUG-B closes on architect served-verify.
