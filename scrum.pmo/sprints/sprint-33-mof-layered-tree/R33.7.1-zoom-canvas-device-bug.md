# R33.7.1 zoom-out — device bug (Tron, IMG_4793): zoom works but canvas does NOT grow

**Tron (device, 13:25, Model-Driven Code Quality / CompanyIndex.ts):** "diagram is much better … but zoom out does NOT increase size really. zoom out works, but not increasing size. good progress on tree navigation and other details!"

## Positive (device-confirmed)
- Diagram rendering much better; **tree navigation (R33.7.4) works on device** ✓; other details good.

## THE BUG (device-QA catch on R33.7.1)
Zoom-OUT **works** (content scales smaller) BUT does **NOT increase the SVG canvas/diagram SIZE**. So it does NOT solve the acute SPACE PROBLEM (Tron's original R33.7.1 intent) — it just shrinks everything inside the *same-size* box; no extra placeable room appears.

Tron's original R33.7.1 AC (round-3 capture): "zooming OUT ... INCREASES the SIZE of the SVG diagram (the diagram CANVAS GROWS)" so there's more room for elements. Current impl scales the CONTENT (transform) but the SVG element's own dimensions stay fixed → canvas doesn't grow → no room gained.

## Why the gate passed but Tron's device shows the bug ([[device-qa-regression-means-missing-ac]])
The R33.7.1 gate asserted **scale<1 / 1=whole / persist** (zoom WORKS) — but NEVER asserted the **SVG canvas DIMENSIONS actually increase** on zoom-out. So it green-passed a "scale" while the real AC (canvas-grows-for-room) was untested. The AC needs a testable measure of the canvas GROWING, not just the scale value.

## FIX DIRECTION (scenario-first)
- **architect DIAGNOSE:** the design ("viewBox canvas-pad = real placeable space", MIN 0.25 + clamp-centers) intended the canvas to GROW, but the impl apparently only applies a content SCALE without growing the SVG element's width/height (or the viewBox/scrollable area). Measure: on zoom-out, does the SVG element's rendered dimensions / scrollable canvas actually increase, or only the inner transform scale? Root the gap.
- **req SHARPEN AC:** R33.7.1 add a testable AC = "zoom-OUT INCREASES the SVG canvas dimensions / scrollable placeable area (measurable px growth), not merely the content scale." Ride the existing R33.7.1 unit (754a1f9d), don't re-mint.
- **expert FIX:** zoom-out must actually GROW the canvas (increase the SVG width/height or the scroll area) so more placeable room appears — reuse RbPanZoom, no fork.
- **tester RE-GATE:** assert the SVG canvas DIMENSIONS increase on zoom-out (measure px, not just scale) @390; + Tron device re-confirm (his real viewport = the acceptance surface).

## Route
architect diagnose → req sharpen AC → expert fix → tester re-gate (canvas-dimensions-grow) → Tron device. Client-only, version-bump. Screenshot: R33.7.1-zoom-canvas-device-feedback-IMG_4793.png.

## ARCHITECT DIAGNOSIS (robbin-architect 2026-07-31, MEASURED on served 0.8.30)
ROOT = TWO gaps, both leave the canvas un-grown on zoom-out:
1. **viewBox = TIGHT content bounds (no CANVAS_PAD).** `buildDiagramSvg` (diagram-view-model.ts:106): `vb = 0 0 max(maxX,100) max(maxY,100)` where maxX/maxY = per-box bounds + a per-box PAD only (:102-103). Design row B ("viewBox CANVAS_PAD = real placeable space") was NOT implemented → no extra placeable coordinate space even at scale 1.
2. **Zoom = pure CSS `scale` transform.** `RbPanZoom.apply()` (pan-zoom.ts:157-159): `content.style.transform = translate(tx,ty) scale(s)`. At scale<1 the content SHRINKS inside the SAME-size surface; the SVG element's rendered width/height AND the surface scroll-area are UNCHANGED. So zoom-out = smaller content in a fixed box, NOT a bigger canvas.
NET: R33.7.1 shipped only the scale-transform half (MIN 1→0.25); the canvas-GROW half never landed. `preserveAspectRatio="xMidYMid meet"` + no explicit SVG width/height means even a padded viewBox would just shrink apparent content (fit-to-container), not add visible room. Architect owns the under-spec: "viewBox canvas-pad" alone (row B) is INSUFFICIENT for the "canvas GROWS" intent — the fix must grow the CANVAS DIMENSIONS on zoom-out.
FIX DIRECTION (expert; reuse RbPanZoom, no fork): on zoom-OUT, GROW the SVG canvas's rendered dimensions / scrollable placeable area — e.g. give the SVG explicit width/height that scale as `canvasBase / scale` (at scale<1 → bigger px canvas) inside a SCROLLABLE surface, so boxes keep their coordinate size and the canvas around them grows = real room; pan/scroll the larger canvas within the fixed viewport. (Equivalently: zoom-out increases the placeable coordinate space + the rendered canvas px, not just the transform scale.) req: sharpen the R33.7.1 AC to a TESTABLE canvas-dimensions-grow measure (px growth on zoom-out), ride unit 754a1f9d. tester: assert SVG canvas width/height (or scroll area) INCREASES on zoom-out @390 + Tron device.
