# Sprint 15 — Object.verb Use-Case Diagrams

Object.verb-style use-case `.puml`/`.svg` (Object = noun/class, verb = method) by
robbin-architect, with `[uc:uuid:...]` tags per the
[traceability standard](../../../standards/traceability-standard.md), linked from
each task's chain section.

Planned:
- `object-verb-usecases.puml` — Requirement/Test/Implementation/Task/UseCase objects
  and their verbs (methods-as-routes); matrix.fix; browser.browse(tree);
  list.search→remoteSearch; object.itemView; task.detail; planning.overview.
