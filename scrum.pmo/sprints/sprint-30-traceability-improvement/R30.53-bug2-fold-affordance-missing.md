# R30.53 BUG-2 (Tron device QA, v0.7.79) — fold affordance MISSING for methods below a point

Tron: *"alignment improved!!! but from here on collapsing/expanding is not offered any more (only visible on hover or click) but not available at all anymore from here below."*

## GOOD NEWS FIRST
The two-part fix WORKED — **alignment is confirmed fixed on Tron's real device.** The parity/misalignment bug (BUG-1) is device-validated. This is a SEPARATE, new/newly-visible issue.

## BUG-2 (evidence: diagrams/R30.53-bug2-fold-affordance-missing-below.png)
Scrolled to lines ~140-170 (all-unchanged tail, BELOW the last change/conflict block). The method declarations there — `otmux.attach.completion.*`, `otmux.attach.readonly()` — have **NO fold chevron (⌄/›) in the gutter**. Collapse/expand is not available for methods from a certain point downward.

## HYPOTHESES (measure to confirm — do NOT assume)
1. Fold RANGES are only computed up to the last change/conflict block → the all-unchanged tail below it gets no foldable ranges (boundary bug in foldByMethodBoundaries / range-provider region).
2. FIX-A (split Math.max floor / collapsible-set change) truncated the foldable set below a boundary.
3. Monaco virtualization/lazy fold-range registration below the viewport.

## SCOPE
Within R30.53 (native code-folding). R30.53 stays In-Progress. Likely a missing AC: **fold/collapse affordance must be available for EVERY method at ALL scroll positions (incl. the all-unchanged tail below the last change).**

## DRIVE (scenario-first, no hotfix)
1. TESTER reproduce+measure: count foldable ranges vs method count at multiple scroll positions; confirm ranges missing below the last change-block + the exact line boundary. (This localizes it — like BUG-1's RED baseline.)
2. ARCHITECT (booting from rewind) diagnose root once measured → fix direction.
3. REQ sharpen AC: fold affordance for every method, all positions.
4. EXPERT hold → fix per diagnosis, atomic, version-bump.
5. TESTER gate (fold-range-per-method at all positions) + Tron device re-confirm.
