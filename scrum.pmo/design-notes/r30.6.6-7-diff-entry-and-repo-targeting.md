# R30.6.6 — Diff entry point + R30.6.7 — OOSH-repo targeting

**Author:** robbin-architect 2026-07-13 (DESIGN-ONLY; req single minter). Extends the R30.6 diff/merge editor. NOTE: the expert is already mid-build on GitApi (server.ts:495-507 `GitApi.ROOT`/safePath, `/api/git/` route :1359) — R30.6.7 should fold into that build, not retrofit.

## R30.6.6 — reaching the diff editor (Diff tab/mode in /edit)
**Measured wiring:** `rb-editor-layout` has `activePanel` (tree/editor/preview) + a **mobile-only** tab bar (`.el-tabs` with `.tab-btn[data-panel]`). Desktop uses a split layout. `rb-editor-toolbar` has a `#tb-mode` button dispatching `mode-change {mode}` (an existing mode mechanism). So Diff must be reachable in BOTH form factors — reuse both hooks rather than inventing a route.

**Design (REFINED per Tron 2026-07-13 — an [Open Diff] BUTTON, not a tab):**
- Add an **`📊 Open Diff` button** to the editor top-right toolbar (`rb-editor-toolbar`, next to the View/Preview mode toggle + Save). On click it dispatches `toolbar-open-diff` (bubbles), like `toolbar-save`.
- `edit.ts` handles `toolbar-open-diff` → calls `layout.showDiff(currentFilePath)`.
- **`RbEditorLayout.showDiff(currentFilePath)`** — mount `<rb-diff-editor>` (lazily, like `rb-file-tree`/`rb-code-editor`) with **LEFT preselected** to the current editor file: pass the current file's **path + content** (reuse the editor's current-file state / `rb-code-editor.getValue()`), so the diff opens ready-to-compare; the user then picks the RIGHT side.

**Methods (name-matching):**
- `RbEditorLayout.showDiff` (param `currentFilePath: string`) — mount rb-diff-editor with Left = current file. Class **RbEditorLayout REUSE** (existing unit 94e7bf82).
- (Toolbar button + `edit.ts` `toolbar-open-diff` wiring is glue under `showDiff`; no separate chain method needed unless req wants one for the button.)

Why a button over a tab/route: the tab bar is mobile-only + a route would duplicate the editor chrome; a toolbar button is one-click from the editor, works both form factors, and naturally carries the current file as Left (Tron's "open it on what I'm editting").

## R30.6.7 — target the OOSH repo safely (not RawBin's PROJECT_ROOT)
**Problem:** GitApi + the file tree currently root at `PROJECT_ROOT` (RawBin). The diff tool must operate on the **OOSH repo** (Tron's version issue). Need a safe way to select a *different* repo root.

**Design — allowlist of repo KEYS (not client-supplied paths):**
The client sends a repo **key** (`?repo=oosh`), never an absolute path. The server maps the key → a configured absolute root from a server-side allowlist. Unknown key → 400. This removes client path-abuse entirely; traversal *within* a root is still blocked by the existing `safePath` (server.ts:505-507).

- **New Class `RepoRegistry`** (single source of the security-critical allowlist; shared by GitApi + the files handler):
  - `RepoRegistry.resolve(repoKey): string | null` — return the allowlisted absolute root for the key, else `null`. Config: `{ rawbin: PROJECT_ROOT, oosh: <abs OOSH repo root> }` (from env / a `repos.json`; ops configures the OOSH abs path).
  - `RepoRegistry.list(): {key,label}[]` — for a repo picker in the diff UI.
- **GitApi** (already SHIPPED, 7c9554494 — ROOT hardcoded as `private static readonly ROOT = path.join(__dirname,'../../../')`, consumed by `GitApi.OPTS.cwd` + `safeRelPath`): R30.6.7 is a small **de-hardcode** — replace the const with a per-request `RepoRegistry.resolve(req.repo)` seam that both `OPTS.cwd` and `safeRelPath` read; `branches`/`commits`/`fileAtRef` then run with `cwd = resolvedRoot`. Ref allowlist `^[\w./-]+$` unchanged. Expert threads it in one change once R30.6.7 is minted + the class-vs-method decision is ruled (no churn during the R30.6 tester gate).
- **Files** (`GET /api/files/`): accept optional `?repo=<key>` → `RepoRegistry.resolve` → `readDir`/`readFile` rooted there. Default `rawbin` when absent (back-compat, R30.5 unaffected).
- **`RbFileTree`** (client, REUSE 0916d007): new method **`RbFileTree.setRepo(key)`** — set a `repo` attribute + reload; `loadDir` appends `?repo=<key>`.
- **`rb-diff-editor`:** a repo selector (global or per-side) sourced from `RepoRegistry.list()`; passes the chosen key into `loadSide`/`pickFile`/`pickRef` so both the file-chooser and git ops target the same repo.

**Security invariants (mandatory):**
1. Client supplies a KEY only; the absolute path lives server-side in the allowlist. Never resolve a client-supplied absolute path.
2. `safePath` still applies *within* the resolved root (no `..`, no leading `/`, `resolve(root,p).startsWith(root+sep)`).
3. Read-only across all repos (git `branch`/`log`/`show`, `readDir`/`readFile`); no writes to a non-default repo unless explicitly allowlisted for write.
4. Default remains `rawbin`=PROJECT_ROOT → R30.5/existing callers unchanged.

## Chain (for req to MINT)
- **R30.6.6:** UC `diffEditor.openDiffButton` → Method `RbEditorLayout.showDiff` (param currentFilePath; Class RbEditorLayout **REUSE** 94e7bf82). Impl design-ahead, sourceFile `rb-editor-layout.ts` (+ toolbar button glue in rb-editor-toolbar.ts / edit.ts).
- **R30.6.7:** UC `diffEditor.repoTargeting` → Methods `RepoRegistry.resolve` + `RepoRegistry.list` (Class RepoRegistry **MINT NEW** — measure first) + `RbFileTree.setRepo` (Class RbFileTree REUSE 0916d007) + (GitApi/files consume RepoRegistry — note in Impl, no new GitApi method beyond wiring `ROOT` to resolve). Impls design-ahead.
- Method names EXACT for AST-attach. Confirm RbEditorLayout Class unit exists (reuse) or mint; verify 0 existing RepoRegistry (I expect new).

## Open decision for PO/Tron
**RepoRegistry as its own Class vs. `GitApi.resolveRepoRoot`.** I recommend **RepoRegistry** (the allowlist is cross-cutting — git AND files use it; one place for the security-critical mapping; satisfies R27.5 one-class-per-file; testable in isolation) over hanging it off GitApi (which files would then depend on awkwardly).

## AC sketch
- Diff tab/mode appears in /edit (mobile tab + desktop entry); activating mounts rb-diff-editor with the current file as Left.
- Repo picker lists allowlisted repos; selecting `oosh` makes the file-tree + git ref-picker operate on the OOSH repo.
- `?repo=unknown` → 400; `?repo` absent → rawbin (unchanged). No client-supplied absolute path is ever honored; `..` still rejected within a repo.
