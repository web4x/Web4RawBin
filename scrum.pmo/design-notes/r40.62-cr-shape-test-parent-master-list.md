# CR shape — Task master-list + Test parent + affected-units children (architect, 2026-08-29)

Tron-assigned (architect designs shape + trace-down rule; req mints). SHORT — impl shape + AC. Tron verbatim: *"the task has … a list of CR as master … each cr needs to parent to a test that has to be reevaluated from task traced down to the test … so the CR has all traceability units as children that are affected to be changed."* ⇒ **BOTH links legitimate, not either/or.** Supersedes R40.60's "CR ownerIor→Task, crossRef→Test" — Tron reverses it: parent IS the Test (semantics: resolving the CR may require that Test to change).

## Impl shape (3 links, each authoritative for a different fact)
1. **Task → CRs = MASTER list** — `task.changeRequests[]` is the AUTHORITATIVE Task→CR list (NOT a "losable mirror" — the server.ts:1391 comment is now WRONG). The Task owns which CRs it has.
2. **CR → Test = PARENT** — `ChangeRequest.ownerIor → ior:class:Test` (the Test that must be re-evaluated/changed to resolve the CR). This is the traceability parent → renders under the Test (5e9a1d887 already does this by reverse-ownerIor scan). NOT ownerIor→Task.
3. **CR → affected units = CHILDREN** — `CR.model.affects[]` = the traceability units along Task→…→Test that must change for the fix to land consistently. The CR is the CONTAINER of what changes; renders CR → affected-units (forward children).

CR unit: `{ uuid, name/reason, status: Open|Resolved, createdBy, createdAt, task→Task (backref, = the master-list holder), affects[]→affected units }`, `ownerIor→Test`.

## Trace-down-and-re-evaluate rule (Tron's core mechanic)
On a CR raised at a Task: **trace DOWN Task → coveredRequirement → UseCase → Class → Method → Impl → Test**; the Test(s) needing change = the CR's parent(s); the units on that path that must change = `affects[]`. **Resolving the CR = re-evaluate the parent Test + each affected unit** so the change lands consistently across the chain; `CR.status → Resolved` ONLY when the parent Test re-evaluates green WITH the change (evidence-gated, not stamped). The band (R40.60 `deriveTaskStatus`) already finds a task's CRs by `model.task` (server.ts:1441) → unaffected by parent=Test.

## By-construction single-source (my lane)
- **Master-list ↔ backref consistency:** every CR in `task.changeRequests[]` has `model.task===that task`, and every CR unit with `model.task===T` is in `T.changeRequests[]`. ONE master (the task list); the backref must equal it — gate RED on a CR in no master OR two.
- **Parent-is-Test:** `CR.ownerIor` resolves to `ior:class:Test` — gate RED on a non-Test CR parent.
- **affects[] are real units** on the Task→Test trace — gate RED on a dangling affects-ref.

## ★ FLAGGED DEFECT (bring fix-shape, do NOT silently fix — PO's instruction)
Measured: `task.changeRequests[]` is EMPTY on all tasks (0 carry it) while CR units exist (found only via `model.task` backref) — under Tron's model the empty master list is a **DEFECT**, not a design choice. FIX-SHAPE (gated, PO-approved): backfill — for each ChangeRequest unit, add its uuid to `model.task`'s `changeRequests[]`; dry-run+COUNT; INV every CR appears in exactly ONE task master; idempotent; correct the :1391 "losable mirror" comment to "MASTER". NOT run until PO GO.

## ACs (req mints, each gateRef + stub-must-fail)
1. **CR-parent-is-Test** — `ChangeRequest.ownerIor → ior:class:Test`; RED on non-Test parent.
2. **task-master-list** — `task.changeRequests[]` is the authoritative populated list; CR.model.task ↔ master bidirectional, no drift; RED on empty-master-with-CRs or backref mismatch.
3. **CR-children-affected** — `CR.affects[]` = the affected units from the Task→Test trace; render shows CR → affected-units; RED on dangling affects-ref.
4. **trace-down-reevaluate** — resolving a CR re-evaluates the parent Test + affected units; `status→Resolved` only on parent-Test green-with-change (evidence-gated, not stamped).
5. **empty-master backfill** — the gated backfill populates masters; INV no-CR-lost; RED baseline = today's 0-master-with-5-CRs.

## Render (5e9a1d887) forward-note
Correct for the current CR shape (leaf under its Test parent). When `affects[]` lands, the render must show CR → affected-units children (hasChildren from affects[].length); non-blocking for THIS deploy (CRs have no children yet).
