# T40.1 checklist regression REPAIR — architect design (2026-08-29, Tron GO "do the recommended")

The checklist fix (fe495e32d, task-policy.ts:132-139) is FORWARD-ONLY: a NEW decline keeps QA Review [x] + adds the sub-step, but T40.1 (7a956c21) was regressed by a PRE-FIX decline and the fix does not retro-heal. Tester caught it on TRON'S surface (a scratch-only check would have read GREEN). This repairs the tasks OUR bug corrupted. **REPAIR, NOT FABRICATION — we restore authored state our bug unticked; anything we cannot prove WE corrupted, we do NOT touch. Non-negotiable.**

## ★ DISCRIMINATOR — CONFIRMED AIRTIGHT (measured, not assumed)
**A task whose checklist contains the `processing change requests` sub-step line WHILE the top-level `QA Review` box is UNTICKED `[ ]` = a PRE-FIX regression, BY CONSTRUCTION.** Proof (measured):
- The sub-step is inserted ONLY by a decline (`insertOpenCrSubstep`, task-policy.ts:154) — a decline requires the task be at QA Review/Done (validated :92-94).
- The FIXED decline (:132-139) does `untickBox('Done')` (only Done) + `tickBox('QA Review')` (ENSURE [x]) + `insertOpenCrSubstep`. So post-fix, inserting the sub-step ALWAYS keeps QA Review [x].
- There is NO live path that unticks QA Review while the sub-step is present: the only `untickBox('QA Review')` was the OLD pre-fix reopen, now REPLACED by `tickBox('QA Review')`. `:92` is validation-only (throws), not an untick.
- ⇒ post-fix, `(sub-step present + QA Review unticked)` CANNOT be produced. Any task in that shape carries OUR pre-fix decline's signature. The repair set FALLS OUT of the property — no hand-list, no task can hide.
- **The ONLY way to defeat it = a manual hand-edit of a checklist outside the seam (not a legitimate flow — the seam is the sole writer). If seam-only writes hold, the discriminator is exact.** If a legitimate seam path is ever found to produce this shape, the discriminator is WRONG and we STOP.
- VERIFIED T40.1 (7a956c21) matches: `- [ ] QA Review` + `  - [ ] processing change requests` (sub-step present, QA unticked) = the RED baseline target.

## THE REPAIR (targeted, seam-only, restore-not-invent)
For each task matching the discriminator: **re-tick the top-level `QA Review` box `[x]` via the SEAM** (`UnitController.apply(idx, 'ior:class:Task', uuid, { tickBox: 'QA Review' }, ...)` / the tickBox checklist edit). NOTHING else changes: the sub-step keeps its state, no direct `m.status` write — the band DERIVES (QA Review [x] + open sub-step → `QA-Review-with-open-CR`; matches his AC). We RESTORE the [x] the pre-fix decline unticked; we invent nothing.

## BINDING REQUIREMENTS (hand to expert — all six, DO NOT run without them)
1. **RED BASELINE FIRST** — the script must IDENTIFY T40.1 (7a956c21) as regressed BEFORE it writes anything. If it cannot find the known-bad case, it is the wrong script → STOP.
2. **BACKUP before the write** — plain copy or `tar` of `scenario/index` (or the matched task files), NEVER `cp -a` (hardlinks write through to live).
3. **PRE/POST COUNTS** — how many tasks match the discriminator, WHICH ones (list uuids), and what ELSE lives in those checklists (so a bad selector's blast radius is visible). Post: matched-count → 0, QA-Review-boxes-ticked count.
4. **WRITE THROUGH THE SEAM ONLY** — re-tick the box; band derives; no direct status write (single-status-writer stands).
5. **IDEMPOTENT** — 2nd run changes nothing (re-ticking an already-[x] box = no-op).
6. **FAIL-SAFE: if ANY preserved count DROPS or an UNEXPECTED task matches → RESTORE FROM BACKUP AND REPORT. Do NOT fix forward.**

## SCOPE DISCIPLINE
Repair ONLY the damage THIS bug caused (the unticked QA Review box). Do NOT opportunistically tidy anything else in those checklists — an unrelated "improvement" riding a data migration is how a repair becomes an incident.

## RULING AGAINST the heal-on-next-action alternative
REJECTED. Heal-on-next-action (T40.1 self-corrects when its next CR is processed) asks TRON to do work to see OUR fix, on the very task he reported — and leaves it visibly corrupted until an action that may never come (it may just sit at QA-Review awaiting his approval, showing the wrong checklist the whole time). We corrupted it; we repair it (with his GO). The targeted repair is correct; heal-on-next-action is insufficient.

## VERIFICATION (after the write)
The TESTER verifies on TRON'S OWN T40.1 @390 — the same surface that caught the forward-only gap. Nothing is reported to Tron as done until his task actually SHOWS the ticked QA Review box (QA Review [x] + Processing Change Requests [ ] → band). DEPLOY/RUN is gated on the RED baseline + backup + counts; expert executes only with all six requirements met.
