# R27.4 — Graph Integrity Repair Migration (design)

**Author:** robbin-architect · 2026-07-01. PO GO, same gated discipline as R27.2. Repairs the pre-existing debt R27.2's DELTA gate deliberately left untouched. req 3-point verifies. UC `f7a06e18` graph.repairIntegrity.

**Measured baseline (disk-fresh, post-R27.2):** 12 dangling UC refs + 51 orphan Methods. **CRITICAL finding: ALL 51 orphans carry an `[impl:uuid]` → 0 prune-candidates.** Every orphan MUST be ATTACHED (pruning any drops its Impl — the INV1b lesson). So R27.4 is a pure ATTACH + REPOINT migration; it deletes nothing except malformed literal refs.

## A. Dangling repair (12 → 0)
### A1 — 10 refs → DEAD RbDetailView `f2f84ce3-bbbc-4bf7-9345-6a9d4dc64fb5` (repoint, clean)
5 detailView UCs, each with `class` + `classes[]` → the dead bbbc: `singularChainSection`, `supersededLinks`, `chainExcludesSelf`, `unifyChainLabel`, `allChildrenSection`. **Repoint all 10 → the LIVE canonical `f2f84ce3-6f8f-4db1-9ab7-dbcfe8d3bc07`** (the R27.2 RbDetailView canonical — it HOLDS those 5 method-names, so the repoint reconnects each chain cleanly). USE FULL uuids (this dead unit shares the `f2f84ce3` 8-char prefix with the live canonical — the collision that caused the earlier confusion).

### A2 — 1 ref → DEAD Method `fcf6dae1-69c7-418d-a68a-6fca1d0a9137` (triage)
`requirement.classifyType.method` → a deleted Method. Triage: search for a LIVE Method named `*.classifyType` / `Requirement.classifyType`; if found → repoint `UC.method` to it; if none exists → **drop the `method` ref** (UC keeps its class, loses the method leg) and flag `requirement.classifyType` as chain-incomplete (candidate for a fresh Method mint). Never leave the dangling.

### A3 — 1 ref → literal string `"TODO-server-class"` (malformed, fix/remove)
`traceChildren.bugForwardKeys.class` = a literal TODO placeholder, NOT a uuid. Someone left a code-TODO in a ref slot. Identify the intended class (`traceChildren`/`bugForwardKeys` → likely TraceGraph/a server class); if identifiable → repoint; else **null the ref** and flag as a TODO to resolve. Either way the literal string is removed.

## B. Orphan Method repair — ALL 51 → 0 (attach) + CLEAR 37 stale markers
**Scope LOCKED (req re-measured, PO confirmed): attach ALL 51.** The 51/51-carry-impl is CONFIRMED via `Method.model.implementations[]` — and req verified all 37 `orphanByDesign` methods' impls point at REAL sourceFiles (rb-trace-tree.ts, server.ts, migration.ts, RoomView.ts, migrate-to-scenario.ts, rb-detail-drawer.ts): 0 phantom, 0 design-ahead. **The 37 `orphanByDesign` markers (2026-06-11 "no-source") are STALE/LYING — code was written AFTER the marker.** So they are NOT design-stage; attach them like the other 14. (My original all-51-attach was right; the 14/37 split was reading stale markers.)

- **ATTACH all 51** to a live Class (resolver priority; most have `ownerIor=null` → name→Class derivable): (1) live `ownerIor` Class; (2) else the `UC.class` of a UC using the method; (3) else derive Class from the method-name prefix (`RbTraceTree.*`→RbTraceTree, `RbDetailDrawer.*`→that Class, `Migration.*`/`server.*`/`RbRoomContent.*`→their Class), reusing the R27.2 canonical for that code-class name.
- **CLEAR the 37 stale markers:** on attach, if a Method carries `orphanByDesign`/`designStage` BUT its Impl HAS a real `sourceFile` → DELETE the stale marker (it's not design-stage; code shipped). Keep `orphanByDesign` meaningful ONLY for a Method whose Impl has NO sourceFile (genuine future/design-stage).
- Since we **attach + clear-marker, delete nothing**, INV1b (431 == 431, distinct Impl unchanged) is trivial.
- Dry-run asserts: 51 orphan-Methods → 0, 37 stale markers cleared, 0 Impl deleted.

## Gate — DRY-RUN + COUNT first (R27.2 discipline)
`--dry-run` emits a count table; `--apply` only after gate + atomic/rollbackable (git-snapshot pre-apply). **HARD INVARIANTS:**
1. **dangling 12 → 0** (all 12 repointed/dropped; A1 repoint 10, A2 triage 1, A3 fix 1).
2. **orphan Methods 51 → 0** (all attached to a live Class.methods[]).
3. **0 NEW dangling** — every repoint target EXISTS + resolves; no repoint creates a new dangling (assert each new ref → live unit).
4. **distinct Impl units UNCHANGED (431 == 431):** R27.4 attaches/repoints, deletes NO Method or Impl unit → INV1b trivially holds (the pure-attach nature is what makes this safe). Assert distinct impl-uuid count before==after + 0 orphaned Impls.
5. **No NEW orphan Method:** attaching doesn't detach anything; count before==after on the attached set.
6. **Atomic + post-apply re-assert:** `--apply` re-emits ACTUAL post-mutation counts (dangling==0, orphans==0, impls==431) and asserts actual==predicted; rollback if any deviates (a validated plan can still be mis-executed).
- Emit the count table as evidence; req 3-point verifies (dangling 12→0, orphan 51→0, 0-new, 431==431, atomic).

## Traceability
UC `f7a06e18` **graph.repairIntegrity** → Class `GraphIntegrity` → Method `GraphIntegrity.repairIntegrity` → Impl (the repair-migration script, design-ahead until coded). The one-time repair = a gated migration task (planner). Pairs with R27.2's `chain.reuseCanonicalClass` invariant — R27.2 prevents new Class dups by construction; R27.4 repairs the residual dangling/orphan debt.
