# R40.19 — History Back-Navigation Regression Gate (design)

**By:** robbin-architect 2026-08-11, per PO. Tron: back-nav + path-label nav are PERFECT NOW and must never regress. This gate is the by-construction protection — the music player died to exactly a refactor silently dropping this kind of connection (R34.7/R-E unification-dropped-connection). Design → req mints scenario-first → expert wires into the device lane → tester gates → I backstop.

## MEASURED subjects (the code being protected)
`src/public/ts/components/rb-editor-toolbar.ts`:
- `historyBack()` @:55 = `window.history.back()` — Impl marker `6b4d7714` (EditorHeaderNav.historyBack). Wired `#tb-back` click @:50.
- `pathLabelNav()` @:58 = `window.location.href = containingFolderHref()` — Impl marker `197054f9` (EditorHeaderNav.pathLabelNav). Wired `#tb-path` click @:51.
Both are EXISTING Impls (do NOT re-credit — R30.11). R40.19 adds the GATE, not new nav code.

## The gate — behavioural @390, RIDES the post-deploy trigger
### INV-HN1 — BEHAVIOURAL, never DOM-count (visual/behaviour gating rule)
The gate asserts what the USER LANDS ON at real WebKit@390, never an element count / class presence:
- Build history (open editor → navigate into a file/view), tap `#tb-back` → assert the resulting VIEW is the correct PREVIOUS one (route/URL identity or the prior view's behavioural signal), not "N nodes exist".
- Tap `#tb-path` → assert path-label nav RESOLVES (containing-folder view actually loads at its href), not that a label string is present.
Real WebKit@390 (the `gate:device:live` lane already emulates this) — pixel/behavioural, per the visual-features-gate-by-screenshot-not-DOM rule.

### INV-HN2 — RIDES DeviceGateTrigger, no reinvented invocation
The gate is a case in the EXISTING `gate:device:live` lane (WebKit@390 vs live prod). `scripts/post-deploy-gate.mjs` (R40.25 `DeviceGateTrigger.runAndRecord`, uuid `7fee0120`) already runs that lane as the deploy's final step and records a version+commit-stamped unit. So R40.19 INHERITS: NOT-RUN==RED (INV-PDG-2), freshness served==committed (INV-PDG-7), no-silencer (INV-PDG-5), durable artifact (INV-PDG-3). We ADD a case to the lane — we do NOT build a new trigger/invocation (expert is wiring the trigger now; this rides it).

### INV-HN3 — STUB-MUST-FAIL (proves it exercises real nav)
Bite (i): stub `historyBack`/`pathLabelNav` to a no-op → the behavioural gate goes RED. If a no-op still passes, the gate is measuring a mock, not the nav — RED by construction.

### INV-HN4 — TOUCH-PROTECTION LINT (the music-player rule: no refactor without this gate green)
No diff may touch `RbEditorToolbar` / `historyBack` / `pathLabelNav` without this gate present + green. Static lint asserts BY CONSTRUCTION:
- (a) the wiring is intact: `#tb-back`→`historyBack` and `#tb-path`→`pathLabelNav` still bound (a unification refactor that drops the listener = the music-player death → RED);
- (b) the R40.19 behavioural case is PRESENT in `gate:device:live` (a refactor that removes the gate → RED).
Two-bite: (i) remove the wiring OR the gate case → RED; (ii) meta: the lint itself runs → weaken/remove the lint → suite RED. Folds into `ci:gates` alongside the facet-type / INV-D1 lints.

## Chain (verify-owner-first)
- **Behavioural device Test** (new) attaches to the EXISTING Impls `6b4d7714` (historyBack) + `197054f9` (pathLabelNav) via their `Impl.tests[]` — NO re-credit of the nav Impls.
- **Static touch-protection lint** = a NEW distinct Impl on a NEW Method `EditorNavGuard.assertBackNavGate` (new file, e.g. `scripts/backnav-gate-lint` / a shared guard) — distinct sourceFile, reads the toolbar + lane as evidence, does NOT cross-wire onto `6b4d7714`/`197054f9`.
- R40.19 requirement → NEW UC `editorNav.protectBackNav` → Class `EditorHeaderNav` (existing, rb-editor-toolbar.ts) for the behavioural leg + Class `EditorNavGuard` for the lint leg. req mints scenario-first (UC + the guard Method/Impl uuids); expert wires the lane case + the lint + places markers; tester runs both bites; I backstop @390 + flip the guard Impl.

## Non-negotiables (restate)
Behavioural/pixel @ real WebKit@390 — NEVER DOM-count. STUB-MUST-FAIL. RIDES the post-deploy trigger (no reinvented invocation). Touch-protection covers RbEditorToolbar/historyBack/pathLabelNav so a future refactor cannot silently break what Tron says is perfect now.
