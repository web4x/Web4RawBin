# Campaign Inventory — Sprint 37 (C1–C7) + Sprint 40 (T40.1–T40.12)

**Authoritative per-task chain-state, measured FROM THE SCENARIO UNITS ON DISK, not the board MD.**
Where the board (`planning.md` / task-MD) and the units disagree, **the units win** and it is called out.

- Author: robbin-planner (audit). Read-only measurement. **Zero status flips. No requirements/board MD touched.**
- Method: read each Task unit (`model.status` + `model.statusChecklist`), then walked each `coveredRequirements[]` → UseCase → Method → Implementation (`markerPending`, `tests[]`) → Test (`model.status`), and cross-checked `test/visual/` gate files + `git log`.
- Test `status` legend on disk: `pass`/`PASS`/`GREEN` = recorded green; `None`/`superseded` = not green.
- **Target = QA-Review** (NOT Done — Done is Tron's act). Gap categories: **(a) BUILD** no impl · **(b) MARKER** strict-AST flip (markerPending still true) · **(c) GATE** no gate GREEN · **(d) TWO-KEY** chain-complete-to-Test not both-directions-verified · **(e) BLOCKED-ON-TRON-DEVICE**.

---

## Sprint 37 — "Consistency by Construction" (unit `b86b53cc`, sprint status = Planned)

Sprint-37 task-UUIDs read from the sprint unit `model.tasks[]`; C-numbers mapped by task name.

| Task | uuid | model.status | checklist reached | Impl (markerPending) | Test (status) | Gate file | Real status | Needs → QA-Review |
|------|------|--------------|-------------------|----------------------|---------------|-----------|-------------|-------------------|
| C1 | 458b6b1c | Planned | Planned only | af97137f **false** | 3519018d pass, fc28b6f1 pass | rc1-sprintpin-resolver-gate.ts (+rc1refine) | chain-complete-to-Test, marker flipped, BITE gates GREEN | **(d) TWO-KEY** |
| C2 | 4bc1b3d5 | Planned | Planned only | b31ae393 **false** | — (Impl.tests EMPTY, no Test unit) | none | impl (generateAll) landed, reconcile-all PARTIAL, no Test | **(c) GATE + create Test** (test-cases missing; reconcile-all residual) |
| C3 | 364785b1 | Planned | Planned only | ee424581 **false** | caf74333 pass | rc3-consistency-guard-metabite.ts | chain-complete-to-Test, marker flipped, BITE GREEN | **(d) TWO-KEY** |
| C4 | 79fd2164 | Planned | Planned only | — (R-C4 has 0 UCs, no impl) | — | none | nothing on disk | **(a) BUILD** |
| C5 | 97e8a6ad | Planned | Planned only | d86f0309 **false** | 30d4b44a pass | rc5-taskstatus-bite-gate.ts | chain-complete-to-Test, marker flipped, BITE GREEN | **(d) TWO-KEY** |
| C6 | 32061171 | Planned | Planned only | 1f38e07e **false** | — (Impl.tests EMPTY, no Test unit) | none | impl (generateOverview) landed + marker placed, no Test | **(c) GATE + create Test** |
| C7 | bb31965b | In Progress | implementing [x], creating-test-cases [~], testing [ ] | 73f045d8 **false** | 0870c78b pass | rc7-migrate-board + rc7b/c/d/e/f (6 BITE gates) | chain-complete-to-Test, marker flipped, extensive BITE GREEN | **(d) TWO-KEY** (finish testing) |

**S37 header count:** near-QA (chain-complete-to-Test, only two-key left) = **4** (C1, C3, C5, C7) · need test+gate = **2** (C2, C6) · need build = **1** (C4).

### S37 per-task detail

- **C1 — "Pin is COMPUTED from files (never hand-set) [R-C1]"** (req `91486de1`). UC `6c016f6a` sprintPin.resolveFromFiles → Impl `af97137f` SprintPinResolver.resolveSprintPin (3-slot, number-keyed), markerPending=false. Two Tests pass: `3519018d` (frozen-excluded + cancelled-terminal refinement) + `fc28b6f1` (golden-pin + drift-invariance + ambiguity BITE). Gate `rc1-sprintpin-resolver-gate.ts` + META-BITE retrofit (commit `c9d18a7de`). **BOARD-vs-UNITS: board = Planned; units = implemented + tested + gated.** Only tester two-key remains.
- **C2 — "Board is a GENERATED view + one-time reconcile-all [R-C2]"** (req `eec7ebb7`). UC `bf1cf902` sprintBoard.reconcileAll → Impl `b31ae393` SprintViewGenerator.generateAll, markerPending=false, **Impl.tests EMPTY, NO Test unit, no gate file.** Commit `5b2630552` "reconcile-all … (partial; honest residual)" + `cf850d26e`. Suspect confirmed: generated-view half-done, reconcile-all migration is the residual. Needs a Test + gate GREEN (and the reconcile-all completion).
- **C3 — "FAIL-LOUD guard asserts pin==board==files [R-C3]"** (req `1530c79c`). UC `029574bd` guard.failClosedOnVacuous → Impl `ee424581` ConsistencyGuard.refuseIfVacuous, markerPending=false. Test `caf74333` pass (vacuous-path battery BITE). Gate `rc3-consistency-guard-metabite.ts`. **BOARD-vs-UNITS: board = Planned; units = chain-complete-to-Test.** Two-key remains.
- **C4 — "Objects self-heal (validate on init/read) [R-C4]"** (req `c8615e9f`). **Req has ZERO useCases, no Method, no Impl, no Test.** Genuine BUILD from scratch. Board = Planned is accurate here.
- **C5 — "Dual-status reconcile — one truth (status vs statusChecklist) [R-C5]"** (req `03fd79ff`). UC `2a840e93` taskStatus.deriveAndAssert → Impl `d86f0309` TaskStatus.assertStatusConsistent, markerPending=false. Test `30d4b44a` pass (fail-loud offender-naming BITE). Gate `rc5-taskstatus-bite-gate.ts`. **BOARD-vs-UNITS: board = Planned; units = chain-complete-to-Test.** Two-key remains. (R-C5 audit is the surface that keeps the live 5/6-Active-sprints drift visible — see pin commits `c0626c835`/`3e789db9e`.)
- **C6 — "sprints.overview.md is a GENERATED view (preserved-narrative region) [R-C6]"** (req `9339cc3b`). UC `833d3525` overview.generatePreserved → Impl `1f38e07e` SprintOverviewGenerator.generateOverview, markerPending=false, **Impl.tests EMPTY, NO Test unit, no gate file.** Commit `cf850d26e` "first --write of the generated sprints.overview.md index — FAIL→ok". Suspect confirmed: R-C6 write landed and marker IS placed; two-key NOT reachable because there is no Test unit and no gate yet. Needs Test + gate GREEN.
- **C7 — "Legacy boards MIGRATED to generated (zero loss) [R-C7]"** (req `6ccbef4e`). UC `c8c3d81e` board.migrateProvenComplete → Impl `73f045d8` BoardMigrator.applyMigration (proven-only, idempotent), markerPending=false. Test `0870c78b` pass (5-gate BITE: proveComplete-refuse + applyMigration). Six gate files present (`rc7`, `rc7b`–`rc7f`). Checklist: creating-test-cases [~], testing [ ] — the only S37 task past "Planned only". Needs tester two-key / finish testing to reach QA-Review.

---

## Sprint 40 — "Server Manager / Deployment / Mobile Input" (tasks T40.1–T40.12)

| Task | uuid | model.status | checklist reached | Impl (markerPending) | Test (status) | Gate file / GREEN | Real status | Needs → QA-Review |
|------|------|--------------|-------------------|----------------------|---------------|-------------------|-------------|-------------------|
| T40.1 | 7a956c21 | **QA Review** | testing [x] | 45853b02 false | c4f8a1d6 pass | r401-rc-link-gate.mjs GREEN DET-3x (7d5ae8d08) | **at QA-Review (awaiting Tron)** | AT QA-Review; residual owner-page ACs (AC-1 visible/fireable, AC-2 e2e isolation) = **(e) TRON DEVICE** |
| T40.2 | ce92294f | **QA Review** | testing [x] | 94ad4f50 false, fcd57103 false | e21b876d, 0172b45d, a3f9c1d7, e9b21f74 all pass | r402-deployment-node + r402root-server-tree | **at QA-Review (awaiting Tron)** | AT QA-Review (note: Impl 94ad4f50 shared with S36/R36.3 renderFacet) |
| T40.3 | d884d8d9 | **QA Review** | testing [x] | c3a56e56 false | f2c7b3e9 pass | r403a-keyboard-suppress + 2 more | **at QA-Review (awaiting Tron)** | AT QA-Review; input-accessory ABOVE-keyboard AC = **(e) TRON DEVICE** (real iOS) |
| T40.4 | ae0548ae | **QA Review** | testing [x] | e7fb7e65 false | d7e4b9a1 pass | r404-sprint-label-single-source-gate.mjs | **at QA-Review (awaiting Tron)** | AT QA-Review |
| T40.5 | a10c3329 | In Progress | refinement [x] only | ffd44b17 false (RIDES R34.7) | cbdb3210 pass — but it is **R34.7's** test, no R40.5-distinct test | (R34.7 reuse) | impl only rides reused RbDetailDrawer bar; no R40.5-distinct chain | **(a) BUILD** distinct dedup impl + distinct Test |
| T40.6 | 95d74272 | In Progress | implementing [~] | e009ace7 **TRUE** | — (empty) | none | build landed but marker still pending | **(b) MARKER** flip, then create Test + **(c) GATE** |
| T40.7 | b6e4a7dd | In Progress | implementing [x] | 6b4d7714 false (flipped 58fba2afc) | — (empty, no Test unit) | none (regression gate R40.19 is BACKLOG, not run) | chain-complete-to-Impl; no Test yet | **create Test + (c) GATE** |
| T40.8 | b0be0668 | In Progress | implementing [x] | 3ee03bde false (flipped a0b84ca91) | — (empty, no Test unit) | none | chain-complete-to-Impl; no Test yet | **create Test + (c) GATE** |
| T40.9 | cc3fa868 | In Progress | implementing [x] | 0ed5cd75 false (flipped af6f0512a) | — (empty, no Test unit) | none | chain-complete-to-Impl; no Test yet | **create Test + (c) GATE** |
| T40.10 | 9a70ce5e | In Progress | implementing [x] | 36b6ce2e false (flipped 6cb12fb1d) | — (empty, no Test unit) | none | chain-complete-to-Impl, built+live (v0.8.73); no Test/device gate | **(c) GATE** (item-zero device gate) + create Test |
| T40.11 | 6e3cc1b2 | Planned | Planned only | — (**UC `249fdab6` MISSING on disk**) | — | (carve-out r4011-carveout-unresolved-gate.mjs GREEN, a339ea395 — DEBT slice only) | task chain NOT built (UC unresolvable) | **(a) BUILD** |
| T40.12 | 241a2be3 | **Planned** | Planned only | aba7b795 false (flipped, commit `R40.12 strict-AST FLIP`) | b3e9f1a4 pass | r4012-music-player-pixel-gate.mjs GREEN DET-3x (a339ea395), shipped 0.8.75 device-verified | **chain-complete-to-Test, marker flipped, gate GREEN + device-verified** | **(d) TWO-KEY + BOARD-FLIP** (board Planned is badly stale) |

**S40 header count:** at QA-Review (awaiting Tron) = **4** (T40.1–T40.4) · near-QA needing only two-key/board-flip = **1** (T40.12) · chain-complete-to-Impl needing Test+gate = **4** (T40.7, T40.8, T40.9, T40.10) · marker-pending = **1** (T40.6) · build = **2** (T40.5, T40.11).

### S40 per-task detail

- **T40.1 — Open Claude.ai RC action (per-pane deep link)** (req `caab6d86`). UC `350ab353` pane.resolveRcLink → Impl `45853b02` markerPending=false → Test `c4f8a1d6` pass, gate `r401-rc-link-gate.mjs` GREEN DET-3x. Test description is an HONEST split: PROVEN by-construction (owner-gated 403 no-leak across 4 vectors + fail-closed, no synthesised URL) but AC-1 (button visible/fireable on real /server-manager owner page) and AC-2 (server-side pane→agent isolation end-to-end) are NOT PROVEN — need the owner page no non-owner identity can load. **Unit status = QA Review → at QA-Review; the two owner-page ACs are Tron-device residual.**
- **T40.2 — WODA.prod as UML deployment Node** (req `adab1bb5`). UC `b9a549e4` → 2 Impls markerPending=false (`94ad4f50` DiagramViewModel.renderFacet [shared with S36/R36.3], `fcd57103` OtmuxBridge.buildRootedTree). 4 Tests pass. Gates `r402-*`. **At QA-Review (awaiting Tron).**
- **T40.3 — Suppress OS keyboard + configurable Keyboard Controller** (req `bfe97d61`). UC `9d1225a4` → Impl `c3a56e56` markerPending=false → Test `f2c7b3e9` pass, gates `r403a-*`. **At QA-Review;** the hard input-accessory-above-keyboard AC is real-iOS device-only (Tron residual; see BACKLOG R40.20).
- **T40.4 — Sprint labels show the number (composed at display)** (req `9a8cbffe`). UC `d6cb7ddd` → Impl `e7fb7e65` markerPending=false → Test `d7e4b9a1` pass (single-source invariant), gate `r404-*`. **At QA-Review (awaiting Tron).**
- **T40.5 — Detail/feature EXTRA buttons de-duplicated onto shared action bar** (req `e152177d`). UC `1c21d43a` → Impl `ffd44b17` markerPending=false, BUT it is `RbDetailDrawer.universalActionBar` and its only Test `cbdb3210` is **R34.7's** ("test:R34.7 (R-E) universal action bar"). No R40.5-distinct-intent impl/test. Checklist: only refinement [x] (creating-test-cases/implementing/testing all [ ]). Commit `1440c5c9a` moved it Planned→In-Progress "build-ready". **BOARD-vs-UNITS: board = In Progress; units = only refinement done, riding a reused bar → needs a distinct BUILD + Test.**
- **T40.6 — deploymentRefs → real typed OOP model** (req `6a9d99c3`). UC `b2c5cdba` → Impl `e009ace7` DeploymentModel.buildTypedModel, **markerPending = TRUE (the only S40 impl still pending)**, tests empty. Build landed (commit `34d297d91`, 12 M2 type sentinels + IS-A/implements relationship edges) but the strict-AST marker has NOT been flipped and no Test exists. Also noted parked on the 5-Active-sprints pin ambiguity (commit `fa4588c7e`). **Needs marker flip → Test → gate.**
- **T40.7 — Back = history.back(); path label navigates to folder** (req `6ce80195`). UC `5d02d562` → Impl `6b4d7714` historyBack markerPending=false (flipped `58fba2afc`, also pathLabelNav `197054f9` in rb-editor-toolbar). **No Test unit yet** ("Tests at gate"). The regression gate R40.19 is captured to BACKLOG only. Chain-complete-to-Impl → **needs Test + gate GREEN.**
- **T40.8 — 'Files' shows the REAL on-disk path** (req `90cc7bab`). UC `98df6abf` → Impl `3ee03bde` ServerManagerApi.unitRealPath markerPending=false (flipped `a0b84ca91`, real shard path via ScenarioIndex.filePath, fail-closed 404, owner-gate). **No Test unit yet** → needs Test + gate GREEN.
- **T40.9 — 'Preview' = traceability chain + details drawer (reuse /trace + rb-detail-drawer)** (req `50753cf6`). UC `23af7ba9` → Impl `0ed5cd75` previewTraceability markerPending=false (flipped `af6f0512a`; repointed onto RbEditorLayout, duplicate EditorPreview deleted per `3c17b03e3`). **No Test unit yet** → needs Test + gate GREEN.
- **T40.10 — Tron QA verdict from the task: Approve/Decline** (req `33451271`). UC `0a3e3653` → Impl `36b6ce2e` approveByOwner markerPending=false (flipped `6cb12fb1d`; also declineToChangeRequest `90089602`), served v0.8.73, owner-403 + cannot-manufacture-Done verified. **No Test unit yet**, no device gate. Suspect confirmed: built+live, needs the **item-zero device gate** (SM commit `98fa79fd4` names item-zero = R40.10 device-gate) + Test. → needs GATE (device) + Test.
- **T40.11 — deploymentRefs are scenario-first units with default views (fix permanent-Loading drawer)** (req `83528e2f`). **Its UC `249fdab6` does NOT exist on disk (directory `scenario/index/2/4/9/f/d/` absent) — no Impl, no Test.** Task status = Planned. Commits show only a CARVE-OUT fail-loud slice shipped (`2cf8f9866` v0.8.76, gate `r4011-carveout-unresolved-gate.mjs` GREEN) explicitly logged as NAMED DEBT (`ead6ed390`). **BOARD-vs-UNITS: commits/gate imply progress, but the task's own chain is unbuilt (UC unresolvable) → BUILD.**
- **T40.12 — File detail renders working type-appropriate preview (audio-player regression fix)** (req `c9fa4f49`). UC `16b3a2ef` fileDetail.autoRenderMediaPreview → Impl `aba7b795` markerPending=false (strict-AST FLIPPED, `[impl:aba7b795]` heads RbFileDetail.autoRenderMediaPreview @rb-file-detail.ts) → Test `b3e9f1a4` pass, gate `r4012-music-player-pixel-gate.mjs` GREEN DET-3x, shipped 0.8.75 **device-verified** (`a339ea395`). **BOARD-vs-UNITS: board/task = Planned (badly stale); units = impl marked + Test pass + device-verified gate GREEN.** Suspect confirmed (SHIPPED + pixel-verified). Only tester TWO-KEY ("tester two-keys next") + the board-flip to QA-Review remain — the single ripest S40 task.

