# R40.81 Slice-3 PRE-FLIP — full store-surface enumeration (architect, 2026-09-06)

Re-verdict after the RED (03eed6cc7) was fixed (v0.8.197, 24912d838: A1 client de-named 1→0, A2 route-refusal proven by PUT 200→403 differential, A3 server token-scan 0). The PO asked for a 5th evasion class. I enumerated EVERY path that can reach the model store (writes AND reads), measured across server + client + scripts (125 store-refs scanned, node-based to dodge the grep glitch). **Verdict: the LIVE request surface is CLOSED. One residual, explicitly OFF the request/editing path, named below. GREEN to flip.**

## WRITE surfaces (every model-store write must go through ModelStoreLocator)
| Surface | Status |
|---|---|
| 6 original mint sites + move-view(3068)/zoom(3093)/add-view read(2988) | **COUPLED** — all `path.join(ModelStoreLocator.modelDir()/dirFor(),…)` (verified per line) |
| FolderService.createPhysicalWithUnit / mintRealUnit | **COUPLED** — only 2 callers (3142/3144), both pass `ModelStoreLocator.modelDir()`; no caller passes a hardcoded store |
| `/api/files` raw PUT (the old RED hole) | **CLOSED** — 3699 refuses `data/model-store/` (403, proven by 200→403 differential) |
| `/api/files/model-unit/<uuid>` (the ✎ Edit save) | **COUPLED** — 3700-3705 resolves the shard via `ModelStoreLocator.dirFor(mu)`; the rewritten `relPath` is used by BOTH the PUT-save (3720) and the GET-load (3729+) → edit read+write track the same store |
| generate-project HTTP handler (2933/2952) | **COUPLED** — output dir = `ModelStoreLocator.modelDir()` |
| usage-index.json (1587) | **COUPLED** — `usageIndexPath()` is locator-derived; read+write both use it (self-consistent) |
| profiles/devices/reports/pairing writes (363/648/846/4883) | **N/A** — not model units, different data |
| room file-unit parent writes (2655/2713/2811) | **N/A** — NOT model-store (absent from the 125 store-ref scan; room files live in prod scenario/index, `isModelUnit=false` = never flag-toggled) |
| federation import (`federationImport` 610, `/api/federation/import` 2536) | **N/A** — writes scenario/room units in prod, never model-store (absent from the store-ref scan) |
| upload / NativeFileIngress | **N/A** — room files → prod, not model-store |

## READ surfaces (reverse direction — reading model-store directly post-flip = stale render)
- All model READ sites were collapsed into `resolveReadDir`/`modelReadDir` → `ModelStoreLocator` in Slice-2, and **Slice-2 PAIR-2 GREEN (213/213) is the positive transparency proof** that reads answer identically from either store = reads are locator-routed. The isModelUnit shard read (65), the add-view validation read (2988) all go via the locator.
- No CLIENT hardcodes a model-store READ url: the only client store-literal was rb-detail-drawer.ts:130 (fixed → `modelUnitEditorHref`); all other client `/md/…` links target `scenario/index` (prod). The ✎ Edit GET routes via the `/api/files/model-unit/` locator branch.

## Symlinks / runtime paths
- migrate-folder-units.mjs replaced some model-store folder-unit files with symlinks → scenario/index: a MITIGATION (those reads/writes resolve to the live store), not a new hazard.
- All runtime-constructed store paths derive from `ModelStoreLocator.modelDir()/dirFor()`; the only literal binding of `data/model-store/index` is the const at server.ts:123, owned by the locator.

## ★ The one RESIDUAL (named, honesty-both-ways) — NOT on the request/editing path
**Offline CLI scripts hardcode `data/model-store`:** `scripts/regen-model.ts:19` + `scripts/generate-project` via CLI (and migrate-*.mjs, which are the migration tools themselves). Post-flip these write the FROZEN model-store, so their output would be invisible to live reads. **Why it is NOT flip-blocking:** these are deliberate OPERATOR CLIs, not a request/user surface — they are not reachable by Tron's editing (the flip's stated risk), and r4081d correctly scans the server request path, not offline tools. **Tripwire + action:** do not run the model-regen/generate CLIs post-flip expecting a live effect; update them to the locator (or retire) at model-store deletion. This is a completeness gap in the tooling, bounded and off the live path — I name it so the flip decision is made with eyes open, but it does not gate the flip.

## Verdict
**GREEN to flip.** No 5th evasion class on the live request surface: every model-store WRITE is locator-routed or refused; the ✎ Edit read+write are coupled through the locator; non-model writes don't touch the store; reads are locator-routed (PAIR-2-proven). The RED I raised is closed with a real differential. The sole residual (offline CLI scripts) is off the request/editing path, bounded, and named with a tripwire. Tron's editing surface is split-brain-safe. Flip is safe; keep the (b) bounded-window conditions (frozen model-store, recovery procedure pre-flip, bounded exit) in force.
