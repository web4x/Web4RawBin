# T40.1 — five CRs on-disk state + shortest path to Done (facts, measured HEAD a844fb551 v0.8.127)

Measured on disk. No new units, no audits. T40.1 = `7a956c21`.

## The 5 CRs — ALL status=Open on disk

| CR | # | demands | shipped code? | owed to close |
|----|---|---------|---------------|---------------|
| `4babebb1` | unnumbered (on T40.1) | decline DRIVES action; CR re-parented Task→Test; renders under Test | **YES** — `declineToChangeRequest` (server.ts:1616) mints+reopens via seam; ownerIor→Test + task-backref verified | render-under-test verify + formal RESOLVE |
| `7286d45a` | #86-2 | task with open CR shows a `processing change requests` sub-step that BLOCKS Done | **YES, SHIPPED v0.8.127** (a844fb551) — derived-on-read: task-status.ts:43 an open CR sub-step ⇒ status `QA-Review-with-open-CR` (not Done) | ⚠ T40.1's checklist NOT yet MIGRATED to carry the sub-step (its 5 CRs predate R40.59) + gate + RESOLVE |
| `c27ae455` | #86-3 | Set-as-Current demotes prev→NEXT | **PARTIAL** — designatedCurrent override exists (CurrentSprint.ts:272); demote path present in comment, unverified | verify demote wired + gate + RESOLVE |
| `18ebe066` | #86-4 | pin auto-advances on QA + rollup-parent kills coord-root | **PARTIAL** — auto-advance built (T40.18 v0.8.95); `rollupParentStatus` FUNCTION exists (task-status.ts:47) BUT T37.4 still stored `Planned` = not deriving through it | wire rollup into the parent read-path + gate + RESOLVE |
| `461d5db6` | #86-5 | status DERIVED, never a written literal | **YES** — 794c8c23a; no direct `m.status=`, UnitController sole writer (check:status-writes gate) | gate/Test + RESOLVE |

## ★ THE BLOCKING GAP (measured): no CR-close mechanism exists

`grep resolveChangeRequest|closeChangeRequest` in src/ts = **0**. A CR can be MINTED (Open, `declineToChangeRequest`) but there is NO shipped path to RESOLVE it. The processing-CR sub-step ticks only when CRs resolve ⇒ **without a resolve path, T40.1 can NEVER reach Done.** This is the #1 owed item.

## Is T40.1's QA-Review-with-Done-unticked HONEST?

- **Done unticked = HONEST** (Tron has not approved; correct — QA-Review holds for him).
- **BUT the bare `QA Review [x]` with NO processing-CR sub-step, while 5 CRs are OPEN, is NOT yet honest/enforced.** #86-2's whole point — an open CR BLOCKS Done by construction — is SHIPPED as a mechanism but NOT APPLIED to T40.1 (its checklist predates R40.59). So today the by-construction block is INACTIVE on this unit: nothing structural stops a Done-tick despite 5 open CRs. It is residual pre-R40.59 state needing migration to become honest.

## Shortest path to T40.1 Done (order matters)

1. **Build `resolveChangeRequest`** — the #1 gap; nothing closes without it (expert).
2. **Migrate T40.1's checklist** to carry `- [ ] processing change requests` via the seam ⇒ T40.1 derives `QA-Review-with-open-CR`, Done blocked by construction, board honestly shows the open CRs (skill-expert single-writer).
3. **Close the 5 CRs** — mechanics are mostly built (#86-2 shipped, #86-5 built, drive-action built, #86-4 auto-advance built); remaining code = #86-4 wire rollup into parent read-path, #86-3 verify demote; then each CR → Resolved. As CRs resolve, the sub-step ticks `[x]` and T40.1 derives back to plain `QA Review`.
4. **Tron judges** — Approve (records approvedBy/approvedAt) = QA-Review→Done, plus any device-@390 ACs on T40.1. Tron's approval is the LAST hop and the only one that writes Done.

**What stands between Tron and T40.1 Done:** (1) a CR-resolve mechanism that does not exist yet, (2) migrate T40.1 to carry the block, (3) close all 5 CRs, (4) his Approve. Steps 1–3 are ours; step 4 is his.
