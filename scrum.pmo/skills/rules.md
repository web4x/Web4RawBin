# Rules — Object.verb skill

> GENERATED by `objectVerb emitDocs` from `skill-classes.ts` — do not edit. The class IS the doc.

| Verb | Params | Description |
|------|--------|-------------|
| `rules.list` | — | List all team protocol rules (rule-*, ship-*, verify-*) with their one-line hooks |
| `rules.show` | `<name:string>` | Show one rule's full text by name (Tab-completes over all rules) |

## Invocation
```bash
taskChain rules.<verb> [args]                      # OOSH (Tab-completes)
npx tsx scripts/objectVerb.ts Rules <verb> [args]    # direct
```

Logic: `src/ts/scenario/skill-classes.ts` → `class Rules`. Dispatcher: `scripts/objectVerb.ts`.
