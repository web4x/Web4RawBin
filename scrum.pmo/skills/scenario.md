# Scenario — Object.verb skill

> GENERATED by `objectVerb emitDocs` from `skill-classes.ts` — do not edit. The class IS the doc.

| Verb | Params | Description |
|------|--------|-------------|
| `scenario.captureQuote` | `<text:string>` `<sprintIor:string>` `<?taskIor:string>` | Capture a verbatim Tron quote as a scenario unit linked to its sprint (and optional task) |
| `scenario.proposeTask` | `<requirementIor:string>` `<name:string>` `<description:string>` `<sprintIor:string>` `<?assigned:string>` `<?effort:string>` | Propose a new Task unit under a requirement (name + description + sprint) |
| `scenario.walkChain` | `<startIor:string>` `<?direction:string>` `<?maxDepth:number>` | Walk the chain from a unit (down/up/both, depth-limited) — returns flat steps |
| `scenario.statusTransition` | `<taskIor:string>` `<verb:string>` `<?tronCommitRef:string>` | Transition a Task through its FSM (startRefinement|startCreatingTestCases|startImplementing|startTesting|requestQAReview|tronApprove) |

## Invocation
```bash
taskChain scenario.<verb> [args]                      # OOSH (Tab-completes)
npx tsx scripts/objectVerb.ts Scenario <verb> [args]    # direct
```

Logic: `src/ts/scenario/skill-classes.ts` → `class Scenario`. Dispatcher: `scripts/objectVerb.ts`.

## Laws enforced (team-laws.md)
- **L1** DATA ON DISK: scenario units = sole truth, persisted via put()
- **L2** MARKDOWN IS VIEW: units are source, .md is generated
- **L12** IDEMPOTENT GENERATORS: regenerate-views re-runnable
