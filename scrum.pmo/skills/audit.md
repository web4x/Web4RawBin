# Audit — Object.verb skill

> GENERATED by `objectVerb emitDocs` from `skill-classes.ts` — do not edit. The class IS the doc.

| Verb | Params | Description |
|------|--------|-------------|
| `audit.strict` | — | Structural trace audit, strict mode (orphans, back-refs, cardinality) — dispatches trace:audit:strict |
| `audit.rulePair` | — | Ship rule-pair gate #66/#67 (version bump + STATIC_SHELL) — dispatches rule-pair:strict |
| `audit.sprintMd` | — | Sprint markdown consistency check — dispatches check:sprint-md |

## Invocation
```bash
taskChain audit.<verb> [args]                      # OOSH (Tab-completes)
npx tsx scripts/objectVerb.ts Audit <verb> [args]    # direct
```

Logic: `src/ts/scenario/skill-classes.ts` → `class Audit`. Dispatcher: `scripts/objectVerb.ts`.

## Laws enforced (team-laws.md)
- **L3** GIT=BACKUP: ci:gates commit-gated, no tar
- **L5** TEAM PROVES: audit gates = team's proof infrastructure
- **L7** SOURCE-VERIFY: strict-marker-audit reads AST, not string-match
