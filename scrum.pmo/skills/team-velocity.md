# team-velocity — SUPERSEDED

> Migrated to Object.verb. See generated [`velocity.md`](./velocity.md) — the class IS the doc.

- Verb: `taskChain velocity.dashboard` (OOSH, Tab-completes) or `npx tsx scripts/objectVerb.ts Velocity dashboard`
- Logic: `src/ts/scenario/skill-classes.ts` → `class Velocity`
- Legacy CLI `scripts/team-velocity.ts` still works (thin shim, same output).
