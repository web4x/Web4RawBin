# Chain — Object.verb skill

> GENERATED by `objectVerb emitDocs` from `skill-classes.ts` — do not edit. The class IS the doc.

| Verb | Params | Description |
|------|--------|-------------|
| `chain.resolvePrefix` | `<prefix:string>` | Resolve a bare UUID prefix to the full UUID in the index |
| `chain.followUp` | `<reqUuids:string[]>` `<?sprint:string>` | Canonical chain completion — one summary row per Requirement (the ONLY completion measure) |
| `chain.listComplete` | `<?sprint:string>` | List the COMPLETE chain set (one diffable line per complete requirement) |
| `chain.scoreboard` | `<reqUuids:string[]>` `<?sprint:string>` | Render the canonical scoreboard (table + dispatch list + Summary line) as markdown |
| `chain.wireImplNode` | `<methodUuid:string>` `<?dryRun:boolean>` | Wire Method→Impl→Test node: create Impl unit, move Method.tests[] to Impl (idempotent) |
| `chain.wireAllMissing` | `<?dryRun:boolean>` | Wire ALL Methods that have no Implementation node (batch wireImplNode) |
| `chain.generateMatrix` | `<matrixPath:string>` `<?sprint:string>` | Regenerate the full traceability matrix from canonical chain data |
| `chain.updateMatrixRow` | `<reqUuid:string>` `<matrixPath:string>` | Update one requirement's row in the traceability matrix |
| `chain.lintMarkers` | — | Lint chain markers: invented-suffix uuids, prefix collisions, shared Impls, orphan markers (catch BEFORE a re-measure) |
| `chain.snapshotComplete` | `<?dir:string>` | Write a dated COMPLETE-set snapshot and name exactly which chains flipped vs the previous snapshot |

## Invocation
```bash
taskChain chain.<verb> [args]                      # OOSH (Tab-completes)
npx tsx scripts/objectVerb.ts Chain <verb> [args]    # direct
```

Logic: `src/ts/scenario/skill-classes.ts` → `class Chain`. Dispatcher: `scripts/objectVerb.ts`.
