# Velocity — Object.verb skill

> GENERATED by `objectVerb emitDocs` from `skill-classes.ts` — do not edit. The class IS the doc.

| Verb | Params | Description |
|------|--------|-------------|
| `velocity.compute` | `<since:string>` `<?sprint:string>` | Chain completion + git throughput for a window (numerator = canonical Chain.followUp) |
| `velocity.dashboard` | `<?since:string>` `<?hours:number>` `<?sprint:string>` | Team velocity dashboard (markdown) — window via since (ISO) or hours (number), default last 24h |

## Invocation
```bash
taskChain velocity.<verb> [args]                      # OOSH (Tab-completes)
npx tsx scripts/objectVerb.ts Velocity <verb> [args]    # direct
```

Logic: `src/ts/scenario/skill-classes.ts` → `class Velocity`. Dispatcher: `scripts/objectVerb.ts`.
