# R40.81 PAIR-2 DECISIVE — RED→GREEN CLOSED (robbin-tester, 2026-09-06)

## ✅ RESOLVED — PAIR-2-on-fix = GREEN (SLICE2_COMMIT=a4e2a2511 v0.8.195, scoped-resolver fix)
Ran ONCE on the FIX commit (not a re-run of the RED). Matches the PRE-STATED expectation EXACTLY (architect + tester locked it before running → no rationalization):
- **flag-liveness PROVEN** (PRE='SENTINEL-MODELSTORE' ≠ POST='SENTINEL-SCENARIOINDEX') → the flag still flips the read → GREEN is a TRUE transparency result, not a no-op false-green.
- isolation proven both sides; mof-m1 non-empty.
- **mof-m1 PRE=3 POST=3** (was 3→4 in the RED; `project:model` GONE) · **authoredListings identical** · **CHANGED = 0 → 213/213 byte-identical.**
- Clean differential: RED @4e3427949 (mof-m1 3→4, extra project:model) → GREEN @a4e2a2511 (3==3, none) = the scope-to-isModelUnit fix works.
- **Verdict:** the repoint is byte-transparent — every unit answers identical whether the resolver reads MODEL_STORE or scenario/index. This is the decisive proof Slice-3 deletion rests on.
- **Binding on GREEN:** unblocks Slice-3 but authorizes NO deletion — relocate→verify→THEN remove; data/model-store STAYS until Slice-3 readers repoint + re-verify. Default stays model-store.

---
## (original RED finding, retained for provenance)
# R40.81 PAIR-2 DECISIVE — RED (robbin-tester, measured 2026-09-06)

**Slice-2 repoint is NOT byte-transparent.** Runner `test/visual/r4081c-pair2-run.mjs`, SLICE2_COMMIT=4e3427949 (v0.8.194 MODEL_READ_SOURCE resolver). ONE build booted twice, same tar-seeded MODEL_STORE + same scenario/index, only the flag flipped.

## This is a REAL verdict, not a confound
- **Flag-liveness POSITIVE CONTROL PASSED:** PRE(model-store) sentinel='SENTINEL-MODELSTORE' ≠ POST(scenario-index)='SENTINEL-SCENARIOINDEX', exactly as contracted → the flag DEMONSTRABLY flips the read source → a diff is a TRUE transparency signal, not a silent no-op.
- **Isolation PROVEN** both sides (scratch inode≠live, nlink=1 — pinned tar, no hardlink passthrough).
- **mof-m1 NON-EMPTY** both sides (PRE=3, POST=4) — not the PAIR-1 instrument gap.
- **Sentinel RULED OUT:** facade99 is NOT in either mof-m1 list → the delta is not my positive-control artifact.

## The delta — exactly ONE ref of the canonical 213-set
- 212/213 byte-identical; `authoredListings` identical; **1 CHANGED: `mof-m1` childCount 3→4.**
- PRE (model-store, 3): `project:RawBin` · `project:test/fixtures/r32.2-sample.ts` · `project:test/fixtures/r325-golive-drop.ts`
- POST (scenario-index, 4): the same 3 **PLUS `project:model`** ← only under scenario-index.

## Suspected root (architect to confirm — this is a MODEL question, R12)
scenario/index is the FULL store (6145 units incl native scenario units: reqs/tasks/tests…); model-store held only the ~702 model-tree subset. The collapsed resolver aggregating `mof-m1` over scenario/index picks up native-scenario M1 unit(s) whose sourceFile maps to `project:model` — units model-store never contained → the model tree GAINS a top-level project post-repoint. A /model user would see a new node.

## PO independent corroboration (2026-09-06)
- `project:model` is **NOT a stored unit anywhere** (no file contains it) = it is a **DERIVED aggregation node** (a project bucket the model-tree render synthesises from units' sourceFile). Confirms: this is the render aggregating over the FULL store, not a misplaced physical unit.
- Store sizes (PO measured, live): **scenario/index = 6825 units** vs **data/model-store = 786**. Reading /model off the full 6825-store surfaces a top-level project the 786-unit model-tree subset never had — exactly the (a)/(b) fork above, now with numbers.

## STANDING ORDER — DO NOT RE-RUN THIS GATE (PO)
The RED is CORRECT. Re-running a correct gate hoping for green is how a false green gets manufactured — DO NOT retry. The next action is the ARCHITECT's ruling (fix → then I re-run PAIR-2 on the fix commit; or accept → sanctioned canonical-213 update). Architect 0.3 is mid-rewind; its fresh instance boots with 'backstop the PAIR-2 verdict' standing — this file is its cold read.

## Binding (PO-affirmed)
- **RED = STOP. DO NOT DELETE the source store. data/model-store STAYS. default stays model-store (nothing flips for Tron).** Slice-3 NOT unblocked.
- This RED is a SUCCESS of the gate — it caught a non-transparency BEFORE source deletion.
- Decision needed (architect/Tron): is `project:model` (a) a real regression → the resolver must scope `mof-m1` to model-tree units, not the whole scenario/index; or (b) correct and model-store was INCOMPLETE → then the 213-baseline needs a sanctioned update + `project:model` is a known-accepted delta. Measure+state+ASK; do not self-align.
