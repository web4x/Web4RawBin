node_modules/
data/rooms/
data/users/
.env
web4rawbin-pre-T118-backup-*.tar.gz
data/migration/

# runtime data aggregates (consistent with data/users,rooms,migration already ignored)
data/profiles.json
data/devices.json
data/repos.json
data/*.bak
data/model-store/
data/token-storage-map.json
# verification screenshots (binary churn; .mjs scripts ARE committed)
test/visual/*.png
package-lock.json
data/users-staging/
# gate RUN-OUTPUT, not baselines (tester classified: every ref is a WRITE — screenshot/mkdir/writeFileSync;
# case-r3013 PNGs + profile-reference-390.png are gate-WRITTEN output; r4019/STAMP.json is ad-hoc). Untrack;
# durable gate/freshness records belong in a scenario UNIT (record-gates), not committed JSON. Files stay on disk.
test-results/
data/logs/
