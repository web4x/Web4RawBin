---
name: rawbin-chain
description: Web4RawBin traceability chain operations — the CANONICAL completion measure. Use when measuring chain completion (scoreboard/followUp), listing or diffing the COMPLETE set (listComplete/snapshotComplete), wiring Method-Impl-Test nodes (wireImplNode/wireAllMissing), linting markers for invented-suffix/shared-impl violations (lintMarkers), or regenerating the traceability matrix (generateMatrix).
---

# rawbin-chain (Object.verb)

> GENERATED by `objectVerb emitClaudeSkills` from `src/ts/scenario/skill-classes.ts` — do not edit; edit the class and re-emit.

## Invocation

```bash
npx tsx scripts/objectVerb.ts Chain <verb> [positional...] [--param value]   # from repo root
taskChain chain.<verb> [args]                                  # OOSH shells (Tab-completes)
```

Repo: /Users/Shared/Workspaces/2cuGitHub/Web4RawBin. Run from repo root (or cd there) so the scenario index resolves.

## Verbs

| Verb | Params | Description |
|------|--------|-------------|
| resolvePrefix | `<prefix:string>` | Resolve a bare UUID prefix to the full UUID in the index |
| followUp | `<reqUuids:string[]>` `<?sprint:string>` | Canonical chain completion — one summary row per Requirement (the ONLY completion measure) |
| listComplete | `<?sprint:string>` | List the COMPLETE chain set (one diffable line per complete requirement) |
| scoreboard | `<reqUuids:string[]>` `<?sprint:string>` | Render the canonical scoreboard (table + dispatch list + Summary line) as markdown |
| wireImplNode | `<methodUuid:string>` `<?dryRun:boolean>` | Wire Method→Impl→Test node: create Impl unit, move Method.tests[] to Impl (idempotent) |
| wireAllMissing | `<?dryRun:boolean>` | Wire ALL Methods that have no Implementation node (batch wireImplNode) |
| generateMatrix | `<matrixPath:string>` `<?sprint:string>` | Regenerate the full traceability matrix from canonical chain data |
| updateMatrixRow | `<reqUuid:string>` `<matrixPath:string>` | Update one requirement's row in the traceability matrix |
| lintMarkers | — | Lint chain markers: invented-suffix uuids, prefix collisions, shared Impls, orphan markers (catch BEFORE a re-measure) |
| snapshotComplete | `<?dir:string>` | Write a dated COMPLETE-set snapshot and name exactly which chains flipped vs the previous snapshot |

## Rules

- ONE canonical completion measure: Chain.followUp — never produce a competing count.
- HARD RULE: marker uuid = uuidgen-fresh OR verbatim 36-char copy. One marker = one unit = one method.
- Logic lives in the class; flags are forbidden as skill surface — verbs are methods.
