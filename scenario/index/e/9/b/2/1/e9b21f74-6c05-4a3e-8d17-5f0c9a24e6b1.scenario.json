{
  "ior": "ior:class:Test",
  "model": {
    "uuid": "e9b21f74-6c05-4a3e-8d17-5f0c9a24e6b1",
    "name": "test:R40.2 deployment-node render contract (rides renderFacet 94ad4f50 EXTENSION, distinct-intent)",
    "description": "Bridged to test/visual/r402-deployment-node-gate.ts (r402, GREEN DET-3x, 394f5e605). DISTINCT-INTENT ride ALONGSIDE the renderFacet owners (R36.2 e21b876d + R36.3 0172b45d, UNTOUCHED): certifies the deployment-NODE render contract — WODA.prod M1 renders as a UML deployment-node (3D box / <<node>> stereotype) via the renderFacet node-branch (not a fork), its 4 deploymentRefs render, childrenLens rides live readSessionTree, and INV-T tree byte-diff==0 (no persisted otmux mirror).",
    "sourceFile": "ior:file:test/visual/r402-deployment-node-gate.ts",
    "implementations": [
      "ior:instance:94ad4f50-aac8-4de5-ad34-e6776fc4b938"
    ],
    "status": "pass",
    "unitLinks": [],
    "certificationScope": {
      "proven_diagram_surface": "renderNodeFacet riding 94ad4f50 draws the «node» 3D box with name + the 4 MEASURED refs as compartment rows (sshd_config / SSH-identity / .env-domain / LE-cert); children+methods NOT mirrored into the facet; deterministic render; INV-T byte-diff==0; stub-must-fail (empty -> 0 rows) + drift-injection (refs -> rows).",
      "not_proven_pending_server_manager_root": "SHRUNK to exactly TWO, no open defects: (1) [REPLICATED] composition — the live re-rooted tree awaiting the experts buildRootedTree EXTRACT (then the distinct LOUD-degradation Test wires to that Impl, tester places the marker); (2) VISUAL @390 render on the owner page (Tron owner-device).",
      "owner_page": "batched into the PO Tron owner-QA list.",
      "BORROWED_loud_degradation_MOVE_OUT_at_buildRootedTree_extract": "FAIL-OPEN now LOUD (r402root-server-tree-gate.ts, GREEN DET-3x, HEAD==served 0.8.69, 1dc1a469b): BOTH degradation paths (else: node missing/not-a-node · catch: exception) emit a server WARN + a VISIBLE type:notice ⚠ row NAMING the uuid, flat sessions still WRAPPED so availability survives; plant-missing -> notice (not silent flat); stub-must-fail proven (strip the notice -> RED to the 0.8.67 silent baseline). This closes Trons original silent-fall-back complaint. ⚠ DISTINCT INTENT (failure-announces-itself vs e9b21f74 happy-path-composes) -> gets its OWN Test at the buildRootedTree extract (below); recorded here as source-proven until the clean Impl exists (R5: no invented uuid, R4: no riding a non-existent Impl). ⚠⚠ MOVE-NOT-COPY (PO): this entry is BORROWED here TEMPORARILY (proof recorded while the clean Impl catches up). When buildRootedTree is extracted + the DISTINCT graceful-degradation-is-LOUD Test is minted riding it, DELETE THIS ENTRY from e9b21f74 entirely — the proof MOVES to the new Test, it must NOT live in both (two Tests on one behaviour = indistinguishable from double-credit to a later auditor). e9b21f74 then certifies ONLY the render-contract; R40.2 pending shrinks to ONE (Tron visual @390)."
    }
  },
  "ownerIor": "ior:instance:94ad4f50-aac8-4de5-ad34-e6776fc4b938"
}