{
 "ior": "ior:class:Requirement",
 "model": {
  "uuid": "c5869b0a-8592-4d86-9250-9034a75e137c",
  "altId": "R30.40",
  "name": "Center Result header shows the targeted repo's ACTUAL current branch, resolved dynamically",
  "description": "The center Result header (filename@branch) MUST show the ACTUAL current checked-out branch of the repo the diff TARGETS, resolved dynamically at request time - never a stale/other-clone/hardcoded branch. BUG (v0.7.61, Tron screenshot): Tron's working repo is on branch 'mcdonges.latest' but the center header reads 'otmux@dev-teampush-astray'. ROOT (expert-measured, emerging): the server's 'oosh' RepoRegistry key resolves to a DIFFERENT clone (the EAMD clone at /home/shared/EAMD.ucp/.../Once.sh/dev, whose HEAD=dev-teampush-astray) than the user's working clone (/root/oosh, mcdonges.latest) - so GitApi.currentBranch returns that other clone's HEAD. AC aligns to the confirmed root once expert posts it. crossRef R30.38 (centerHeaderBranch + currentBranch chain) + R30.6.7 (RepoRegistry clone resolution).",
  "tronQuote": "> TRON (v0.7.61, screenshot): the center Result header must show the CORRECT current branch of the selected repo - my git status is 'mcdonges.latest' but center shows 'otmux@dev-teampush-astray' which is NOT the current branch. it must match the repo's real current branch dynamically, not a hardcoded/stale name.",
  "tasks": [
   "ior:instance:34d3439e-31de-4e21-87d2-2a54b12eac9f"
  ],
  "tests": [],
  "sourceFile": "ior:file:scrum.pmo/sprints/sprint-30-traceability-improvement/requirements.md",
  "parent": "ior:instance:2173e549-ca99-43e5-aea8-946b02141c13",
  "useCases": [
   "ior:instance:90f5e309-7ab2-49c6-b679-3ad891d5ab2f"
  ],
  "crossRef": [
   "ior:instance:4aa5ec49-34f0-41a5-b19e-5530cae2ad34",
   "ior:instance:12922d5d-470c-4044-a3a2-2f1248169b44"
  ],
  "crossRefNote": "R30.38 established the center header = filename@currentBranch (setCenterTitle 41504f5f <- currentBranch a2cbd78e/b9e8ba29). R30.40 is the CORRECTNESS follow-up: the branch value is WRONG (other clone's HEAD). Emerging root is R30.6.7 RepoRegistry: the 'oosh' key -> wrong clone. Distinct new bug -> next-free R30.40.",
  "discoverySource": {
   "type": "tron-bug",
   "flaggedBy": "Tron (v0.7.61 screenshot) via robbin-po; expert root-causing in parallel",
   "date": "2026-07-19",
   "note": "3-way editor bug B: center header otmux@dev-teampush-astray != the repo's real branch (mcdonges.latest). Expert (0.1) found the server's oosh root = the EAMD clone /home/shared/.../Once.sh/dev (HEAD dev-teampush-astray), not /root/oosh (mcdonges.latest). ROOT-CAUSE ALIGNMENT PENDING: expert confirms whether the 'oosh' RepoRegistry key must target the user's clone, or currentBranch must resolve against the right root. I re-point the impl-AC on confirm."
  },
  "acceptanceCriteria": [
   {
    "id": "AC-dynamic-branch",
    "group": "header",
    "text": "The center Result header shows the ACTUAL current checked-out branch of the repo the diff targets, resolved dynamically (git) at request time - matching the user's `git -C <repo> branch --show-current`."
   },
   {
    "id": "AC-not-stale",
    "group": "header",
    "text": "The header is NEVER a stale, cached, hardcoded, canonicalized, or OOSH_DIR-overridden branch; when HOME/oosh points to the mcdonges.latest worktree the header reads otmux@mcdonges.latest (not dev-teampush-astray)."
   },
   {
    "id": "AC-correct-clone",
    "group": "root",
    "text": "The 'oosh' RepoRegistry root resolves BY CONSTRUCTION to os.homedir()+'/oosh' - the HOME/oosh SYMLINK path (path.resolve, NOT realpath'd/canonicalized to a fixed worktree), NOT an OOSH_DIR env override. Git FOLLOWS the symlink live, so the header reflects the branch of WHATEVER worktree HOME/oosh currently points to (now mcdonges.latest); if `oo` mode-switch repoints the symlink (dev/macos/prod/mcdonges.latest), the header tracks it DYNAMICALLY. Never a misconfigurable env, never a canonicalized fixed worktree."
   },
   {
    "id": "AC-gate",
    "group": "gate",
    "text": "GATE (DET-3x + Tron visual): open the real deep-link -> the center header branch == the git current-branch of the targeted repo clone (dynamic); client-facing -> version-bump + atomic deploy (R30.28)."
   }
  ],
  "rootCauseConfirmed": "OOSH_DIR env override had 'oosh' -> the FIXED Once.sh/dev worktree (HEAD dev-teampush-astray) instead of the user's HOME/oosh. FIX (built, repo-registry.ts ROOTS.oosh): resolve 'oosh' to path.resolve(os.homedir(),'oosh') - the HOME/oosh symlink kept as-is (NOT canonicalized) so git follows it to the current oo-mode worktree; OOSH_DIR override dropped. Correct-by-construction (never a misconfigurable env). Impl 9b95b458 on RepoRegistry.resolve (distinct from d7dc0059). currentBranch/resolve code was already correct."
 },
 "ownerIor": "ior:instance:2173e549-ca99-43e5-aea8-946b02141c13"
}
