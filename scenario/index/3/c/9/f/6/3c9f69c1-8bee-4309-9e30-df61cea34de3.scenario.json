{
 "ior": "ior:class:Requirement",
 "model": {
  "uuid": "3c9f69c1-8bee-4309-9e30-df61cea34de3",
  "altId": "R30.44",
  "name": "[BACKLOG v1] Add a repository by entering a git clone URL then picking a checkout location to clone and register",
  "description": "In the add dialog, entering a GIT CLONE URL then choosing a CHECKOUT LOCATION clones the repo to that location and registers it in the dynamic registry. Shared IMPL-ENABLER (architect's decomposition, NOT a separate Tron-observable): the RepoRegistry becomes a DYNAMIC, mutable, persisted registry (add/register/clone/switch at runtime) - superseding the R30.40 static ROOTS allowlist. Architect derives the impl-units (dynamic-registry / add-local / add-clone / manage-switch); I mint/re-point methods+impls on confirm.",
  "tronQuote": "> TRON (v0.7.65): add a repo by entering a git clone URL, then pick a checkout location -> clone + register.",
  "tasks": [],
  "tests": [],
  "sourceFile": "ior:file:scrum.pmo/sprints/sprint-30-traceability-improvement/requirements.md",
  "parent": "ior:instance:2173e549-ca99-43e5-aea8-946b02141c13",
  "useCases": [
   "ior:instance:eb0902d5-b5f8-4c45-b27a-c89287e89a64"
  ],
  "crossRef": [
   "ior:instance:c5869b0a-8592-4d86-9250-9034a75e137c",
   "ior:instance:12922d5d-470c-4044-a3a2-2f1248169b44",
   "ior:instance:9a2c9c46-4def-4273-b896-60ad17b79a6a"
  ],
  "crossRefNote": "Repo add/manage feature. Shared IMPL-ENABLER (architect's decomposition, NOT a separate Tron-observable): the RepoRegistry becomes a DYNAMIC, mutable, persisted registry (add/register/clone/switch at runtime) - superseding the R30.40 static ROOTS allowlist. Architect derives the impl-units (dynamic-registry / add-local / add-clone / manage-switch); I mint/re-point methods+impls on confirm. crossRef R30.40 (static ROOTS -> dynamic), R30.6.7 (key resolution), R30.24 (selector/deep-link).",
  "discoverySource": {
   "type": "tron-feature",
   "flaggedBy": "Tron via robbin-po; architect deriving decomposition in parallel",
   "date": "2026-07-19",
   "note": "Add/manage git repositories via the repo selector. Observable ACs from Tron; impl-units PENDING architect derive."
  },
  "implSitePending": "Architect derives the impl-unit(s) for this observable; method(s) below are BEST-GUESS proposed names on RepoRegistry (dynamic) - re-point on architect confirm.",
  "acceptanceCriteria": [
   {
    "id": "AC-enter-url",
    "group": "add",
    "text": "The dialog accepts a GIT CLONE URL and a CHECKOUT LOCATION (server path); it clones the repo to that location."
   },
   {
    "id": "AC-clone-register",
    "group": "add",
    "text": "After a successful clone, the repo is registered in the dynamic registry and APPEARS in the selector."
   },
   {
    "id": "AC-clone-feedback",
    "group": "add",
    "text": "Clone progress/failure is surfaced (success -> repo usable; failure -> clear error, nothing half-registered)."
   },
   {
    "id": "AC-sec-clone-location",
    "group": "security",
    "text": "RATIFIED D1: the clone CHECKOUT LOCATION, after realpath, MUST be within the HOME subtree OR REPO_ALLOW; a location outside those bounds is REJECTED before cloning."
   },
   {
    "id": "AC-sec-clone-url",
    "group": "security",
    "text": "RATIFIED D2 (architect §9 Guard-2, built GitApi.assertAllowedUrl v0.7.67): The clone URL is validated by GitApi.assertAllowedUrl using the WHATWG URL parser (new URL(); NEVER legacy url.parse - its authority split is @-confusion-prone). ACCEPTED iff ALL hold: (a) scheme in {https, ssh} only (reject http/file/git/ext); (b) NO password component; (c) username is EMPTY (anonymous https) OR exactly \"git\" (ssh) - any other username rejected; (d) hostname EXACTLY matches an entry in HOST_ALLOW {github.com, <TEAM_GIT_HOST>}. Otherwise -> 400. The clone subprocess additionally runs env GIT_ALLOW_PROTOCOL=https:ssh + -c protocol.file.allow=never (defense-in-depth vs redirect/submodule escape). Exact-host is the PRIMARY @-confusion/SSRF defense: WHATWG sets host = substring after the LAST @, so credential-confusion forms (e.g. https://github.com@evil.com) resolve host=evil.com and are rejected."
   },
   {
    "id": "AC-sec-auth",
    "group": "security",
    "text": "RATIFIED D4: cloning requires the admin-key (all writes admin-key-gated)."
   },
   {
    "id": "AC-sec-persistence",
    "group": "security",
    "text": "RATIFIED D4: registration is an admin-key-gated write; a failed clone leaves NOTHING half-registered (atomic)."
   },
   {
    "id": "AC-sec-url-empty-username",
    "group": "security",
    "text": "RATIFIED D2 invariant (must-hold, mirror of the R30.39 over-reject bug): EMPTY username MUST remain ALLOWED - the rule is \"reject if username && username!==git\", NOT \"require username===git\" (else it re-breaks anonymous https clones). An anonymous https URL (no username) is ACCEPTED."
   },
   {
    "id": "AC-sec-url-whatwg-parser",
    "group": "security",
    "text": "RATIFIED D2 invariant (must-hold): the URL parser MUST be WHATWG new URL() (never legacy url.parse) - with both invariants held there is NO residual SSRF hole."
   },
   {
    "id": "AC-gate",
    "group": "gate",
    "text": "GATE (DET-3x + Tron visual): clone a URL to a location -> repo appears + opens a diff; client-facing -> version-bump."
   }
  ],
  "securitySurface": "SECURITY SURFACE RATIFIED by Tron D1-D4 (2026-07-19): D1 path-bounds = HOME-subtree + REPO_ALLOW, realpath-checked; D2 clone SSRF = host-allowlist (github.com+team) + https/ssh only (reject file/git/ext); D3 worktree switch = READ-ONLY ref-pick, worktree-as-key, NO server checkout; D4 auth = admin-key for ALL writes (register/clone/registry-mutation). Read-only actions need no admin-key. D2 URL-guard RATIFIED verbatim by architect §9 (WHATWG new URL, scheme https/ssh, no-password, username empty-or-git, exact-host allowlist; +GIT_ALLOW_PROTOCOL/protocol.file.allow=never).",
  "status": "backlog",
  "backlog": {
   "v1": false,
   "reason": "Tron V1 scope-simplification 2026-07-19: clone (add-by-URL) DEFERRED to backlog - needs the D2 clone-URL SSRF host-allowlist security (assertAllowedUrl) which is itself backlogged. Rationale kept: SSRF/@-confusion. V1 = add-local (.git-check) only; clone revisits post-v1 with the ratified D2 guard."
  }
 },
 "ownerIor": "ior:instance:2173e549-ca99-43e5-aea8-946b02141c13"
}
