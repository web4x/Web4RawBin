{
 "ior": "ior:class:Requirement",
 "model": {
  "uuid": "797113c5-5824-4d82-899f-89bc04886dff",
  "altId": "R31.10",
  "name": "Traceability tree resolves the correct UC.method in EVERY view — never sibling-fallback when UC.method is set",
  "description": "The traceability tree MUST resolve a UseCase to its OWN UC.model.method in EVERY view — trace-mode AND non-trace / scenario-mode — and MUST NOT fall back to a sibling method on the same Class when UC.method is set. ROOT (skill-expert audit R31-traceability-audit-RESULT.md, IMG_4647): the server attaches chainMethod from UC.model.method ONLY in queryMode==='trace' (server.ts:1644-1647); the client rb-trace-tree.buildSeedNode renders chainMethod if present, ELSE falls back to fetchAndRenderChildren (the Class's methods) — which surfaces a WRONG sibling (e.g. R31.9 UC drawer.observePosition resolving to R25.4 onGrabBarPointer on the shared Class RbDetailDrawer). This is a CODE fix (architect designs the Class/Method topology + the resolve path), not merely a data-wiring gap: even with UC.method correctly set (as R31.9's IS on disk), non-trace views mis-resolve. Captured per Tron completeness directive (R31-completeness-directive.md) — completeness of the tree resolution across all views.",
  "tronQuote": "> TRON (2026-07-24, completeness directive): the traceability chain is not complete in the tree — review the whole sprint and complete all missing scenarios; the tree must resolve every UC to its correct method in every view, not a sibling.",
  "tasks": [],
  "tests": [],
  "sourceFile": "ior:file:scrum.pmo/sprints/sprint-31-server-manager/R31-completeness-directive.md",
  "parent": "ior:instance:3c05f411-5e6e-4dfa-940e-b85ad55d7bea",
  "sprintName": "Sprint 31",
  "useCases": [],
  "unitLinks": [],
  "discoverySource": {
   "type": "team-discovery + tron-directive",
   "flaggedBy": "robbin-skill-expert audit (R31-traceability-audit-RESULT.md) + Tron completeness directive, via robbin-po",
   "date": "2026-07-24",
   "note": "Tree mis-resolution root: chainMethod attaches queryMode==='trace' only (server.ts:1644-1647); client buildSeedNode sibling-fallback when chainMethod absent. R31.9 observePosition->onGrabBarPointer was the acceptance probe. Architect designs the code fix; chain (UC->Class->Method->Impl->Test) mints onto the built fix."
  },
  "acceptanceCriteria": [
   {
    "id": "AC-resolve-correct-method-everywhere",
    "group": "functional",
    "text": "For a UseCase whose model.method is SET, the traceability tree resolves and displays THAT method (and its chain) in EVERY view — trace-mode AND non-trace / scenario-mode — never a sibling method on the same Class. Acceptance probe: R31.9 UC drawer.observePosition resolves to observePosition (e8097351), NOT onGrabBarPointer (R25.4), in non-trace/scenario views too."
   },
   {
    "id": "AC-no-sibling-fallback",
    "group": "functional",
    "text": "When UC.method is set, the client tree MUST NOT fall back to the Class's sibling-methods (the fetchAndRenderChildren path); the UC's own method resolution is authoritative in all query modes (the chainMethod attach is not gated to queryMode==='trace' alone). Architect owns the code fix."
   }
  ]
 },
 "ownerIor": "ior:instance:3c05f411-5e6e-4dfa-940e-b85ad55d7bea"
}
