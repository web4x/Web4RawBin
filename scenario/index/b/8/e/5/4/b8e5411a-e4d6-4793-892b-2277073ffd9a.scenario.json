{
 "ior": "ior:class:Requirement",
 "model": {
  "uuid": "b8e5411a-e4d6-4793-892b-2277073ffd9a",
  "name": "Scratch boot-smoke gate — a server change fires a FULL real-boot check against scratch config, on the LANDING FORM (build-not-deploy cannot bypass)",
  "altId": "R40.36",
  "tasks": [],
  "tests": [],
  "sourceFile": "ior:file:scrum.pmo/sprints/sprint-40-server-manager-deployment-mobile-input/requirements.md",
  "sourceLine": 1,
  "parent": "ior:instance:8e8b32d6-22bf-46f7-bf5c-7da31ef41e19",
  "sprintName": "Sprint 40",
  "unitLinks": [],
  "useCases": [],
  "description": "A SCRATCH BOOT-SMOKE GATE boots the REAL server against a SCRATCH config (random high port + scratch data dir + hard TIMEOUT) and asserts /api/config responds within N seconds — proving module-top boot succeeded (a crash-LOOP never answers, so the timeout catches it too). FAIL-CLOSED (no answer = RED). Isolated per R40.31 (no prod mutation, never data/users or a real room, cleanup-that-survives-failure). ★★ Fires on the LANDING FORM: triggered by ANY change to src/ts/server via pre-commit/CI, so a BUILD-NOT-DEPLOY landing CANNOT bypass it — the rule 'a server change needs a boot-check' already EXISTED and did NOT fire, because build-not-deploy does not LOOK like a deploy (the disguise pattern). Mechanize against the FORM (a server-file change), NOT the intent (a 'deploy'). FULL BOOT, not a unit test: the actual failure was INTEGRATION (startServers retry double-binding :4000) — a unit test of bootstrapSeed would have PASSED. WHY (recorded so it cannot be forgotten): an 8-MINUTE PROD OUTAGE on the R40.18 restart while Tron was connected, caused by parked B1 owner-safety code that had NEVER booted (its first boot was an unrelated deploy).",
  "tronQuote": "> (No direct Tron quote) — PO-directed outage-preventer capture 2026-08-12 after an 8-min prod outage on the R40.18 restart; architect designs the gate when flagged; do NOT execute tonight. (B1 owner-safety code = the OUTAGE CAUSE only; B1 itself stays PARKED for Tron's explicit go — this req is a boot-safety gate, NOT the security work.)",
  "discoverySource": {
   "type": "prod-incident-then-po-directed",
   "incident": "8-min prod outage on the R40.18 restart (parked B1 owner-safety code, first boot = unrelated deploy, module-top/startServers integration failure double-binding :4000)",
   "directedBy": "robbin-po",
   "date": "2026-08-12",
   "note": "Ranked ABOVE the R37.19 chain-integrity sweep (a prod outage already happened while Tron connected). Architect designs; req minted scenario-first so it cannot be forgotten."
  },
  "crossRef": [
   "ior:instance:70bbaec5-a445-457a-b123-db0f2822ab16"
  ],
  "crossRefNote": "Gate-safety / deploy-safety family (S40): R40.30 gate-ROT + R40.31 gate-POLLUTION (isolation/cleanup this reuses) + R40.32 SW-cache + R40.36 boot-smoke. campaignScope=OUTSIDE next-phase (R40.35 finishability precedent). Fire-on-the-FORM (any src/ts/server change) is the sibling of R40.25 post-deploy-device-gate 'NOT-RUN==RED' — a check that can be bypassed by a disguise is decorative.",
  "acceptanceCriteria": [
   {
    "id": "AC-real-boot-scratch",
    "group": "functional",
    "text": "The gate boots the REAL server (not a mock) against a SCRATCH config: random high port + scratch data dir + hard TIMEOUT; asserts /api/config responds within N seconds = module-top boot succeeded. A crash-LOOP never answers -> the timeout catches it. FAIL-CLOSED: no answer within the timeout = RED."
   },
   {
    "id": "AC-landing-form-trigger",
    "group": "gate",
    "text": "★ LOAD-BEARING: the gate fires on the LANDING FORM — triggered by ANY change to src/ts/server (pre-commit + CI), so a BUILD-NOT-DEPLOY landing CANNOT bypass it. Mechanize against the FORM (a server-file change), NOT the intent (a 'deploy'). The disguise pattern (build-not-deploy does not look like a deploy) is exactly why the pre-existing 'server change needs a boot-check' rule did not fire."
   },
   {
    "id": "AC-full-boot-not-unit",
    "group": "functional",
    "text": "It is a FULL BOOT integration check, NOT a unit test — the actual failure was INTEGRATION behaviour (startServers retry double-binding :4000); a unit test of bootstrapSeed would have PASSED. The gate exercises the whole module-top boot path end-to-end, not a function."
   },
   {
    "id": "AC-isolated-cleanup-survives",
    "group": "gate",
    "text": "Isolated per R40.31: scratch store BY-CONSTRUCTION, NO prod mutation (never data/users or a real room), cleanup-that-survives-failure/timeout/crash. The smoke gate can NEVER damage prod while running (R40.31 pollution-safety)."
   },
   {
    "id": "AC-stub-must-fail",
    "group": "gate",
    "text": "STUB-MUST-FAIL: plant a module-top throw (or a crash-loop) -> the smoke goes RED; a clean boot -> GREEN; weaken/remove the gate -> the bite suite goes RED (meta-bite). A boot-check that cannot fail certifies nothing."
   },
   {
    "id": "AC-why-recorded",
    "group": "framing",
    "text": "The requirement RECORDS WHY: the 8-minute prod outage on the R40.18 restart (parked B1 owner-safety code that had never booted; first boot = an unrelated deploy; module-top/startServers integration failure) — so the rationale is durable and the gate is never removed as 'unexplained'. B1 stays PARKED (this is a boot-safety gate, not the security work)."
   }
  ],
  "campaignScope": "OUTSIDE-S30++-finish-campaign-count (next-phase OUTAGE-PREVENTER, ranked ABOVE R37.19 sweep by PO; R40.35 finishability precedent; do NOT execute tonight — architect designs)"
 },
 "ownerIor": "ior:instance:8e8b32d6-22bf-46f7-bf5c-7da31ef41e19"
}